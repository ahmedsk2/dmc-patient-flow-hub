PRODUCTION READINESS — RE-SCORE — DMC Internal Medicine patient-flow hub (laravel/)          Verdict: NEEDS FIXES (62/100)
Run:   mixed · 8 categories re-audited fresh after the same-day fixes · 7 carried from the morning run · 2026-09-03T18:15:53   |   Emphasis view: 72/100
Prior: morning run 2026-09-03T15:11:44 — BLOCKED (58/100), emphasis 70

## Delta by category (weight-ordered; ● = re-audited this run)
| Category | Re-audited | Morning | Now | Δ | Crit | High | Med | Low |
|---|:-:|--:|--:|--:|--:|--:|--:|--:|
| Security & App Hardening | ● | 86 | 94 | +8 | 0 | 1 | 0 | 0 |
| Reliability, SLOs & Error Budgets |  | 0 | 0 | 0 | 0 | 0 | 1 | 0 |
| Data, Backups, DR & Correctness |  | 73 | 73 | 0 | 0 | 2 | 3 | 0 |
| Config, Secrets & Environments | ● | 76 | 76 | 0 | 0 | 1 | 2 | 0 |
| Resilience & Fault Tolerance | ● | 50 | 32 | -18 | 0 | 1 | 4 | 1 |
| Observability |  | 43 | 43 | 0 | 0 | 4 | 0 | 0 |
| Testing & Code Quality | ● | 57 | 73 | +16 | 0 | 0 | 3 | 0 |
| CI/CD & Release Engineering | ● | 49 | 74 | +25 | 0 | 2 | 1 | 1 |
| Operational Readiness & Incidents |  | 60 | 60 | 0 | 0 | 2 | 0 | 0 |
| Performance & Scalability | ● | 25 | 29 | +4 | 0 | 0 | 5 | 0 |
| Compliance, Privacy & Legal |  | 68 | 68 | 0 | 0 | 0 | 2 | 0 |
| Visual QA & Accessibility | ● | 79 | 79 | 0 | 0 | 1 | 1 | 0 |
| Documentation & Runbooks |  | 75 | 75 | 0 | 0 | 0 | 2 | 1 |
| Cost, FinOps & Sustainability |  | 100 | 100 | 0 | 0 | 0 | 0 | 0 |
| i18n, l10n & Text | ● | 71 | 94 | +23 | 0 | 0 | 0 | 1 |

Totals: Critical 2 → **0** · High 19 → **14** · overall 58 → **62** · emphasis 70 → **72**

## Remaining High findings
- **HIGH** [SEC-11] Multi-party authorization and tamper-evident audit on the control plane (Security & App Hardening) — gh api branch-protection endpoint verified live 2026-09-03, no required_pull_request_reviews key present, mean
- **HIGH** [DATA-02] Backups automated, encrypted, and stored off-region (Data, Backups, DR & Correctness) — laravel/docs/BACKUP-AND-RESTORE.md:76-93 (section 2.3 config: S3_REGION=me-riyadh-1, same endpoint as AUDIT_S3
- **HIGH** [DATA-04] Point-in-time recovery and DR drift detection (Data, Backups, DR & Correctness) — laravel/docs/BACKUP-AND-RESTORE.md:28-29
- **HIGH** [CFG-10] Cloud & service-to-service auth via short-lived federated identity (Config, Secrets & Environments) — laravel/config/services.php lines 42-46 (AUDIT_S3_ACCESS_KEY/AUDIT_S3_SECRET); laravel/app/Support/S3SigV4.php
- **HIGH** [OBS-01] Structured logging to stdout/stderr (Observability) — laravel/config/logging.php:21,56-71; laravel/.env.example:27-31; laravel/docs/DEPLOY-LARAVEL.md:216,298
- **HIGH** [OBS-03] Error tracking wired with a real DSN (Observability) — laravel/composer.json:8-15 (no sentry/bugsnag/rollbar/honeybadger package); laravel/.env.example:37-39
- **HIGH** [OBS-04] RED / golden-signal metrics as histograms (Observability) — missing: metrics client (no prom-client/prometheus/opentelemetry-metrics/micrometer/statsd package in laravel/
- **HIGH** [OBS-05] Distributed tracing with W3C context propagation (Observability) — missing: tracing SDK (no opentelemetry/opentracing/jaeger/zipkin/ddtrace/elastic-apm package); outbound hops c
- **HIGH** [CICD-05] Supply-chain provenance: SBOM, signed attestations, pinned actions/images (CI/CD & Release Engineering) — missing: SBOM generation such as syft, cyclonedx or cdxgen, and signing or attestation such as cosign or a SLS
- **HIGH** [CICD-08] Fully scripted, idempotent deploy with post-deploy smoke test + auto-rollback (CI/CD & Release Engineering) — laravel/scripts/smoke.sh is invoked manually by the deployer from their own workstation after a deploy, per do
- **HIGH** [RES-12] Correct graceful shutdown / disposability (Resilience & Fault Tolerance) — laravel/docs/DEPLOY-LARAVEL.md section 1 step 5 (start new container, wait, repoint Traefik, stop the old one,
- **HIGH** [OPS-02] On-call rotation staffed, sustainable, with escalation (Operational Readiness & Incidents) — laravel/docs/compliance/INCIDENT-RESPONSE.md:36-48 (Roles table - every holder is [PLACEHOLDER]); Appendix A:4
- **HIGH** [OPS-06] Progressive delivery with a fast, practiced rollback / kill-switch (Operational Readiness & Incidents) — laravel/docs/DEPLOY-LARAVEL.md:180-187 (section 4.4 rehearsal table - both the app-only Coolify rollback drill
- **HIGH** [UX-04] Accessible names: alt text, form labels, control labels (Visual QA & Accessibility) — STILL BROKEN, unchanged: Auth/ForgotPassword.vue:21-22; Auth/ForgotUsername.vue:21; Auth/ResetPassword.vue:27-

## Orchestrator notes on this re-score

- **What was re-audited and why.** The eight categories touched by the same-day fixes (PR #7 deployed as `3fbbd73`; branch rule; database session/cache store) were re-run fresh by new auditor instances; the other seven carry the morning results unchanged (their inputs did not change). Scope, weights, and the near-binary caveats from the morning report still apply.
- **Verified live, not inferred:** `main` now rejects direct pushes — a probe push was refused with "4 of 4 required status checks are expected" (GH006) — and production runs `SESSION_DRIVER=database` / `CACHE_STORE=database` (a request writes a `sessions` row). The testing auditor's intake note that "GitHub does not block a direct push" predates that probe and is superseded by it.
- **Auditor variance, stated plainly.** Resilience moved 50 → 32 even though its morning High (`RES-01`, SMTP timeout) is fixed: the new instance surfaced findings the morning one did not — chiefly `RES-12`, no drain step on the Coolify container swap, which the deploy runbook itself acknowledges — and it graded one previously-passing check as a fail. That is a real gap set, not a regression, but the *number* reflects a different reader as much as changed code. Treat category deltas on re-audited rows as indicative, the finding lists as the substance.
- **Accessibility scope.** `UX-04` stays a High: the two forms named in the morning finding (Login, Admission) are fixed and CI-guarded, but the same unpaired-label pattern remains on six others (ForgotPassword, ForgotUsername, ResetPassword, Profile/Edit, Audit/Index, Registry/Index). Same `fid()` fix, mechanical.
- **Security's remaining High** (`SEC-11`, multi-party authorization on the control plane) cannot be satisfied by a single-maintainer repository with required reviewers; the enforced required checks and Auto Deploy being off are the compensating controls. Decide whether to waive it (`.prod-ready/waivers.yml`, with an expiry) rather than carry it as a permanent red.
- **PHP coverage floor** was attempted (pcov + `artisan test --coverage --min`) and removed because Collision rendered no coverage table in CI on PHPUnit 12; the Vitest thresholds are the enforced floor. Open item.
- **Not re-run:** the legacy daily system on `dmc-im.com` is outside this scope and still graded F for headers/TLS; whole-unit readiness remains bounded by it until cutover.

## Open items after this run (ranked)

1. Automate the post-deploy smoke + rollback in the pipeline, and add a drain/grace step to the container swap [CICD-08, RES-12].
2. Structured JSON logs to stdout with retention off the container, and an error-tracking sink [OBS-01/03]; metrics/tracing later [OBS-04/05].
3. Binlog shipping (RPO) and a second in-Kingdom region for backups [DATA-02/04].
4. Name the incident roles and on-call path; appoint the DPO; sign the controller–processor contract [OPS-02/06, CMP-03/06].
5. Replace the static OCI object-storage keys with an instance-principal identity [CFG-10].
6. Pair labels on the six remaining forms [UX-04]; `__Host-` session-cookie prefix [G14].
7. PHP coverage floor (diagnose Collision's table first) and ESLint for the Vue side [TST-02/04].
8. Decide on a dated waiver for SEC-11, or add a second maintainer.

---

# Full current scorecard and findings (mechanical; re-audited categories fresh, others carried from the morning run)


## Scorecard
| Category | W | Score | ● | Crit | High | Med | Low | Pass | N/A | Intake |
|---|--:|--:|:-:|--:|--:|--:|--:|--:|--:|--:|
| Security & App Hardening | 10 | 94 | 🟡 | 0 | 1 | 0 | 0 | 11 | 0 | 0 |
| Reliability, SLOs & Error Budgets | 9 | 0 | 🟡 | 0 | 0 | 1 | 0 | 0 | 2 | 5 |
| Data, Backups, DR & Correctness | 9 | 73 | 🟡 | 0 | 2 | 3 | 0 | 8 | 1 | 0 |
| Config, Secrets & Environments | 9 | 76 | 🟡 | 0 | 1 | 2 | 0 | 6 | 2 | 1 |
| Resilience & Fault Tolerance | 8 | 32 | 🟡 | 0 | 1 | 4 | 1 | 2 | 3 | 2 |
| Observability | 8 | 43 | 🟡 | 0 | 4 | 0 | 0 | 2 | 5 | 0 |
| Testing & Code Quality | 8 | 73 | 🟡 | 0 | 0 | 3 | 0 | 6 | 2 | 0 |
| CI/CD & Release Engineering | 8 | 74 | 🟡 | 0 | 2 | 1 | 1 | 7 | 1 | 0 |
| Operational Readiness & Incidents | 7 | 60 | 🟡 | 0 | 2 | 0 | 0 | 4 | 3 | 3 |
| Performance & Scalability | 6 | 29 | 🟡 | 0 | 0 | 5 | 0 | 1 | 1 | 3 |
| Compliance, Privacy & Legal | 6 | 68 | 🟡 | 0 | 0 | 2 | 0 | 3 | 2 | 1 |
| Visual QA & Accessibility | 5 | 79 | 🟡 | 0 | 1 | 1 | 0 | 8 | 0 | 0 |
| Documentation & Runbooks | 4 | 75 | 🟡 | 0 | 0 | 2 | 1 | 5 | 1 | 0 |
| Cost, FinOps & Sustainability | 4 | 100 | 🟢 | 0 | 0 | 0 | 0 | 3 | 2 | 5 |
| i18n, l10n & Text | 3 | 94 | 🟢 | 0 | 0 | 0 | 1 | 6 | 0 | 0 |

N/A: AI/LLM & Agentic Readiness (no model usage) · API & Contract Lifecycle (self-contained app, no versioned API or events) · Infrastructure, Containers & IaC (No Dockerfile/Containerfile, docker-compose file, Kubernetes/Helm manifest, IaC (Terraform/CloudFormation/Pulumi/Bicep/CDK/ARM), or platform-deploy config (vercel.json, netlify.toml, fly.toml, render.yaml, Procfile, app.yaml, railway.json, nomad files, nixpacks.toml) exists anywhere in the repository (verified by exhaustive filename search across the whole DMC repo excluding vendor and node_modules). The Laravel app deploys via Coolify v4 plus Nixpacks on a single OCI host (laravel/docs/DEPLOY-LARAVEL.md): Coolify auto-detects the build pack from composer.json and package.json, and all platform configuration -- build pack selection, base directory, domain, health-check path (/up), env vars and their build-time versus runtime split, and the deploy-time migrate --force step -- lives in the Coolify UI/API, not as a committed artifact. This is exactly the categorys own carve-out: an app deployed only via an opaque managed PaaS with no committed config. Every one of the 10 checks self-skips per the categorys own N/A logic (no Dockerfile means INFRA-01 is skipped; no k8s manifests means INFRA-02/03/04/05/07/09 are skipped; no IaC means INFRA-06 is skipped; no platform-deploy file means INFRA-08 is skipped; CI here builds no container or IaC artifact, only runs composer/npm install, PHPUnit, Vitest, gitleaks and Semgrep, so INFRA-10 is skipped too), so the whole category is N/A and excluded from the weighted score.)

## Findings
🟠 HIGH · [OBS-01] Structured logging to stdout/stderr            confidence: high   (Observability)
   Where:  laravel/config/logging.php:21,56-71; laravel/.env.example:27-31; laravel/docs/DEPLOY-LARAVEL.md:216,298
   Why:    Default channel is stack -> single (config/logging.php:21,56-64), a Monolog line-formatted file at storage_path(logs/laravel.log), and production is documented to run LOG_CHANNEL=daily (still file-based, DEPLOY-LARAVEL.md:216), not JSON to stdout/stderr. The project's own ops doc states that sessions and logs live in the container and that a redeploy discards the previous container's log files unless storage/ is mounted as a persistent volume (DEPLOY-LARAVEL.md:298) - on this single-instance, no-staging, frequently-redeployed host, every deploy silently discards the prior container's log history unless a volume happens to be mounted. Logs are also not JSON, so they are not directly machine-parseable in aggregation.
   Fix:    Set LOG_CHANNEL=stderr (or add a JSON formatter to the daily/single channel) so Monolog emits JSON lines to stdout/stderr per 12-factor; keep LOG_LEVEL configurable via env as today. Optionally mount storage/ as a persistent Coolify volume as a stopgap, but stdout-based shipping is the durable fix.
   Auto-fixable: yes

🟠 HIGH · [OBS-03] Error tracking wired with a real DSN            confidence: high   (Observability)
   Where:  laravel/composer.json:8-15 (no sentry/bugsnag/rollbar/honeybadger package); laravel/.env.example:37-39
   Why:    No error-tracking SDK is present anywhere in the stack (composer.json lists only dompdf/inertia-laravel/framework/tinker/openspout - zero APM or error-tracking packages; package.json has no frontend error tracker either). .env.example:37-39 documents SENTRY_DSN as a not-yet-done step (comment: composer require sentry/sentry-laravel, then set the DSN) and DEPLOY-LARAVEL.md:216 lists SENTRY_DSN as optional/inert while empty. Combined with OBS-01's file-based, ephemeral logging, unhandled exceptions in this PHI-holding clinical system have no aggregation, no alerting and no MTTR signal - they only exist in a per-container log file that a redeploy can discard.
   Fix:    Run composer require sentry/sentry-laravel (or an equivalent), publish its config, set SENTRY_DSN/environment/release from env to a real non-empty production value, and verify it captures a test exception before relying on it.
   Auto-fixable: yes

🟠 HIGH · [OBS-04] RED / golden-signal metrics as histograms            confidence: high   (Observability)
   Where:  missing: metrics client (no prom-client/prometheus/opentelemetry-metrics/micrometer/statsd package in laravel/composer.json or laravel/package.json)
   Why:    No request-rate, error-count or duration-histogram instrumentation exists anywhere in the app or its scheduled console commands (audit:ship, backup:verify, dq:notify, the monthly report job). Tail latency (p95/p99) and saturation (DB pool, disk, CPU/mem) are invisible; no SLO can be computed for the clinical board/registry/reports pages, and a slow-query or resource-exhaustion incident has no metric signal, only whatever an operator happens to notice manually.
   Fix:    Add a metrics client (e.g. an OpenTelemetry PHP SDK or a Prometheus PHP exporter) recording request duration as a histogram, an error counter labeled by outcome, and saturation gauges for the DB connection pool and disk; expose a scrape endpoint or push to a backend.
   Auto-fixable: no

🟠 HIGH · [OBS-05] Distributed tracing with W3C context propagation            confidence: high   (Observability)
   Where:  missing: tracing SDK (no opentelemetry/opentracing/jaeger/zipkin/ddtrace/elastic-apm package); outbound hops confirmed at laravel/app/Support/S3SigV4.php (audit shipping + backups to S3-compatible buckets), config/database.php (MySQL), and SMTP mail sends via RuntimeConfigServiceProvider-configured mail settings
   Why:    The app makes real outbound calls (MySQL, an SMTP relay on US hosting, S3-compatible buckets for audit shipping and backups) with zero tracing. A slow or failing dependency (e.g. the SMTP relay, or the audit-ship S3 call) cannot be isolated from application-side slowness; there is no span to show which hop an incident lives in.
   Fix:    Adopt an OpenTelemetry PHP SDK with auto-instrumentation (or manual spans) for the DB client, the SMTP send, and S3SigV4 HTTP calls; register a W3C traceparent propagator; set SpanKind and error status on failures.
   Auto-fixable: no

🟠 HIGH · [SEC-11] Multi-party authorization and tamper-evident audit on the control plane            confidence: high   (Security & App Hardening)
   Where:  gh api branch-protection endpoint verified live 2026-09-03, no required_pull_request_reviews key present, meaning no required reviewers; gh api environments endpoint returns total_count 0, no protected deployment environment; no CODEOWNERS file anywhere in the repo after an exhaustive search; CLAUDE.md section 2 states main is the single Laravel dev branch and direct-to-main is the consented workflow, an explicit owner-consented single-operator model
   Why:    A single compromised or mistaken credential can push code straight to main and have it manually deployed to the one production environment holding real PHI for about 17k patients, with no second person review and no audit trail distinguishing author from authorizer. This is a real, detectable-from-config gap. It is improved but not closed since the last run: the four CI jobs are now required, admin-enforced status checks, and Coolify Auto Deploy is now off so deploys are operator-triggered rather than firing on raw push, which closes the worse auto-deploy scenario but does not add a second-person approval gate
   Fix:    Add a CODEOWNERS entry for the workflows directory, the deploy runbook, and any future pipeline or IaC so review is structurally required the day a second trusted person joins. Until then, record this explicitly as an accepted single-operator risk with an owner sign-off and review date rather than leaving it an implicit gap
   Auto-fixable: no

🟠 HIGH · [CICD-05] Supply-chain provenance: SBOM, signed attestations, pinned actions/images            confidence: high   (CI/CD & Release Engineering)
   Where:  missing: SBOM generation such as syft, cyclonedx or cdxgen, and signing or attestation such as cosign or a SLSA generator, anywhere in .github/workflows/laravel-ci.yml or in the Coolify build described in laravel/docs/DEPLOY-LARAVEL.md S1. What is done well: every uses line is pinned to a full commit SHA and the Semgrep image is pinned to a manifest digest, documented with a bump procedure in docs/CI.md, so this is only a partial pass on the pinning half of the check.
   Why:    A compromised transitive Composer or npm package, or a tampered Coolify-built image, would ship to the single production environment holding real PHI with no per-release manifest of what is inside and no cryptographic proof of provenance; nothing in this pipeline would detect it.
   Fix:    Add an SBOM step to the backend and frontend CI jobs, for example cyclonedx-php-composer and the cyclonedx npm package, and archive it as a release artifact; consider signing the Coolify-built image or at minimum recording its digest and SBOM alongside a release tag, once release tagging is actually used (see CICD-12).
   Auto-fixable: yes

🟠 HIGH · [CICD-08] Fully scripted, idempotent deploy with post-deploy smoke test + auto-rollback            confidence: high   (CI/CD & Release Engineering)
   Where:  laravel/scripts/smoke.sh is invoked manually by the deployer from their own workstation after a deploy, per docs/DEPLOY-LARAVEL.md S3.3, not wired into the Coolify pipeline or CI; rollback in S4 is a human-run runbook, either a Coolify UI Rollback for app-only or a manual DB restore, started on human judgement against documented trigger criteria, not an automated gate; the rollback-drill table in S4.4 has both rows unchecked, marked as not yet done, meaning the five-minute target and the restore procedure have never actually been executed or timed
   Why:    No automated check verifies a release actually works before it serves all production traffic, and no automated trigger reverts a bad one; recovery depends on a human noticing within minutes and manually running steps that have never been rehearsed end to end. The only automated safety net is the up liveness probe that Coolify itself provides, per DEPLOY-LARAVEL.md S9, so a container that boots but is functionally broken, for example a 500 on the patient board or a broken MFA challenge, stays live until a person catches it. Mitigating factors keep this at High rather than Critical: the deploy itself is fully scripted end to end, the manual smoke test is unusually thorough, and a mandatory pre-deploy dump exists for the DB-restore path.
   Fix:    Wire scripts/smoke.sh into the deploy itself, for example a Coolify post-deploy hook or a small job that calls the Coolify deploy API, runs smoke.sh, and calls the Rollback API on any FAIL, so traffic gating and rollback trigger automatically instead of depending on a person; separately, actually run and time both rows in DEPLOY-LARAVEL.md S4.4 so the five-minute target and the restore procedure are proven, not just documented.
   Auto-fixable: no

🟠 HIGH · [RES-12] Correct graceful shutdown / disposability            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/docs/DEPLOY-LARAVEL.md section 1 step 5 (start new container, wait, repoint Traefik, stop the old one, with no drain step described) and section 2 (explicitly: the swap itself is seconds, but any user mid-form at that instant loses the form); no Dockerfile or entrypoint in the repo since the build is Nixpacks-generated, so there is no visible SIGTERM trap, preStop delay or drain deadline
   Why:    Every deploy that swaps containers can drop an in-flight request or an active staff member mid-form submission with no drain or grace period, and the runbook states this outcome explicitly rather than it being a hypothesis. Since 2026-09-03 the session itself survives the swap (database session driver), but a request mid-flight at the exact swap instant is not gracefully finished first.
   Fix:    Configure the container stop behaviour to allow a grace window before a hard kill, confirm the PHP process manager actually finishes in-flight requests during that window before the old container is torn down, and document the configured grace period next to the existing deploy-window and notice mitigation already in the runbook.
   Auto-fixable: no

🟠 HIGH · [DATA-02] Backups automated, encrypted, and stored off-region            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/BACKUP-AND-RESTORE.md:76-93 (section 2.3 config: S3_REGION=me-riyadh-1, same endpoint as AUDIT_S3_*); laravel/config/services.php:41,54; laravel/.env.example:149-157
   Why:    Backups ARE automated (nightly cron), encrypted (AES-256-CBC/PBKDF2, key escrowed) and genuinely off-box (a separate OCI Object Storage bucket, not the DB host disk) - this closes the prior real risk of plaintext dumps sitting next to the database. But the backup bucket is in the SAME OCI region (me-riyadh-1) and the SAME tenancy/credentials family as production, in a DIFFERENT bucket only. A region-wide OCI outage or a compromise/suspension at the tenancy level could still take out primary and backup together - real but bounded correlated-loss risk. Downgraded from the category default Critical to High because: (a) the most likely loss vectors (disk failure, host compromise, bad migration) are genuinely mitigated by the off-box design; (b) Saudi PDPL mandates in-Kingdom residency, which constrains true cross-region replication as an option - this is a deliberate, documented regulatory trade-off, not an oversight.
   Fix:    Segregate the backup bucket into a separate OCI compartment/tenancy with independent IAM credentials not derived from the same root/administrative account as production, as defense-in-depth against tenancy-level compromise. Confirm with OCI whether a second Kingdom-resident region exists and evaluate a second in-Kingdom copy under separate administrative control. Document the residual correlated-loss risk and get an explicit owner risk-acceptance given the PDPL residency constraint.
   Auto-fixable: no

🟠 HIGH · [DATA-04] Point-in-time recovery and DR drift detection            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/BACKUP-AND-RESTORE.md:28-29
   Why:    No PITR/binlog shipping exists - recovery granularity is capped at the previous night 02:15 dump, so up to ~24h of admissions, discharges, transfers and consultations can be permanently lost in a worst-case incident (explicitly acknowledged in the doc). Clinicians would have to re-enter that window from paper/HIS records. Separately, there is no IaC for the DR/backup infrastructure (Coolify plus hand-run host cron/scripts) and therefore no automated drift detection between what is documented and what is actually configured on the host.
   Fix:    Enable MySQL binary logging with off-box shipping (or a managed point-in-time-recovery mechanism) if the hospital actual RPO tolerance is tighter than 24h (confirm via the DATA-12 BIA intake first). If binlog shipping is out of scope, increase backup frequency and/or document DR-infra-as-code with a scheduled drift check so the host configuration cannot silently diverge from the runbook.
   Auto-fixable: no

🟠 HIGH · [CFG-10] Cloud & service-to-service auth via short-lived federated identity            confidence: high   (Config, Secrets & Environments)
   Where:  laravel/config/services.php lines 42-46 (AUDIT_S3_ACCESS_KEY/AUDIT_S3_SECRET); laravel/app/Support/S3SigV4.php (signs every request with a static long-lived key/secret pair); laravel/.env.example DB_BACKUP_S3_* / AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY use the same pattern
   Why:    Hourly audit-log shipping (audit:ship) and backup:verify authenticate to OCI Object Storage with a static, non-expiring access-key/secret pair injected via env vars. The app already runs on an OCI compute instance where Instance Principal (workload identity) could remove the standing credential entirely; instead a leaked or misconfigured key is a durable credential with no auto-rotation and, per CFG-03, unconfirmed IAM scoping.
   Fix:    Migrate to OCI Instance Principal (native REST API) for the compute-instance workload, eliminating AUDIT_S3_ACCESS_KEY/SECRET; if the S3-compatible path-style endpoint must be kept, split audit vs. backup into two separately-scoped least-privilege IAM keys and put both on a documented rotation cadence.
   Auto-fixable: no

🟠 HIGH · [OPS-02] On-call rotation staffed, sustainable, with escalation            confidence: high   (Operational Readiness & Incidents)
   Where:  laravel/docs/compliance/INCIDENT-RESPONSE.md:36-48 (Roles table - every holder is [PLACEHOLDER]); Appendix A:418-433 (contact tree, every cell blank); corroborated by laravel/docs/compliance/EVIDENCE-PACK.md:126 (G9: named roles not designated)
   Why:    No named, staffed responder or 2-tier escalation path exists for this live-PHI system. If an incident happens today it depends on one person (the developer/operator) being reachable - a bus-factor-1 single point of failure with no documented backup, and the incident runbook's own severity-based paging table (section 2) has no one to actually page.
   Fix:    Name primary and deputy holders for every role in INCIDENT-RESPONSE.md section 3 and Appendix A (incident lead, technical lead, DPO/legal, clinical owner). At minimum stand up a 2-person escalation path (e.g. a shared phone/chat alert that reaches two people) even without a formal pager tool, and keep it current.
   Auto-fixable: no

🟠 HIGH · [OPS-06] Progressive delivery with a fast, practiced rollback / kill-switch            confidence: high   (Operational Readiness & Incidents)
   Where:  laravel/docs/DEPLOY-LARAVEL.md:180-187 (section 4.4 rehearsal table - both the app-only Coolify rollback drill and the scratch-DB restore drill are marked as not yet done); laravel/docs/RELEASE-CHECKLIST.md:85 (last rollback drill date / measured time left blank)
   Why:    The rollback mechanism itself is real and specific (immutable per-commit-SHA Coolify images make app-only rollback a genuine ~2-3 min swap; DB rollback is a documented dump-restore, correctly never migrate:rollback - section 4.3), which is why this is High rather than Critical. But it has never been exercised end-to-end, so the 5-minute target and the DB-restore path are unverified under real conditions - a bad release could take materially longer to revert than assumed, at exactly the moment speed matters.
   Fix:    Run the two drills already scaffolded in DEPLOY-LARAVEL.md section 4.4 (app-only Coolify Rollback + smoke.sh; restore a current-size dump into the scratch DB + audit:verify) at least once, record the measured times in the table, and repeat periodically (e.g. quarterly or after significant deploy-pipeline changes).
   Auto-fixable: no

🟠 HIGH · [UX-04] Accessible names: alt text, form labels, control labels            confidence: high   (Visual QA & Accessibility)
   Where:  STILL BROKEN, unchanged: Auth/ForgotPassword.vue:21-22; Auth/ForgotUsername.vue:21; Auth/ResetPassword.vue:27-29; Profile/Edit.vue:59-60,78-80 (5 fields); Audit/Index.vue:98-99; Registry/Index.vue:263. FIXED today (verified 2026-09-03, commit 14e4e02): Auth/Login.vue (both fields now use a fid() unique-id helper with for/id pairing and aria-describedby, resources/js/Pages/Auth/Login.vue:11-12,64-77) and Admissions/Create.vue (all 10 fields, resources/js/Pages/Admissions/Create.vue:12-13,44-106), each with a new regression-guarding vitest-axe spec (resources/js/Pages/Auth/__tests__/Login.a11y.spec.js, resources/js/Pages/Admissions/__tests__/Create.a11y.spec.js; both re-run locally this pass: 6/6 passing) that runs inside the required Frontend CI job via the unrestricted npx vitest run --coverage step.
   Why:    The app own front door (Login) and its core clinical workflow (new admission, 10 fields) are fixed and now regression-guarded by a required CI check, a real, verified improvement closing the two highest-traffic instances of this defect. However the check is scored on the whole category-relevant surface: six other forms (forgot-password, forgot-username, reset-password, profile edit with 5 fields, admin audit search, one registry filter) still have bare sibling label elements with no for/id/aria-label, so a screen-reader user tabbing into those controls still hears only edit text with no field name. The check remains a fail until the remaining forms are brought in line with the now-established house pattern.
   Fix:    Apply the same fid()/useId() pairing already shipped on Login.vue and Admissions/Create.vue to ForgotPassword.vue, ForgotUsername.vue, ResetPassword.vue, Profile/Edit.vue, Audit/Index.vue and the flagged Registry/Index.vue filter; add a vitest-axe spec per page following the two new specs as the template.
   Auto-fixable: yes

🟡 MEDIUM · [REL-05] Symptom-based SLO burn-rate alerting            confidence: medium   (Reliability, SLOs & Error Budgets)
   Where:  laravel/app/Console/Commands/BackupVerify.php:94-96 (backup age threshold), laravel/app/Console/Commands/AuditVerifyDaily.php (hash-chain break alert), laravel/app/Http/Controllers/Auth/AuthController.php:147-171 (failed-login-count threshold), laravel/app/Console/Commands/DataQualityNotify.php (data-quality threshold)
   Why:    The alerting that does exist (in-app admin notifications: backup.stale, audit.integrity_failure, security.failed_logins, data-quality thresholds) is entirely cause-based and single-threshold, not tied to any SLO or symptom of user-facing impact. There is no burn-rate or multi-window alert because there is no SLI/SLO to burn against (REL-01/REL-02). A slowly-degrading user-facing problem, such as rising page latency or a rising error rate on admissions or discharges, would not raise any alert today, even though the existing operational alerts (backup, audit chain, brute force, data quality) are real and reasonably well targeted for a system at this scale.
   Fix:    Once SLIs/SLOs exist, add a small number of symptom-based alerts (e.g. error-rate or latency burn against the SLO) on top of the existing cause-based operational alerts, which are worth keeping since they cover genuine non-SLO risks (backup integrity, audit tamper-evidence, brute force).
   Auto-fixable: no

🟡 MEDIUM · [TST-02] Coverage thresholds + critical-path coverage            confidence: high   (Testing & Code Quality)
   Where:  laravel/vitest.config.js:33-39 (JS coverage threshold enforced: lines 75/statements 75/branches 76/functions 48, verified green this session at 77.49%/77.49%/78.77%/50.71%) and .github/workflows/laravel-ci.yml:83-85 (vitest run --coverage, blocking); vs .github/workflows/laravel-ci.yml:138-142 (coverage: none on the PHP setup step; a pcov-based coverage floor was tried on 2026-09-03 and removed because a coverage table never rendered in the CI log, so no PHP threshold exists today)
   Why:    The frontend now has a real, enforced, honestly-set coverage floor, but the PHP backend, which holds essentially all of the clinical business logic (admissions, consultations, MFA, audit hash-chain, encrypted narratives), still has no coverage gate, so a change that quietly deletes or bypasses PHP tests for a risky module would not be caught by a coverage regression on that side. Downgraded from the category default High because critical-path breadth itself is strong on the PHP side (AuthorizationTest, MandatoryMfaTest, StepUpTest, ClinicalFlowTest, PatientMergeTest, AuditHashChainTest, ClinicalNarrativeEncryptionTest and more all exist per the 106-file Feature suite) and the JS gap that used to exist is now closed with a documented, non-inflated baseline; it is specifically the PHP threshold that is missing, not test coverage itself.
   Fix:    Re-attempt a PHP coverage floor via a path that does not depend on a rendered coverage table: generate a Clover/text coverage report with pcov or xdebug (php artisan test --coverage-clover=coverage.xml) and gate on it with a small script that parses the XML and fails below a threshold, instead of relying on a --min flag plus console table output.
   Auto-fixable: no

🟡 MEDIUM · [TST-04] Lint passes            confidence: high   (Testing & Code Quality)
   Where:  PHP: laravel/composer.json (laravel/pint ^1.27) + .github/workflows/laravel-ci.yml:180-184 (blocking vendor/bin/pint --test step, deliberately last); verified locally this session: vendor/bin/pint --test reports passed, 0 violations. JS/Vue: no .eslintrc* or eslint.config.* anywhere under laravel/, no eslint devDependency in package.json (verified); roughly 88 Vue/JS source files (vs 96 PHP files in app/) have zero static linting
   Why:    The PHP half of the stack is now fully solved: Pint is a real, blocking, currently-clean CI gate, a major improvement over the prior audit, which found roughly 140 of 150 files in violation because Pint had never actually been run as a gate. The JS/Vue half, roughly half the codebase by file count, including all Inertia page components, has no linter at all, so unused variables, undefined references and other mechanically-catchable Vue/JS mistakes merge unchecked.
   Fix:    Add ESLint (flat config) plus eslint-plugin-vue, an npm run lint script, and wire it into the frontend CI job alongside the existing Vitest/build/style gates.
   Auto-fixable: yes

🟡 MEDIUM · [TST-10] Async-pattern safety            confidence: medium   (Testing & Code Quality)
   Where:  laravel/resources/js/composables/useHandover.js lines 22-44 (saveHandover/fetchHandover/preflight: raw fetch() with no catch and no try/catch around the await); laravel/resources/js/Layouts/AppLayout.vue lines about 292-320; laravel/resources/js/Components/QuickJump.vue line 99; laravel/resources/js/Pages/Auth/Register.vue line 26; laravel/resources/js/Pages/Admin/PatientMerge.vue lines 52 and 248; grep for unhandledrejection/window.onerror across laravel/resources/js: zero matches, no global handler
   Why:    Re-verified unchanged from the prior audit: if one of these raw fetch() calls fails (network drop, 500, JSON parse error), the rejection is unhandled; loading/saving flags still reset via finally, but the user gets no error feedback and the failure is invisible beyond a browser console warning (no error-tracking SDK is installed). Severity is bounded because most writes in the app go through the Inertia router, which has its own error handling; this affects only the smaller set of raw-fetch composables/handlers, including one clinical write path (handover save).
   Fix:    Add a catch/try-catch branch to each raw-fetch async helper that surfaces a toast or error state, and add one top-level unhandledrejection listener in resources/js/app.js as a last-resort net.
   Auto-fixable: no

🟡 MEDIUM · [CICD-11] Privileged release/control-plane actions require multi-party authorization            confidence: high   (CI/CD & Release Engineering)
   Where:  gh api repos/ahmedsk2/dmc-patient-flow-hub/collaborators shows a single collaborator, ahmedsk2; no CODEOWNERS file exists anywhere in the repo; gh pr view 7 shows it was merged by its own author with zero reviews and an empty review decision, even though the live branch-protection rule now shows required_approving_review_count as 1 with enforce_admins true, a live contradiction, since no second human can supply that review when the repo has no other collaborator, yet the last merge went in with none anyway. Coolify deploys are triggered by the same single operator; per the RE-SCORE context, Auto Deploy is off and deploy is a manual single-actor API or UI action with no second-approver gate.
   Why:    One person can write code, merge it with or without a review as PR 7 shows, and push it to the only production environment holding real PHI, with no second human in the loop at any stage; a compromised or mistaken single account can ship arbitrary code straight to prod. Rated Medium rather than High because this is a consented, documented operating model for a one-person project, per CLAUDE.md stating that direct-to-main is the consented workflow, not an unnoticed gap, and the four required CI checks provide an automated backstop that a solo actor cannot silently skip.
   Fix:    Either accept and document solo operation as a permanent risk, and in that case fix the branch-protection contradiction by setting required_approving_review_count to 0 so the rule matches reality and relies on the four required status checks plus the audit trail as the real control, or add a second reviewer and a CODEOWNERS entry for the workflows directory before the next pipeline-config change.
   Auto-fixable: no

🟡 MEDIUM · [PERF-01] Static performance anti-patterns            confidence: high   (Performance & Scalability)
   Where:  laravel/resources/js/app.js:4,39 (static top-level import of ChartCanvas + global app.component registration); laravel/resources/js/Components/ChartCanvas.vue:20 (static import of lib/chartjs); laravel/vite.config.js (no manualChunks / defineAsyncComponent split for it)
   Why:    Chart.js (plus chartjs-plugin-datalabels) ships in the single shared entry bundle every page pays for, including Login, Registry, Patients and every admin page that never renders a chart -- inflating parse/execute time and time-to-interactive on the majority of page views even though only Dashboard, Statistics and ConsultationDashboard actually chart anything.
   Fix:    Register ChartCanvas locally (import plus components: ChartCanvas) only on the 3 pages that render charts, or wrap it in defineAsyncComponent importing Components/ChartCanvas.vue so Vite code-splits it into its own lazily-loaded chunk.
   Auto-fixable: yes

🟡 MEDIUM · [PERF-03] Pagination and bounded result sets            confidence: medium   (Performance & Scalability)
   Where:  app/Http/Controllers/PatientsController.php boardGroups approx lines 234-297 (no LIMIT/paginate; view=longterm sets includeDischarged=true, dropping the whereNull discharge_date filter, so it returns every long-term-flagged episode ever recorded, open or closed) and activeList/index which call it
   Why:    The default open-episode board is self-limiting by physical bed capacity, but the view=longterm and view=tb board variants are monotonically-growing historical queries with no page cap or date filter -- as the flagged cohort accumulates over years this becomes an ever-larger single-response payload and an ever-larger table scan, unlike every other list surface in the app (Registry paginate 20, Consultations paginate 15, Audit paginate 50) which is deliberately bounded.
   Fix:    Paginate the longterm/TB board view (or add a date-range/status filter with a hard cap) the same way Registry already paginates history; leave the default open-episode board unbounded since it is naturally capacity-limited by physical bed count.
   Auto-fixable: no

🟡 MEDIUM · [PERF-04] Core Web Vitals and API payload bounds            confidence: high   (Performance & Scalability)
   Where:  missing: no web-vitals, reportWebVitals or lighthouserc config anywhere in laravel/ (grepped resources/js, package.json, repo root)
   Why:    LCP, INP and CLS are never measured in the field or gated in CI, so a real-user rendering/bundle regression (e.g. the PERF-01 chart-bundle issue) would ship and stay invisible until a clinician complains. API payload discipline itself is good (Registry/Statistics/Dashboard controllers select narrow column sets) and compression is plausibly handled by the Cloudflare edge, so the gap is specifically the absence of Vitals measurement.
   Fix:    Wire the web-vitals package to report LCP/INP/CLS to the existing audit/log pipeline or a lightweight endpoint, or add a Lighthouse CI budget step to the frontend CI job.
   Auto-fixable: yes

🟡 MEDIUM · [PERF-07] Horizontal scaling, statelessness and caching limits            confidence: high   (Performance & Scalability)
   Where:  app/Jobs/GenerateMonthlyPdf.php line 32 (Storage disk local put report file); app/Http/Controllers/ReportsController.php lines 445-446 (downloadGenerated reads the same local disk path). RESOLVED since the 2026-09-03 morning audit: config/session.php line 21 and config/cache.php line 18 now default to database and laravel/.env.example lines 84,116 pin SESSION_DRIVER=database and CACHE_STORE=database (matches the RE-SCORE fact that production runtime now verifies session.driver=database live), so sessions and the DashboardCache heavy tier are externalized.
   Why:    The dominant risk from the morning audit (a second replica silently logging users out, or serving a stale per-replica dashboard cache) is closed. A narrower gap remains: the queued monthly-PDF-booklet feature writes to and reads from the container private local disk. QUEUE_CONNECTION=sync means generation runs inline in whichever web-serving process handled the request; on a future horizontally-scaled deployment without shared/networked storage, a download request landing on a different replica than the one that generated the file would 404, and CLAUDE.md notes storage/ is not guaranteed to be a persistent volume across redeploys either. Bounded: admin-only, one generated PDF per admin per year, not a core clinical flow, and not an issue on today single-host deployment.
   Fix:    Move the reports storage to the same S3-compatible bucket already used for audit shipping and backups (app/Support/S3SigV4.php gives a ready-made client), or mount storage/ as a shared/persistent volume before adding a second app replica.
   Auto-fixable: no

🟡 MEDIUM · [PERF-08] Resource pools, GC, deadlines and load shedding            confidence: medium   (Performance & Scalability)
   Where:  routes/web.php (only throttle:auth/register/register-email/stepup/email-verify/csp-report are rate-limited; no general throttle on Registry search/export, Dashboard, or the Patients/Consultations boards); no PHP-FPM worker-count or MySQL max_connections tuning documented or committed anywhere in laravel/ (Nixpacks/Coolify-managed, opaque to the repo). PARTIALLY IMPROVED since the morning audit: config/mail.php line 51 now sets an explicit timeout via env MAIL_TIMEOUT default 10, closing the previously-unbounded synchronous SMTP-send worker-exhaustion risk.
   Why:    Beyond auth-related endpoints and the now-fixed mail timeout, the app has no explicit request deadlines and no 503/Retry-After overload-shedding path; under a burst of concurrent clinical-page load (e.g. shift-change) PHP-FPM would queue or the process would hang rather than degrade predictably, and pool sizing is entirely opaque to anyone reading this repo. Actual risk is bounded today by the small user base (about 330 staff, one unit) rather than internet-scale traffic.
   Fix:    Document the Nixpacks/Coolify PHP-FPM worker count and MySQL max_connections against the single-host capacity; add a general per-route throttle on the heavier admin read paths (Registry/Statistics/Reports); consider a maintenance/overload 503 response with Retry-After for DB exhaustion.
   Auto-fixable: no

🟡 MEDIUM · [RES-04] Bulkheads & back-pressure (bounded resources)            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/app/Http/Controllers/ReportsController.php:420-434 (monthlyPdf) and 74-80 (boundSyncRender); laravel/app/Jobs/GenerateMonthlyPdf.php; laravel/docs/DEPLOY-LARAVEL.md:307 (self-documented gap)
   Why:    The full-year PDF booklet render can run inline on a shared PHP-FPM web worker for up to 120s (boundSyncRender only raises max_execution_time, it does not bound the work); the same shared pool serves every other clinical page, so a few concurrent admin report requests can tie up enough workers to slow or stall unrelated staff traffic for the duration.
   Fix:    Run an actual background worker instead of QUEUE_CONNECTION=sync so the existing async=1 report path truly offloads the render off the web-request pool; alternatively cap concurrent synchronous PDF renders with a lock or semaphore.
   Auto-fixable: no

🟡 MEDIUM · [RES-05] Graceful degradation, fail-fast & load shedding            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/app/Jobs/GenerateMonthlyReport.php:38-40
   Why:    The monthly-report mailer loops every active ReportRecipient and calls Mail queue with no try/catch; since QUEUE_CONNECTION=sync this runs inline, so an SMTP failure on the Nth recipient throws and aborts the loop, every recipient after the failing one silently never gets that month's booklet, and unlike BackupVerify and AuditVerifyDaily (which both raise an in-app admin alert on failure) nothing surfaces this beyond a log line.
   Fix:    Wrap the per-recipient send in try/catch, continue the loop on failure, collect failed addresses, and log or notify admins, mirroring the incident-raising pattern already used in BackupVerify.php and AuditVerifyDaily.php.
   Auto-fixable: no

🟡 MEDIUM · [RES-08] Cache stampede / thundering-herd protection            confidence: medium   (Resilience & Fault Tolerance)
   Where:  laravel/app/Http/Controllers/DashboardController.php:98; laravel/app/Support/DashboardCache.php:17-25
   Why:    The dashboard heavy tier is cached under one global key with a fixed 300s TTL and no jitter, request-coalescing or dogpile lock; Cache::remember is not atomic across concurrent PHP-FPM processes, so several dashboard loads landing right as the key expires or right after a cache bust on write can each independently recompute the same multi-query aggregation against MySQL at once. Blast radius is bounded by the small staff headcount.
   Fix:    Add TTL jitter and wrap the recompute in a Cache lock guarded single-flight so only one process recomputes on expiry or bust while others serve the still-warm value or wait briefly.
   Auto-fixable: yes

🟡 MEDIUM · [RES-09] Single points of failure & failure-mode enumeration            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/docs/DEPLOY-LARAVEL.md topology table in section 0 (app container and mysql:8 container both on one OCI host, no replica) and section 9 (documented but never-drilled rebuild-from-zero procedure); root CLAUDE.md section 1
   Why:    A single host or container failure is a full outage with no automatic recovery. Data loss is mitigated by nightly encrypted off-box backups with a measured, logged restore drill, but there is no equivalent mitigation for availability, and the rebuild-from-zero runbook that would be used to recover from full host loss has never itself been drilled end to end the way the DB restore was.
   Fix:    Either document this as an accepted cost or complexity tradeoff at the current scale, or add a second app replica behind Traefik plus a replicated or standby MySQL instance before cutover makes this host the sole daily system of record; run the rebuild-from-zero procedure once as a drill and record the RTO.
   Auto-fixable: no

🟡 MEDIUM · [DATA-12] Business Impact Analysis, bus-factor SPOFs, and crisis comms            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:20 (A7) and :36 (B8, sole collaborator on the GitHub repo); no BIA/BCP/crisis-comms document found under laravel/docs/
   Why:    No documented Business Impact Analysis backs the RPO/RTO numbers in DATA-01; the system owner is explicitly the sole GitHub collaborator and sole information-security lead (a documented, self-acknowledged single point of failure for deploys, credentials and incident response); no crisis-communication plan (declaration owner, notification templates, status-page ownership) was found in INCIDENT-RESPONSE.md or elsewhere. If that one person is unavailable during an incident, there is no documented second path to deploy, restore, or communicate.
   Fix:    Conduct a lightweight BIA that explicitly derives the accepted RPO/RTO from clinical impact; cross-train or secret-share at least one additional person on deploy access and the backup-key escrow; write a short crisis-communication plan naming who declares an incident and how the unit is told what data window was lost.
   Auto-fixable: no

🟡 MEDIUM · [DATA-13] Per-release supply-chain evidence archived to immutable storage            confidence: high   (Data, Backups, DR & Correctness)
   Where:  .github/workflows/laravel-ci.yml (no sbom/provenance/attestation/object-lock/WORM step found)
   Why:    CI runs composer audit (arbitrated by scripts/composer-audit-gate.php) and gitleaks/Semgrep as blocking gates, and Dependabot is enabled, but no SBOM, build provenance/attestation, or per-release dependency manifest is generated or archived to tamper-protected storage. Which shipped releases carried dependency X is not answerable from the repo alone, which slows incident response if a dependency CVE surfaces later.
   Fix:    Add a CycloneDX/SPDX SBOM generation step to laravel-ci.yml (composer and npm SBOM tooling) and archive the SBOM plus the composer-audit-gate output per commit SHA to a retention-locked location, reusing the existing S3SigV4 pattern already used for audit/backup archives.
   Auto-fixable: no

🟡 MEDIUM · [DATA-14] Storage/data footprint minimized and lifecycled            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:60 (D1, PHI-copy inventory); laravel/docs/compliance/DATA-RETENTION.md:58-66 (section 2.3 backups/copies table, multiple GAP rows)
   Why:    Multiple ungoverned plaintext copies of regulated health data currently exist outside the new controlled pipeline: legacy dumps on the owner personal workstation, plaintext dumps and leftover credential files under /home/ubuntu/migrate/dmc/ on the OCI host, and manual pre-deploy dumps that accumulate unencrypted on the host with no expiry rule. The backup bucket own 90-day lifecycle rule is a placeholder not yet confirmed as actually applied. Clinical rows themselves are retained indefinitely with no purge/anonymisation mechanism built yet. Severity raised from the category Low default to Medium because the accumulating copies are unencrypted PHI on a multi-project shared host and a personal workstation, not merely a cost/hygiene issue.
   Fix:    Inventory and securely delete (shred) every ad-hoc plaintext dump on the owner workstation and the OCI host; confirm and document the actual lifecycle rule configured on the dmc-db-backups bucket; encrypt and auto-expire manual pre-deploy dumps.
   Auto-fixable: no

🟡 MEDIUM · [CFG-06] Dev/staging/prod parity; complete .env.example            confidence: high   (Config, Secrets & Environments)
   Where:  laravel/config/mail.php line 51 reads env('MAIL_TIMEOUT', 10); the key is absent from laravel/.env.example
   Why:    An operator tuning SMTP behavior (or diagnosing a mail-relay brownout under the sync queue) has no way to discover MAIL_TIMEOUT exists without reading source; the .env.example onboarding template is silently incomplete for a key added in today's same-day fix. Blast radius is bounded because the code default (10s) is safe.
   Fix:    Add MAIL_TIMEOUT=10 next to the other MAIL_* keys in laravel/.env.example, with a short comment matching the one already in config/mail.php lines 48-51.
   Auto-fixable: yes

🟡 MEDIUM · [CFG-11] Environments rebuildable from IaC with drift detection & parity            confidence: high   (Config, Secrets & Environments)
   Where:  missing: Terraform/Pulumi/CloudFormation/Ansible definitions for the OCI instance, MySQL container, Cloudflare rules or object-storage buckets; laravel/docs/DEPLOY-LARAVEL.md section 3.1 documents provisioning Via the Coolify UI
   Why:    The single production environment (there is no staging) cannot be reliably reproduced after a host loss; infra facts (security-list rules, Coolify app config, bucket policies, Cloudflare rules) live only in prose runbooks and operator memory, and no scheduled drift detection would catch an out-of-band console change.
   Fix:    Codify the OCI instance, MySQL/Coolify resource config, Cloudflare rules and buckets in Terraform/OpenTofu (or export/version the Coolify resource definitions); add a scheduled plan/driftctl job that alerts on divergence; keep the manual runbook as interim fallback documentation.
   Auto-fixable: no

🟡 MEDIUM · [CMP-03] DPIA, ROPA, DPAs & 72-hour breach notification            confidence: high   (Compliance, Privacy & Legal)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:18 (A5 - no controller-processor contract); laravel/docs/compliance/DPA-AND-TRANSFERS.md:18-21 (P0-P3 rows: No DPA on file / No signed DPA on file, status OPEN); laravel/docs/compliance/EVIDENCE-PACK.md:129 (G12 top-priority gap)
   Why:    DPIA.md, ROPA.md and INCIDENT-RESPONSE.md exist as substantive drafts (progress, not absent), but the dated (2026-09-03) project documentation directly confirms zero signed DPAs exist with any processor (OCI, Cloudflare, the SMTP relay) and no controller-processor contract exists between the hospital and the operator company at all. Real PHI for roughly 17k patients is processed today under this gap: no contractual audit rights, breach-notification duty, or liability allocation with any processor.
   Fix:    Execute the controller-processor contract (IR Art. 17 minimum content per PROPOSED-CITATIONS.md) as the top action (G12); obtain OCI DPA, Cloudflare Customer DPA, and a DPA for the SMTP relay (or move it in-Kingdom, G13); then route DPIA.md/ROPA.md/INCIDENT-RESPONSE.md through DPO/governance sign-off so they move from draft to adopted.
   Auto-fixable: no

🟡 MEDIUM · [CMP-06] Privacy-by-design: notice, consent & DPO            confidence: medium   (Compliance, Privacy & Legal)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:19 (A6 - DPO not appointed, tagged CONFIRMED (gap)); laravel/docs/compliance/DPO.md:22-24 (the hospital should assume it is in scope unless counsel concludes otherwise); laravel/docs/compliance/EVIDENCE-PACK.md:126 (G9)
   Why:    The privacy notice itself is implemented and reachable (routes/web.php:86 GET /privacy, no session required, EN+AR via resources/lang/{en,ar}/privacy.php), which is real credit -- but the hub processes sensitive health data at hospital scale for roughly 17k patients and its own compliance drafts self-assess a DPO appointment as effectively mandatory (IR Art. 32(1)(c) [PROPOSED]), yet none has been named. There is no independent, designated contact for data-subject or regulator escalation today.
   Fix:    Have DMC / Eastern Health Cluster issue a written, dated DPO designation (internal or external, per DPO.md section 2) and publish the contact on /privacy and in the privacy-notice text (currently placeholders).
   Auto-fixable: no

🟡 MEDIUM · [DOC-07] On-call, ownership & key links            confidence: high   (Documentation & Runbooks)
   Where:  laravel/SECURITY.md:16 (security@<hospital domain> [SET THIS] placeholder) vs laravel/.env.example:47 (SECURITY_CONTACT=mailto:info@dmc-im.com, the value actually served at /.well-known/security.txt)
   Why:    The human-readable vulnerability-disclosure and escalation contact published in SECURITY.md is a literal unfilled placeholder, inconsistent with the real address already configured as the SECURITY_CONTACT env default. A researcher or auditor reading SECURITY.md rather than fetching security.txt has no working contact. There is also no CODEOWNERS file or named on-call rotation anywhere in the repo; ownership is only informally the owner throughout CLAUDE.md and HANDOFF.md, with no documented escalation path for an incident when the owner is unavailable.
   Fix:    Replace the [SET THIS] placeholder in laravel/SECURITY.md with the real contact (matching SECURITY_CONTACT, or the intended production address if different) so the human-facing doc and the machine-readable security.txt agree. Consider adding a minimal CODEOWNERS or an Operations block naming the accountable owner/team and an escalation path.
   Auto-fixable: yes

🟡 MEDIUM · [DOC-09] Documentation freshness / sync with code            confidence: high   (Documentation & Runbooks)
   Where:  laravel/README.md:114-115; laravel/docs/DEPLOY-LARAVEL.md:31,50,314-319; laravel/docs/RELEASE-CHECKLIST.md:17; laravel/docs/CI.md:130 vs laravel/composer.lock:1781; laravel/.github/workflows/laravel-ci.yml (4 jobs); laravel/routes/public.php:20 plus app/Http/Controllers/HealthController.php
   Why:    Three separate docs, README.md, DEPLOY-LARAVEL.md and RELEASE-CHECKLIST.md, still say CI is a two-job (frontend, backend) pipeline that is billing-blocked and proves nothing, and that GET /health is being added in a parallel workstream or still 404. All of this is stale: CI was hardened to four jobs (frontend, backend, secrets=gitleaks, sast=semgrep) and confirmed green on 2026-09-03 per CLAUDE.md section 12 and HANDOFF.md, and the CI-hardening commit 4b75aca and the health-endpoint commit dea9dff both predate a later edit to DEPLOY-LARAVEL.md (ed704db) that touched the same file without correcting these lines; /health has been live since dea9dff. RELEASE-CHECKLIST.md is the literal pre-deploy gate, if a box cannot be ticked the release does not proceed, and its own header line tells every deployer CI proves nothing, so an operator following it verbatim would distrust or ignore a real CI signal. Separately, docs/CI.md line 130 itself claims league/commonmark 2.8.2 still carries eight HIGH advisories and keeps the backend job red by design, but composer.lock already locks league/commonmark at 2.10.0, the very version CI.md itself says resolves the advisories, so that paragraph is also out of date and overstates a currently-nonexistent CI failure.
   Fix:    Do a pass over DEPLOY-LARAVEL.md sections 0, 1 and 11, RELEASE-CHECKLIST.md gates header, README.md Tests-and-gates section, and CI.md commonmark paragraph, replacing the billing-blocked, two-job, health-not-shipped language with the current four-job, CI-green, health-live state, matching CLAUDE.md section 12 and HANDOFF.md which already have it right. Consider a short doc-freshness check in CI (a grep for known-stale phrases, or a periodic review) so operational runbooks do not drift from code again.
   Auto-fixable: yes

🟡 MEDIUM · [UX-03] Semantic HTML: landmarks, heading order, no clickable div/span            confidence: high   (Visual QA & Accessibility)
   Where:  resources/js/Layouts/AppLayout.vue:497 (unconditional h1 title in the app chrome) co-rendered with a second, on-screen h1 in: ActiveList.vue:56, Consultations/Handover.vue:76, Consultations/Dashboard.vue:95, Reports/Governance.vue:31, Reports/Index.vue:43, Reports/Monthly.vue:39
   Why:    Six pages render two h1 elements at once on screen (not print-only) - the sticky app-chrome title and the page own report letterhead. Screen-reader users navigating by heading level get a confusing document outline with two competing top-level headings - WCAG 1.3.1. Landmarks (header, nav, main) are otherwise consistent app-wide, and the div/span click handlers found elsewhere are all modal/dropdown backdrop-dismiss patterns backed by real focus-trap plus Escape handling, not the clickable-div antipattern this check targets, so severity is bounded. Unchanged since the morning audit; not touched by todays re-score commit (14e4e02), which addressed only UX-04.
   Fix:    Demote the inner report-letterhead heading to h2 or below, or mark it screen-reader-hidden and only expose it as h1 in a print media context, so exactly one h1 exists on screen per page.
   Auto-fixable: yes

🔵 LOW · [CICD-12] Value-stream flow health & DORA metrics measurable            confidence: high   (CI/CD & Release Engineering)
   Where:  git tag -l returns zero tags in the repository, despite laravel/docs/RELEASE-CHECKLIST.md S2 documenting a per-release annotated-tag convention using a v YYYY.MM.N format; no DORA or four-keys dashboard or metrics document was found anywhere in the repo
   Why:    Deployment frequency and lead time cannot be derived from the repo the way the documented convention intends, because it has never actually been used, so delivery performance is unmeasured and a slowdown would only be noticed anecdotally.
   Fix:    Start tagging every production release per the documented convention, or automate the tag as a deploy step, and derive deployment frequency and lead time from tag timestamps against Coolify deployment timestamps.
   Auto-fixable: yes

🔵 LOW · [RES-03] Circuit breakers on flapping dependencies            confidence: medium   (Resilience & Fault Tolerance)
   Where:  laravel/app/Support/S3SigV4.php (no breaker around S3 archive/backup calls); laravel/app/Http/Controllers/Auth/RegisterController.php:107-112, EmailVerificationController.php:57, UsernameReminderController.php:42-56 (SMTP calls with a timeout and a fail-fast catch, but no breaker)
   Why:    During an SMTP or S3-archive outage, every affected request or run still waits out its full timeout (up to 10s for mail, 30s for S3) before failing instead of failing fast after the first observed failure; bounded, not catastrophic, given the low internal traffic volume and the existing per-call timeouts.
   Fix:    If worth the complexity at this scale, wrap the S3SigV4 client and mail send calls in a simple failure-counter or half-open guard so a known-down dependency is skipped instantly rather than re-timed-out on every call; otherwise document that timeouts plus fail-fast messaging are the accepted substitute.
   Auto-fixable: no

🔵 LOW · [DOC-05] Changelog & versioning            confidence: medium   (Documentation & Runbooks)
   Where:  laravel/docs/RELEASE-CHECKLIST.md:33 (documents there is no CHANGELOG file, the tag message is the release note); repo has 0 git tags (git tag -l)
   Why:    The one documented substitute for a changelog, an annotated git tag per release (v<YYYY.MM.N>), has never actually been used. The repo has zero tags despite many production releases. An operator or auditor trying to answer what changed between release X and Y, and when, has only raw git log on main with no release boundaries, and GET /health app.version reports the deploy SHA, not a human-meaningful version.
   Fix:    Either start tagging releases per the RELEASE-CHECKLIST.md convention (retroactively tag recent deploy SHAs so the practice has a starting point), or add a lightweight CHANGELOG.md / rely explicitly on conventional commit messages plus git log and remove the unused tag instruction from RELEASE-CHECKLIST.md so the doc matches practice.
   Auto-fixable: yes

🔵 LOW · [I18N-05] Unicode-correct string operations (NFC, graphemes, invariant case)            confidence: medium   (i18n, l10n & Text)
   Where:  laravel/app/Providers/AppServiceProvider.php:30; laravel/app/Http/Controllers/Auth/AuthController.php:41; laravel/app/Http/Controllers/Auth/UsernameReminderController.php:34
   Why:    The login identity lookup (username-or-email) and the forgot-username lookup fold case with plain strtolower() instead of the mb_strtolower()/Str::lower() pattern used correctly elsewhere in the codebase (LegacyImport.php, ConsultationsController.php:619,633, ConsultationRequest.php:196,202, ImportController.php). Plain strtolower() only case-folds ASCII bytes, so a non-ASCII email would not be folded the same way in PHP as it is by MySQL utf8mb4_unicode_ci collation, which is what actually arbitrates the WHERE match; this is a latent inconsistency, not a demonstrated bypass. No setlocale() call exists anywhere in the app (verified by search), so the classic Turkish-I locale-flip is not reachable, which is why this stays Low rather than the category auth-bypass escalation. AppServiceProvider.php:30 is lower-stakes still: it only builds a rate-limiter bucket key, not an actual identity comparison.
   Fix:    Replace the three strtolower() calls on identity fields with Str::lower()/mb_strtolower() for consistency with the rest of the codebase and to avoid relying solely on DB collation for correct Unicode case-folding.
   Auto-fixable: yes

## What's Good (per auditor)
- (Reliability, SLOs & Error Budgets) laravel/app/Http/Controllers/HealthController.php is a genuine deep liveness probe: fresh-connection DB check, storage-writable check, and a scheduler heartbeat that goes stale after 5 minutes, built specifically because the scheduler had previously died silently (the monthly report, audit shipping and the integrity check all quietly stopped). That is real reliability instinct even without formal SLOs.
- (Reliability, SLOs & Error Budgets) laravel/scripts/smoke.sh runs a post-deploy check against /up and /health and the build manifest, so a broken deploy is caught immediately rather than discovered by a user report.
- (Reliability, SLOs & Error Budgets) Cause-based operational alerting exists and is reasonably well targeted for the risks that matter most here: stale or missing off-box backups (BackupVerify.php), a broken audit hash-chain (AuditVerifyDaily.php), and failed-login bursts (AuthController.php) all raise deduplicated in-app notifications to active admins.
- (Reliability, SLOs & Error Budgets) laravel/docs/DEPLOY-LARAVEL.md section 0 documents the production topology (Cloudflare, one OCI host, MySQL container, scheduler cron, audit off-box shipping) clearly enough to serve as the starting point for a real dependency map.
- (Reliability, SLOs & Error Budgets) laravel/docs/BACKUP-AND-RESTORE.md section 1 states RPO and RTO in plain English and records a real, measured restore drill dated 2026-09-03 rather than an untested claim.
- (Observability) OBS-07 is a genuinely strong implementation: the stock Laravel /up liveness route is left untouched for the Coolify poll (bootstrap/app.php:12), while a separate, deliberately-engineered /health readiness probe (app/Http/Controllers/HealthController.php) opens a fresh, 2-second-bounded DB connection rather than reusing the request's lazily-opened one, checks storage/framework writability, and checks a scheduler heartbeat that goes stale after 5 minutes - the code comment explains this was added specifically because the scheduler had previously died silently and taken audit shipping, backups and the monthly report with it. It correctly returns 503 on any failure and deliberately carries no PHI, secrets or hostnames.
- (Observability) OBS-02 discipline is real and intentional, not accidental: CspReportController allow-lists only 7 non-PHI CSP fields and truncates them; EncryptedNarrative logs table/id/column on a decrypt failure but never the value; code comments in MfaController explicitly state the audit trail must never record the selector or validator when logging MFA device trust/revocation events. No Log:: or AuditLog::create() call site was found dumping request bodies, headers, passwords, or tokens.
- (Observability) The audit_log hash-chain (app/Support/Audit.php) is a well-designed structured event trail for security-relevant actions (login, password change, MFA, admission/consultation writes) with actor and IP always session-sourced - not a substitute for OBS-01/03/04/05 but a solid adjacent control worth noting.
- (Security & App Hardening) Zero live secrets in tracked source with a narrow, well-justified gitleaks allowlist covering only the AWS-published SigV4 doc vectors; gitleaks plus push protection plus Dependabot enabled, and the secret-scan job is a required, admin-enforced status check on main
- (Security & App Hardening) composer.lock and package-lock.json are both genuinely clean against the current advisory database when audited the way CI actually audits them; the composer-audit-ignore gate is a real, reviewed, expiring blocking mechanism, not a report-only step
- (Security & App Hardening) The prior High finding, SESSION_DRIVER file silently no-opping the session-kill paths on password reset, admin MFA reset, and self password change, is now resolved: production runs SESSION_DRIVER database and the code comments were corrected to match
- (Security & App Hardening) SecurityHeaders middleware is prepended so it covers even 419 CSRF responses, with a strong nonce-based CSP, full HSTS preload, and no-store on authenticated pages
- (Security & App Hardening) Rate limiting is identity-keyed, correctly accounting for a whole hospital behind one NAT, with a dedicated session-independent IP-keyed limiter specifically closing a cookie-drop mail-relay abuse path on the registration email-send endpoint
- (Security & App Hardening) Mandatory TOTP MFA for every account including admins, remember-me deliberately disabled to close an MFA-bypass path, a uniform anti-enumeration login error message, and consistent server-side object-level authorization
- (Security & App Hardening) SAST and secret-scan both run on every push and PR to main and are now required, admin-enforced status checks, closing the biggest gap from the prior CI/CD-adjacent findings
- (Testing & Code Quality) main is now branch-protected with the 4 Laravel CI jobs as required, admin-enforced status checks (verified live via gh api), up from zero required checks at the prior audit; this alone flips TST-11 from a Medium fail to a pass.
- (Testing & Code Quality) Laravel Pint is now a blocking CI gate and the codebase is fully clean: vendor/bin/pint --test passes with 0 violations, verified locally, a real fix of the roughly 140/150-file violation count found in the prior audit, not just a gate being added.
- (Testing & Code Quality) Vitest coverage thresholds are now enforced in CI with an honest baseline-derived floor; verified green this session at 77.49% lines / 78.77% branches / 50.71% functions over 723 tests across 77 files.
- (Testing & Code Quality) Strong critical-path test breadth on the PHP side: AuthorizationTest, MandatoryMfaTest, StepUpTest, ClinicalFlowTest, PatientMergeTest, AuditHashChainTest and ClinicalNarrativeEncryptionTest all exist among 106 Feature + 3 Unit test files.
- (Testing & Code Quality) No debug statements and no orphaned TODO/FIXME/HACK/XXX markers found anywhere in shipped app/, routes/ or resources/js code.
- (Testing & Code Quality) Clean, minimal dependency footprint on both composer.json (5 runtime deps) and package.json (10 runtime deps), each traceable to a real, documented feature, no evident dead weight.
- (CI/CD & Release Engineering) Branch protection on main is live and verified via gh api: all 4 Laravel CI jobs are required, enforced for admins, with no force-push or delete allowed; this closes the top Critical from the prior audit, taken on a morning when no protection existed.
- (CI/CD & Release Engineering) The composer-audit gate genuinely blocks: the latest green run confirms league/commonmark was upgraded to 2.10.0, fixing 8 HIGH advisories, rather than being added to the ignore list, and the remaining ignore-list entries in .composer-audit-ignore.json each carry a specific code-path-level justification, a review date and an expiry.
- (CI/CD & Release Engineering) Every third-party GitHub Action is pinned to a full commit SHA and the Semgrep image is pinned to a manifest digest, with a documented bump procedure in docs/CI.md.
- (CI/CD & Release Engineering) public/build is committed and the CI build-reproducibility check gives a real, enforced hermetic-build guarantee for the frontend with no extra tooling required.
- (CI/CD & Release Engineering) Least-privilege workflow permissions by default, contents read, narrowly widened per job; a safe pull_request trigger, never pull_request_target, on a public repo.
- (CI/CD & Release Engineering) Migrations are disciplined as additive and expand-only by explicit, well-reasoned convention, naming the specific migrations whose down method would be destructive and explaining in writing why migrate rollback is banned in production.
- (CI/CD & Release Engineering) The release and rollback runbook, DEPLOY-LARAVEL.md and RELEASE-CHECKLIST.md, is unusually thorough for a solo-maintained project: mandatory pre-deploy dumps, numeric rollback-trigger criteria, and a documented app-only versus app-plus-database decision table.
- (Performance & Scalability) N+1 discipline is genuinely strong: PatientsController::boardGroups, RegistryController and ConsultationDashboardController all batch-load related data (eager-loads, one grouped handovers/signatures lookup, one whereIn icd10 lookup) before any foreach/map, with inline comments documenting the no-per-row-queries invariant.
- (Performance & Scalability) Registry, Consultations and Audit list endpoints all use hard server-side page sizes (paginate 20/15/50) that are never client-controlled; CSV/XLSX exports stream via chunk(500) instead of materializing the full result set.
- (Performance & Scalability) Migrations add explicit indexes on every column actually filtered in hot queries; ApexCharts (500KB, revenue-gated license) was replaced with a tree-shaken MIT Chart.js import (lib/chartjs.js) registering only the controllers/elements actually used.
- (Performance & Scalability) Pages are individually code-split via import.meta.glob over the Pages directory; no full-library barrel imports (lodash/moment/rxjs) exist anywhere in resources/js.
- (Performance & Scalability) NEW since the morning audit: SESSION_DRIVER and CACHE_STORE now default to database (config/session.php line 21, config/cache.php line 18, .env.example lines 84 and 116), and the RE-SCORE context confirms this is live in production -- closing the dominant PERF-07 risk (cross-replica session loss / stale per-replica dashboard cache).
- (Performance & Scalability) NEW since the morning audit: config/mail.php line 51 now sets an explicit MAIL_TIMEOUT (default 10s) on the SMTP mailer, bounding the previously-unlimited synchronous-mail worker-exhaustion risk that fed into PERF-08.
- (Resilience & Fault Tolerance) Every outbound HTTP call in the codebase has an explicit, finite timeout: the S3SigV4 client (30s on put, head and get) and the SMTP mailer (MAIL_TIMEOUT=10s, added in the 2026-09-03 re-score) rely on no client defaults.
- (Resilience & Fault Tolerance) Auth-adjacent mail sends (registration code, email-verify code, username reminder) are wrapped in try/catch with a friendly user-facing failure message rather than a raw 500 or an indefinite hang.
- (Resilience & Fault Tolerance) backup:verify and audit:verify-daily treat any unreadable, stale or unconfigured off-box dependency as an alerting failure (de-duplicated on repeat) rather than a silent pass, a genuinely good graceful-degradation and monitoring pattern.
- (Resilience & Fault Tolerance) audit:ship is properly idempotent: it advances a persisted high-water mark only after a confirmed successful upload, so a failed or partial ship never loses a row and re-running never double-ships.
- (Resilience & Fault Tolerance) Every scheduled command in routes/console.php is registered with withoutOverlapping, preventing double-processing from overlapping cron runs.
- (Resilience & Fault Tolerance) Heavy iteration and exports consistently use chunk or paginate rather than loading full tables into memory, despite roughly 37k admissions and 17k patients.
- (Resilience & Fault Tolerance) The single-host topology's data-loss risk, as opposed to its availability risk, is genuinely mitigated: a real restore drill was run and its RTO recorded, and a rebuild-from-zero runbook exists.
- (Data, Backups, DR & Correctness) A genuinely new (shipped 2026-09-03) nightly encrypted off-box backup pipeline (scripts/backup/db-backup.py) replaced six ad-hoc plaintext on-host mysqldump gzips - the runbook is explicit about what risk this closed.
- (Data, Backups, DR & Correctness) A real end-to-end restore drill has already been executed and logged with object key, timings, and row-count comparison against the live database (BACKUP-AND-RESTORE.md section 8), not just a script that has never been run.
- (Data, Backups, DR & Correctness) backup:verify runs daily, alerts every active admin exactly once per open incident, auto-resolves, and never silently passes on a storage/credential error (app/Console/Commands/BackupVerify.php).
- (Data, Backups, DR & Correctness) A deliberate, well-reasoned migration-rollback policy: migrations are additive-only by convention, and the team explicitly documents why migrate:rollback is unsafe here and uses tested database restore instead.
- (Data, Backups, DR & Correctness) Real DB-level uniqueness constraints protect the identity invariants that matter (MRN, staff email, one diagnosis per admission) instead of racy application checks.
- (Data, Backups, DR & Correctness) Application-layer AES-256 encryption of clinical narrative columns, shipped with a transactional, idempotent, chunked, resumable migration and an honest what-is-NOT-encrypted-and-why section.
- (Data, Backups, DR & Correctness) The bulk CSV import boundary uses a real preview-validate-then-commit-only-valid-rows pattern, with invalid rows surfaced rather than silently written.
- (Data, Backups, DR & Correctness) Documentation quality across BACKUP-AND-RESTORE.md, DATA-RETENTION.md and ENCRYPTION-AT-REST.md is unusually candid - it self-flags placeholders, gaps and unmeasured numbers instead of overclaiming them.
- (Config, Secrets & Environments) laravel/.env is git-ignored and was never committed at any point in project history (git log --all --full-history -- laravel/.env returns nothing) - not a purge, a clean history.
- (Config, Secrets & Environments) laravel/.env.example is exceptionally thorough: every env() key referenced across app/, config/, routes/ and bootstrap/ is documented there with a safe placeholder/default and prose explaining its production value (verified by diffing a full grep of env() calls against the file), with exactly one gap (CFG-06).
- (Config, Secrets & Environments) gitleaks over the full 481-commit history, scoped with laravel/.gitleaks.toml, found zero real secrets under laravel/; the only two hits are in the retired root legacy app (out of scope) and are themselves false positives (a descriptive string, a documented TOTP test vector).
- (Config, Secrets & Environments) Secret scan (gitleaks) is a required, admin-enforced branch-protection status check on main (confirmed live via gh api .../branches/main/protection), not merely present in the workflow file.
- (Config, Secrets & Environments) .npmrc sets ignore-scripts=true (blocks npm install-time RCE) and composer.json's allow-plugins is a closed, reviewed two-entry whitelist; both composer.lock and package-lock.json are committed and CI installs from them frozen (composer install / npm ci).
- (Config, Secrets & Environments) docs/compliance/INCIDENT-RESPONSE.md section 7.4 is a genuinely usable, per-secret emergency-rotation table (DB password, APP_KEY - with its encrypted-column re-encryption trap explicitly called out, AUDIT_S3 keys, SMTP password, SSH keys, Cloudflare/GitHub tokens), each with concrete commands and a post-rotation verification step.
- (Config, Secrets & Environments) No App::environment('production')-style branching found anywhere in app/; config/app.php's debug default is false; no source maps ship in the committed public/build.
- (Compliance, Privacy & Legal) ROPA.md is a real, schema-derived data inventory (not boilerplate) built table-by-table from the actual migrations and tagged SENSITIVE, cross-checked against DATABASE-AND-BEHAVIOR.md and HANDOVER-COMPLIANCE.md.
- (Compliance, Privacy & Legal) GET /privacy is implemented, reachable without a session, and served in both English and Arabic from resources/lang (LegalController.php, routes/web.php:86), with no PHI in the route.
- (Compliance, Privacy & Legal) An unusually thorough, dated (2026-09-03), owner-confirmed compliance paper trail exists: 9 PDPL drafts plus CONFIRMED-FACTS.md (facts tagged CONFIRMED/PLACEHOLDER/DECISION) plus PROPOSED-CITATIONS.md (sourced legal citations for counsel) plus EVIDENCE-PACK.md (control-to-evidence map, P1-P20, gap register G1-G13) plus a tracked OPEN-ITEMS.md (529 markers catalogued by file:line) -- this is a genuinely rare level of audit-readiness discipline for a project this size.
- (Compliance, Privacy & Legal) Dependency licensing is clean: 119 Composer packages and 264 unique npm packages, no AGPL/GPL/SSPL/unknown-license findings; all licenses present (MIT/BSD/Apache-2.0/MPL-2.0/weak-copyleft) are compatible with closed-source SaaS distribution.
- (Compliance, Privacy & Legal) INCIDENT-RESPONSE.md has a concrete SEV1-4 severity matrix keyed to this system actual detection signals (audit:verify-daily, /csp-report, failed-login alerts) rather than generic boilerplate text.
- (Compliance, Privacy & Legal) The documentation is candid about its own gaps -- no processor DPAs, no DPO, a public GitHub repo, and a legacy system still un-hardened on US hosting are all stated plainly with CONFIRMED/gap tags rather than glossed over, which is what makes this category auditable at all.
- (Cost, FinOps & Sustainability) Every guest-reachable, cost-relevant surface (login, registration including the email-send step, MFA challenge, step-up, email-verify, CSP report sink, /health, security.txt) carries an explicit named RateLimiter or inline throttle (app/Providers/AppServiceProvider.php:29-75); the classic denial-of-wallet vector, an unbounded outbound-mail/compute relay from an unauthenticated form, is closed, including a session-independent IP-keyed cap added specifically to stop a cookie-drop bypass on the registration email-send endpoint.
- (Cost, FinOps & Sustainability) Fixed single-host architecture (no autoscaler, no serverless, no per-token model API; LLM/AI is None per detection) means there is no consumption-scaled billing surface for an attacker to inflate in the first place; worst case is an availability incident, not a runaway invoice.
- (Cost, FinOps & Sustainability) Real lifecycle/TTL discipline already in code and docs for ephemeral cost-bearing artifacts: local backup pruning (scripts/backup/db-backup.py), an off-box bucket lifecycle rule (docs/BACKUP-AND-RESTORE.md section 6), and a defensive, non-zero-guarded audit-log retention/prune command (app/Console/Commands/AuditPrune.php); nothing here grows storage unbounded by default.
- (Cost, FinOps & Sustainability) The dashboard heaviest queries are cached with explicit, write-triggered invalidation (app/Support/DashboardCache.php) rather than recomputed on every page view, cutting both DB load and compute-proportional cost/energy.
- (Cost, FinOps & Sustainability) No duplicate non-prod environment running 24/7 to waste spend on; the project deliberately runs a single environment (CLAUDE.md section 1).
- (Documentation & Runbooks) laravel/README.md is exceptionally thorough and verified accurate: every referenced command, class and path (DemoSeeder, LegacyImport, .env.example, artisan commands) actually exists in the tree.
- (Documentation & Runbooks) CLAUDE.md functions as a comprehensive, currently-accurate architecture document. Its component counts (20 models, 47 migrations, the 8-file middleware list) were cross-checked against the code and matched exactly.
- (Documentation & Runbooks) laravel/docs/BACKUP-AND-RESTORE.md is an exemplary runbook: a symptom-meaning-action table (section 7), a full restore procedure with sequencing and gotchas (section 5), and a monthly drill with a real logged entry and measured timings (section 8) rather than a template left blank.
- (Documentation & Runbooks) laravel/docs/compliance/INCIDENT-RESPONSE.md has concrete, stack-specific containment playbooks (session revocation, per-user disable, a secrets-rotation table) rather than generic advice.
- (Documentation & Runbooks) laravel/.env.example fully covers every environment variable actually referenced by application code under app, routes and bootstrap. No gap found.
- (Documentation & Runbooks) Load-bearing decisions are consistently recorded with context and consequences even without a formal docs/adr folder (DEPLOY-LARAVEL.md section 4.3 on why migrate:rollback is banned, section 7 on the consultations cutover flag, docs/CI.md per-advisory ignore rationale).
- (Operational Readiness & Incidents) INCIDENT-RESPONSE.md is a genuinely thorough, stack-specific runbook: a SEV1-4 matrix with concrete examples tied to this system's own signals (audit.integrity_failure, registry.export_xlsx row_count, trusted-device cookies), a first-hour checklist, containment playbooks for real scenarios (session revoke, user disable, Cloudflare edge block, secret rotation table, audit-chain-break localisation, PHI-exfil-via-export), an evidence-preservation table with chain-of-custody, and a PDPL/SDAIA breach-notification decision tree - unusually complete for a system this size.
- (Operational Readiness & Incidents) Blameless-postmortem template (section 11) enforces owner, due-date and tracking-link per action item, and section 12's tabletop-exercise schedule uses concrete, stack-specific scenarios (compromised-admin exfil, ransomware, audit-chain break, auth bypass) rather than generic drills.
- (Operational Readiness & Incidents) The rollback mechanism in DEPLOY-LARAVEL.md is mechanically real, not aspirational: immutable per-commit-SHA Coolify images make app-only rollback an actual ~2-3 minute swap, and every migration whose down() is destructive or a no-op is explicitly enumerated so migrate:rollback is correctly banned in favour of dump-restore.
- (Operational Readiness & Incidents) RELEASE-CHECKLIST.md gives every release a concrete go/no-go gate (gates green, backup taken, smoke passed, audit chain verified, rollback owner named) - real, repeatable release-readiness discipline even though it is not a one-time PRR.
- (Operational Readiness & Incidents) Backup/restore automation (scripts/backup/db-backup.py, db-restore-drill.sh) is genuinely automated, encrypted, off-box, and was already drilled once for real on 2026-09-03 with measured timings recorded in BACKUP-AND-RESTORE.md section 8 - real toil reduction on the highest-stakes runbook.
- (Operational Readiness & Incidents) RFC 9116 security.txt and SECURITY.md VDP exist, are exercised by a real test (VulnerabilityDisclosureTest.php), and are deliberately session-less so a scanner hitting the well-known path cannot mint sessions.
- (Operational Readiness & Incidents) The consultations_source_of_truth flag is a well-documented incremental-migration seam (strangler/dark-launch pattern) for the legacy-to-Laravel cutover, backed by a parallel-run architecture rather than a big-bang switch, on a single mainline branch with no long-lived divergent branches.
- (Visual QA & Accessibility) Real, verified UX-04 fix on the two highest-consequence pages (Login, Admissions/Create): every label now paired via for/id with aria-describedby on errors, using the same fid()/useId() idiom PatientForm.vue already established; re-run locally, 6/6 new tests pass.
- (Visual QA & Accessibility) Those two pages are now regression-guarded by dedicated vitest-axe specs that run inside a required, admin-enforced CI status check (confirmed live via GitHub branch protection API), not just a plain CI run; a real accessibility regression on either page can no longer be merged to main.
- (Visual QA & Accessibility) scripts/contrast.mjs is a genuinely rigorous, self-validated WCAG plus CIEDE2000 perceptual-distance gate blocking CI; well above what most codebases ship for color a11y, and is itself inside that same required job.
- (Visual QA & Accessibility) A real, tested focus-trap composable (useModalA11y.js) is shared by every modal, with Escape-to-close, role=dialog/aria-modal, and focus restore-on-close.
- (Visual QA & Accessibility) A skip-to-content link, a correctly implemented global focus-visible ring, and zero positive tabindex anywhere in the app.
- (Visual QA & Accessibility) prefers-reduced-motion is handled globally and prefers-color-scheme drives the theme with a persisted, no-flash toggle.
- (Visual QA & Accessibility) Strong design-token discipline; almost no stray hardcoded hex values in components.
- (i18n, l10n & Text) I18N-02 is now fixed and well-verified: DataQualityController, DashboardController and RegistryController all bind today from Carbon/now() as a PHP-side query parameter instead of raw MySQL CURDATE(), matching the pattern ConsultationDashboardController already used. A dedicated regression test, tests/Feature/AppClockDayBoundaryTest.php, deliberately re-creates the production skew (DB session pinned to UTC via SET time_zone=+00:00, app clock frozen at 01:30 Asia/Riyadh) and asserts DataQuality future-date/LOS flags and the dashboard boarding-delay count are computed from the app day, not the DB day. The fix docblock (lines 25-27) explicitly documents why a MySQL session time_zone pin was rejected: the schema TIMESTAMP columns (created_at, assigned_at, mfa_enrolled_at, etc.) are stored UTC-internally and re-interpreted through the session zone on read, so pinning the session zone would silently shift every stored timestamp by three hours; the PHP-bound-parameter approach was the deliberate, correct choice given that constraint. A repo-wide grep confirms no live CURDATE()/day-truncating call sites remain outside comments; the two remaining NOW()-INTERVAL usages (AuthController.php:164, DashboardController.php:425, SecurityController.php:29) are relative instant-window comparisons, not day-boundary logic, and are unaffected by session timezone.
- (i18n, l10n & Text) All real database connections (mysql, mariadb, the read-only legacy connection) are pinned to utf8mb4/utf8mb4_unicode_ci in config/database.php and .env.example, and none of the 47 migrations override that with a 3-byte utf8 charset or a different collation.
- (i18n, l10n & Text) The one genuinely bilingual surface (the PDPL privacy notice) is implemented carefully: resources/lang/en and ar privacy.php ship as a real committed catalog, the Vue page sets dir and lang dynamically and uses Tailwind logical properties (text-start, ps-6) instead of physical left/right, embedded Arabic fragments elsewhere (sidebar trust badge, login footer) are correctly isolated with lang=ar dir=rtl spans, and PrivacyNoticeTest test_the_two_languages_have_the_same_shape runs as a structural missing-key/shape-parity gate in the backend CI job.
- (i18n, l10n & Text) Patient and staff identity validation is Unicode-friendly by construction: StoreAdmissionRequest name rule has no ASCII-only regex, nationality is constrained to a canonical countries reference table instead of free text, and the patients table carries no phone/address/postal-code columns that could impose US-centric formats; the only regex in the Requests directory constraining a text field is the digits-only MRN rule (a deliberate clean-data policy, not an i18n defect).

## Intake — could not verify from the repo
❓ [REL-01] Are user-facing SLIs (availability + latency percentiles) defined and measured anywhere outside the repo (Cloudflare Analytics, an external uptime monitor, APM) for https://dmc-new.towardpcc.com?  (Reliability, SLOs & Error Budgets)
❓ [REL-02] Is there an agreed SLO target and rolling window per SLI, and how was it chosen (clinical-user need vs. whatever the app currently hits)?  (Reliability, SLOs & Error Budgets)
❓ [REL-03] Is there an agreed error-budget policy, i.e. what happens (release freeze, reliability-first prioritization) when the currently-undefined error budget is exhausted?  (Reliability, SLOs & Error Budgets)
❓ [REL-07] Are reliability expectations tracked per critical clinical journey (admission, assignment, discharge, consultation ledger, handover) rather than only as a single site-wide up-or-down signal?  (Reliability, SLOs & Error Budgets)
❓ [REL-08] Are the hard dependencies' own availability figures known (Cloudflare Free plan, single OCI host me-riyadh-1, SiteGround US SMTP relay) and reconciled against any reliability target, given the app currently runs on one host with no documented failover?  (Reliability, SLOs & Error Budgets)
❓ SEC-07: Is there a documented threat-model or secure-design review step for high-risk changes such as auth, schema, or clinical logic. SAST itself is present and gating, so this is a supplementary process question the repo cannot answer on its own  (Security & App Hardening)
❓ [TST-02] Is there a plan to re-attempt a PHP coverage floor via a Clover/XML report (bypassing the console-table-rendering blocker) so both languages get an enforced coverage gate, not just JS?  (Testing & Code Quality)
❓ [TST-11] main branch protection has required status checks plus enforce_admins but no required_pull_request_reviews (verified via gh api), so a direct push can still land on main before CI reports; is this gap explicitly accepted as part of the single-operator direct-to-main workflow (CLAUDE.md section 2), relying on Auto-Deploy-OFF plus operator discipline as the real backstop, or should main require PRs going forward?  (Testing & Code Quality)
❓ [CICD-04 and CICD-11] Live branch protection on main now requires 1 approving PR review with admin enforcement on, but the repo has a single collaborator and the most recent merge, PR 7, went in with zero reviews. Is this intentional, and how will future PRs be approved: add a second reviewer, or drop the required-review count to zero and rely on the four required status checks alone? As configured, this may block all future merges through the standard flow.  (CI/CD & Release Engineering)
❓ [CICD-08] Has the app-only rollback drill or the DB-restore-to-scratch drill in DEPLOY-LARAVEL.md S4.4 ever actually been run and timed? Both rows are still unchecked.  (CI/CD & Release Engineering)
❓ [CICD-11] Is a second reviewer or operator planned, or is solo operation, meaning a single point of compromise on deploy and pipeline-config changes, an accepted permanent risk?  (CI/CD & Release Engineering)
❓ [CICD-12] Will the RELEASE-CHECKLIST.md tagging convention actually start being used, given zero tags exist despite multiple recorded production deploys?  (CI/CD & Release Engineering)
❓ [PERF-05] Has this service been load-tested to its knee point (e.g. simulated shift-change concurrent board/registry access)? What RPS/latency was observed?  (Performance & Scalability)
❓ [PERF-06] Is there a documented peak/6-month/10x growth plan for patient volume and concurrent clinician sessions on the single OCI host, and a named first bottleneck (DB CPU, PHP-FPM workers, disk I/O)?  (Performance & Scalability)
❓ [PERF-10] Which platform quotas sit on the critical path (Cloudflare Free-plan request/rule limits, OCI instance vCPU/network caps, MySQL max_connections) and what is their headroom versus projected peak concurrent usage?  (Performance & Scalability)
❓ [RES-11] Beyond the processor identities already recorded in CONFIRMED-FACTS.md, is a formal vendor SLA reconciliation and a per-vendor outage runbook, including a mail fallback transport, wanted, or is this accepted risk given mail is off the core clinical path?  (Resilience & Fault Tolerance)
❓ [RES-13] Is any fault-injection or GameDay practice planned for this system beyond the existing monthly DB restore drill, which proves backup and restore but not live fault tolerance under a real deploy or traffic pattern?  (Resilience & Fault Tolerance)
❓ [DATA-01] Is the current <=24h RPO (nightly-dump-only, no PITR) an accepted business trade-off for this clinical unit, or does a hospital-level BIA require tighter recovery for admissions/discharges/consultations?  (Data, Backups, DR & Correctness)
❓ [DATA-12] Is there a written Business Impact Analysis and a crisis-communication plan, and is there a second person (beyond the sole GitHub collaborator / system owner) who can deploy, restore a backup, or access the backup encryption key if the owner is unavailable?  (Data, Backups, DR & Correctness)
❓ [DATA-14] What lifecycle/retention rule is actually configured on the dmc-db-backups OCI bucket today - the runbook marks the 90-day figure as a legal placeholder set outside these scripts and not yet confirmed?  (Data, Backups, DR & Correctness)
❓ [CFG-03] Coolify's environment-variable store is the documented production secret location and no secret is committed to the repo - but is access to that store itself access-controlled/audited (who can view/edit prod env vars, is it logged), and is the OCI IAM customer-secret-key pair behind AUDIT_S3_ACCESS_KEY/SECRET (and DB_BACKUP_S3_*) scoped by policy to only the dmc-audit-log / dmc-db-backups buckets rather than broader account access?  (Config, Secrets & Environments)
❓ [CFG-02] A legacy DB password and the info@dmc-im.com SMTP password were found committed in a commit that is now confirmed fully purged from git history (the old commit hash no longer resolves). Purging proves the current repo is clean; it does not prove those two specific credential values were rotated at the provider/DB end. Were they rotated, and does info@dmc-im.com (which still carries live OTP/reset mail) run on a password never exposed in that commit?  (Config, Secrets & Environments)
❓ [CFG-04] Only an emergency-rotation runbook exists (INCIDENT-RESPONSE.md section 7.4); there is no documented routine/scheduled rotation cadence for long-lived credentials (DB password, AUDIT_S3 keys, SSH keys). Is a periodic cadence planned, or is emergency-only rotation an accepted risk?  (Config, Secrets & Environments)
❓ [CMP-02] How are patient/staff access, erasure, portability and rectification requests actually fulfilled today, within what time bound, and is there a request log? DPO.md R6 names the HIM office as the verification point but EVIDENCE-PACK.md marks this OWNER (not yet supplied).  (Compliance, Privacy & Legal)
❓ [CMP-03] What is the target date for signing the controller-processor contract (G12, top priority) and the OCI / Cloudflare / SMTP-relay DPAs, and who (DPO once appointed, or the owner in the interim) will formally sign off DPIA.md / ROPA.md / INCIDENT-RESPONSE.md as adopted rather than draft?  (Compliance, Privacy & Legal)
❓ [CMP-06] Does counsel confirm the PDPL Implementing-Regulations DPO-appointment trigger applies here (health data at hospital scale), and if so, who will be designated and by when?  (Compliance, Privacy & Legal)
❓ [general] EVIDENCE-PACK.md section 2 row P8 and ROPA.md section 2 both describe the GitHub repo as private, while CONFIRMED-FACTS.md D3/G10 state it is deliberately PUBLIC until go-live -- worth reconciling so the evidence pack does not misstate this to an external auditor.  (Compliance, Privacy & Legal)
❓ [COST-01] Is cost-allocation tagging/labelling enforced on the OCI/Coolify/Cloudflare/S3-bucket resources at the console level, since there is no IaC in this repo to check?  (Cost, FinOps & Sustainability)
❓ [COST-02] Is a billing budget with an alert threshold configured for the OCI account, and is any per-patient/per-episode unit-cost metric tracked?  (Cost, FinOps & Sustainability)
❓ [COST-03] Is OCI cost-anomaly detection (or an equivalent) enabled and routed to an on-call/owned channel?  (Cost, FinOps & Sustainability)
❓ [COST-04] Is the single OCI Ampere instance on a committed-use/reserved rate, or is on-demand an intentional, reviewed decision?  (Cost, FinOps & Sustainability)
❓ [COST-06] Who owns hosting-cost review for this system and at what cadence; has a crawl/walk/run FinOps maturity stage ever been named?  (Cost, FinOps & Sustainability)
❓ [DOC-07] Is there, or should there be, a named on-call or escalation path beyond the owner for an incident when the owner is unavailable, and should that be written down (CODEOWNERS or an Operations block)?  (Documentation & Runbooks)
❓ [DOC-09] Is there a plan to periodically re-check operational runbooks (DEPLOY-LARAVEL.md, RELEASE-CHECKLIST.md) against the live CI and feature state after changes like the 2026-09-03 CI hardening, so they do not silently drift the way they did here?  (Documentation & Runbooks)
❓ [OPS-01] Has a formal production-readiness / launch review (architecture, capacity, failure modes, monitoring, rollout) been completed and signed off, particularly ahead of the planned cutover from the legacy dmc-im.com system to this app as the daily system of record? RELEASE-CHECKLIST.md and DEPLOY-LARAVEL.md cover release mechanics but not a one-time go-live/cutover review.  (Operational Readiness & Incidents)
❓ [OPS-03] Do the existing in-app signals (backup.stale, audit.integrity_failure, security.failed_logins, dq:notify) reach anyone outside business hours, or are they only seen when an admin happens to check the bell/e-mail? Is any external paging/alerting tool in use that is not reflected in the repo?  (Operational Readiness & Incidents)
❓ [OPS-07] Beyond the backup/restore automation already shipped, is manual operational toil (secret rotation, deploy verification, data reloads - all heavily manual per DEPLOY-LARAVEL.md) tracked anywhere, and is there a plan to automate the highest-frequency manual steps?  (Operational Readiness & Incidents)
❓ [OPS-12] Is the SECURITY_CONTACT / security.txt mailbox (currently defaults to mailto:info@dmc-im.com, the legacy US-hosted general inbox) actively monitored as a security-report inbox, or should a dedicated address be created before go-live as SECURITY.md itself flags?  (Operational Readiness & Incidents)
❓ [UX-01] No lighthouse/screenshot runtime hook was enabled for this audit. Have the key screens been visually verified at mobile (375px) and desktop (1440px) widths, including tap-target sizing, on a real rendered browser?  (Visual QA & Accessibility)
❓ [UX-04/UX-08] Six forms (ForgotPassword, ForgotUsername, ResetPassword, Profile/Edit, Audit/Index, Registry/Index filter) still have the pre-fix bare-sibling-label defect and no axe coverage. Is there a plan to extend today Login/Create pattern and specs to these six before the next review?  (Visual QA & Accessibility)
❓ [I18N-01] Is there a Unicode round-trip test (emoji + CJK + an RTL string) that POSTs through a real endpoint and reads the value back byte-identical from MySQL? None was found under laravel/tests/; non-blocking given the charset config is already correct.  (i18n, l10n & Text)
❓ [I18N-02] AppClockDayBoundaryTest could not be executed in this static-only audit environment (no local MySQL instance reachable; connection refused on 127.0.0.1:3306/dmc_test); the fix and test were verified by source review only. Recommend confirming it is green in CI (laravel-ci.yml backend job runs against a real MySQL 8 service) before relying on it as proof.  (i18n, l10n & Text)
❓ [I18N-05] Has non-ASCII (accented Latin, Arabic-script) text ever been entered into a users.email value in production, and if so, was a case-insensitive duplicate or login-lookup miss ever caused by the mismatch between PHP strtolower() and MySQL utf8mb4_unicode_ci collation? No incident evidence was found either way.  (i18n, l10n & Text)

## Waived (risk-accepted)
None — no .prod-ready/waivers.yml.

## Footer

```
Morning full report: docs/compliance/evidence/prod-ready-2026-09-03.md  ·  Re-run: /prod-ready:audit  ·  Cache: laravel/.prod-ready/ (git-ignored)
Engine: prod-ready@prod-ready-marketplace (Documents/prod-ready-plugin) · auditors on claude-sonnet · orchestrated 2026-09-03
```
