# PRODUCTION READINESS — CLOSE-OUT RE-SCORE — DMC Internal Medicine patient-flow hub (laravel/)

**Verdict: NEEDS FIXES (70/100) · emphasis view 76/100 · Critical 0 · High 12 · Medium 17 · Low 5**

Run 3 of 2026-09-03: a **full fresh audit** of all 16 applicable categories (`--fresh`, 16 sonnet auditors, nothing carried over) against `main` after the engineering close-out (PR #10/#11 deployed as `a4dd4bd`), then **re-audits of five categories** (05, 11, 13, 15 and 08) after the same-evening follow-through PRs #13–#18 merged. Prior: evening re-score 2026-09-03T18:15:53 — NEEDS FIXES (62/100, emphasis 72; Critical 0 · High 14 · Medium 24 · Low 4). Scored mechanically by the same scorer as the earlier runs (category-file weights; zero-scorable categories excluded).

## Delta by category (weight-ordered; every row re-audited fresh this run)

| Category | Weight | Evening | Now | Δ | Crit / High / Med / Low now |
|---|--:|--:|--:|--:|:--:|
| Security & App Hardening | 10 | 94 | 94 | +0 | 0 / 1 / 0 / 0 |
| Data, Backups, DR & Correctness | 9 | 73 | 81 | +8 | 0 / 1 / 3 / 0 |
| Config, Secrets & Environments | 9 | 76 | 76 | +0 | 0 / 1 / 1 / 1 |
| CI/CD & Release Engineering | 9 | 74 | 73 | -1 | 0 / 3 / 0 / 0 |
| Resilience & Fault Tolerance | 9 | 32 | 52 | +20 | 0 / 1 / 2 / 3 |
| Reliability, SLOs & Error Budgets | 9 | 0 | 0 | +0 | 0 / 0 / 1 / 0 |
| Testing & Code Quality | 8 | 73 | 100 | +27 | 0 / 0 / 0 / 0 |
| Observability | 8 | 43 | 62 | +19 | 0 / 2 / 1 / 0 |
| API & Contract Lifecycle | 8 | n/a | n/a | — | — |
| Operational Readiness & Incidents | 7 | 60 | 76 | +16 | 0 / 2 / 0 / 0 |
| Infrastructure, Containers & IaC | 7 | n/a | n/a | — | — |
| Compliance, Privacy & Legal | 6 | 68 | 59 | -9 | 0 / 0 / 3 / 0 |
| Performance & Scalability | 6 | 29 | 43 | +14 | 0 / 0 / 4 / 0 |
| AI/LLM & Agentic Readiness | 6 | n/a | n/a | — | — |
| Visual QA & Accessibility | 5 | 79 | 100 | +21 | 0 / 0 / 0 / 0 |
| Documentation & Runbooks | 5 | 75 | 94 | +19 | 0 / 0 / 0 / 1 |
| Cost, FinOps & Sustainability | 4 | 100 | 100 | +0 | 0 / 0 / 0 / 0 |
| i18n, l10n & Text | 4 | 94 | 56 | -38 | 0 / 1 / 2 / 0 |

## Orchestrator notes (run 3 of 2026-09-03 — full fresh audit after the engineering close-out)

**How this run differs from the evening re-score.** Every applicable category was re-audited fresh
(16 auditors, `--fresh`, nothing carried over), against `main` at `fcd917a` with the production
deploy `a4dd4bd`/`9f81bb9` live. The evening run had re-audited only 8 categories and carried 7 from
the morning. Read the category deltas with that in mind: a move of a few points on a category whose
auditor changed is variance, not progress or regression.

**Auditor variance to discount before reading the table.**
- *i18n 94 → 56.* The evening auditor credited I18N-02 after the CURDATE fix; this auditor fails it
  (High) because `config/database.php` never pins a MySQL session `timezone`, so TIMESTAMP columns
  are written on the Riyadh wall clock. That state is **known and deliberately kept**: pinning the
  session zone now would shift every existing TIMESTAMP row by three hours (the reason the fix
  chosen this morning binds "today" from PHP instead; see `AppClockDayBoundaryTest` and HANDOFF
  item 5). Treat I18N-02 as an accepted, documented risk with a future data-migration path, not as a
  new defect. The other two i18n items (I18N-05 rate-limiter key case, I18N-06 PDF bidi) are real
  but small.
- *Compliance 68 → 59.* Same facts, stricter reading: this auditor scores the missing
  controller–processor contract and the absent DPAs (CMP-03), the un-implemented data-subject
  request mechanics (CMP-02) and the unappointed DPO (CMP-06) as fails rather than intake. All three
  are owner/legal items already listed in HANDOFF item 2 and 4; nothing regressed in the code.
- *Reliability 0.* One scorable check (REL-05) out of eight; the rest are intake because SLOs do not
  exist yet. The mechanical score is honest but meaningless as a trend until SLIs are defined
  (owner decision).

**Genuine movement since the evening (+8 overall to 70, +4 emphasis to 76; High 14 → 12, Medium 24 → 17, Critical 0 throughout).**
Testing 73 → 100 (ESLint gate, PHP coverage floor from Clover at 83 % against 86.1 % measured,
Vitest thresholds), Resilience 32 → 52 (single-flight dashboard cache with re-read after
lock-wait, per-recipient mail isolation, readable fetch errors in the modals, SMTP timeout),
Observability 43 → 62 (JSON structured logs live on stderr, `LOG_LEVEL=warning` finally in effect
at runtime), Ops 60 → 76 (rollback rehearsed on production: 50 s swap, 121 s roll-forward; restore
drill logged twice), a11y 79 → 93 (every form labelled; the one remaining medium — duplicate
`<h1>` on five report pages — is fixed in PR #15), Performance 29 → 43 (chart code-split, cache
lock; the PERF-03 "unbounded long-term registry" is 176 rows accumulated since 2022 in production,
all discharged, so it is bounded in practice and left as legacy-parity behaviour by design).

**Same-evening follow-through, merged and re-audited (credited in the numbers above).** PR #13
(legacy CI manual-only; rollback rehearsal and restore-drill records), PR #14 (CODEOWNERS +
security-policy contact pointer → DOC-07 narrowed to the incident-response contact tree, Low),
PR #15 (one `<h1>` per page → UX-03 pass, a11y 93 → 100), PR #16 (DATABASE-AND-BEHAVIOR.md resynced
from code → DOC-09 pass), PR #17 (ten ADRs → DOC-06 pass; docs 74 → 94), PR #18 (RoPA / retention /
DPIA reworked to the processor-and-legacy-daily framing, C12/C13 reconciled, OPEN-ITEMS regenerated
to 575 live markers → compliance unchanged at 59, correctly: a more honest document is not a signed
contract). CI/CD re-audited unchanged at 73 (its three Highs are owner decisions; CODEOWNERS alone
does not constitute a second reviewer). Point-in-time recovery (hourly encrypted binlog shipping +
PITR runbook) went through an adversarial review that found four Highs — deadlock on a broken pipe,
one unshippable file stalling the queue, no heartbeat monitoring, a wrong replay filter in the
runbook — was hardened, reviewed a second time (six further findings, all addressed), merged as PR #19
and **installed on the host at 19:21 UTC**: first run archived both closed binlogs in 47 s, the archived
object decrypts and parses (25,900 events), hourly cron and logrotate in place. DATA-04 re-audited
fail → pass (Data 73 → 81); DATA-02 (an off-region copy) stays open and is an OCI-side decision.

**Remaining Highs, sorted by who can close them.**
- *Owner decision only:* SEC-11 / CICD-11 (a second reviewer on `main` — with one maintainer this is
  a waiver candidate; CODEOWNERS now names the owner), OPS-02 / OPS-03 (on-call rotation and a
  paging channel for the audit-chain, backup and brute-force alerts), CICD-08 (automatic
  smoke-gated rollback; the owner chose to keep deploys manual — the drain step, RES-12, is a
  separate two-setting change that does not require auto-deploy and is still open), DATA-02
  (second-region backup copy) and CFG-10 (OCI instance principal instead of static S3 keys) both
  need an OCI console/IAM step, OBS-03 / OBS-04 (an error-tracking and metrics sink; needs the same
  IAM step for an in-Kingdom target), CICD-05 (SBOM/attestations — engineering, low value until the
  repo is private and releases are tagged).
- *Accepted with rationale:* I18N-02 (above).

**Things the auditors got right that earlier runs missed.** The `LOG_LEVEL` build-time-only
finding was confirmed live this evening and fixed as a runtime variable; the duplicate-h1 defect is
real and was not visible to jsdom-based axe specs because the layout is stubbed there; the
long-term registry query really is unbounded by construction even though it is small today.

---

PRODUCTION READINESS — DMC Internal Medicine patient-flow hub (laravel/)          Verdict: 🟡 NEEDS FIXES (70/100)
Stack: Laravel 13 · PHP 8.3 · Inertia + Vue 3 · MySQL 8.4 · Coolify/Nixpacks on 1× OCI (me-riyadh-1) · Cloudflare Free · GitHub Actions
Run:   fresh · 15 categories audited · 3 N/A · 2026-09-03T22:33:46   |   Emphasis view (security · privacy/compliance · data · config · observability): 76/100   |   PARTIAL — see notes

## Scorecard
| Category | W | Score | ● | Crit | High | Med | Low | Pass | N/A | Intake |
|---|--:|--:|:-:|--:|--:|--:|--:|--:|--:|--:|
| Security & App Hardening | 10 | 94 | 🟡 | 0 | 1 | 0 | 0 | 11 | 0 | 0 |
| Reliability, SLOs & Error Budgets | 9 | 0 | 🟡 | 0 | 0 | 1 | 0 | 0 | 2 | 5 |
| Resilience & Fault Tolerance | 9 | 52 | 🟡 | 0 | 1 | 2 | 3 | 4 | 1 | 2 |
| CI/CD & Release Engineering | 9 | 73 | 🟡 | 0 | 3 | 0 | 0 | 7 | 1 | 1 |
| Config, Secrets & Environments | 9 | 76 | 🟡 | 0 | 1 | 1 | 1 | 5 | 2 | 2 |
| Data, Backups, DR & Correctness | 9 | 81 | 🟡 | 0 | 1 | 3 | 0 | 9 | 1 | 0 |
| Observability | 8 | 62 | 🟡 | 0 | 2 | 1 | 0 | 3 | 5 | 0 |
| Testing & Code Quality | 8 | 100 | 🟢 | 0 | 0 | 0 | 0 | 9 | 2 | 0 |
| Operational Readiness & Incidents | 7 | 76 | 🟡 | 0 | 2 | 0 | 0 | 7 | 2 | 1 |
| Performance & Scalability | 6 | 43 | 🟡 | 0 | 0 | 4 | 0 | 2 | 1 | 3 |
| Compliance, Privacy & Legal | 6 | 59 | 🟡 | 0 | 0 | 3 | 0 | 3 | 2 | 0 |
| Documentation & Runbooks | 5 | 94 | 🟢 | 0 | 0 | 0 | 1 | 6 | 2 | 0 |
| Visual QA & Accessibility | 5 | 100 | 🟢 | 0 | 0 | 0 | 0 | 10 | 0 | 0 |
| i18n, l10n & Text | 4 | 56 | 🟡 | 0 | 1 | 2 | 0 | 4 | 0 | 0 |
| Cost, FinOps & Sustainability | 4 | 100 | 🟢 | 0 | 0 | 0 | 0 | 3 | 2 | 5 |

N/A: AI/LLM & Agentic Readiness (no model usage) · API & Contract Lifecycle (self-contained app, no versioned API or events) · Infrastructure, Containers & IaC (No Dockerfile/Containerfile, docker-compose file, Kubernetes/Helm manifest, Terraform/CloudFormation/Pulumi/Bicep/CDK/ARM IaC, or platform deploy config (vercel.json, netlify.toml, fly.toml, render.yaml, Procfile, app.yaml, railway.json, *.nomad, nixpacks.toml, .dockerignore) exists anywhere under laravel/ (verified via recursive glob/find, node_modules and vendor excluded). Per CLAUDE.md and the detection summary, the app is built and deployed by Coolify v4 using its bubuilt-in Nixpacks buildpack directly from the `main` branch on a single OCI host -- there is no committed Dockerfile or IaC in this repo for Nixpacks/Coolify to read; it introspects composer.json/package.json itself. This is exactly the category's own N/A case: 'an app deployed only via an opaque managed PaaS with no committed config.' All 10 checks self-skip on their missing artifact; per scoring.md the whole category is excluded from the weighted overall score.)
MALFORMED (excluded): test2.json: Expecting value: line 1 column 1 (char 0)

## Findings
🟠 HIGH · [OBS-03] Error tracking wired with a real DSN            confidence: high   (Observability)
   Where:  laravel/composer.json (no sentry/sentry-laravel, bugsnag, rollbar, honeybadger or airbrake in require/require-dev); .env.example:39-41 documents SENTRY_DSN as a recommended-but-not-installed integration
   Why:    Production PHP exceptions and fatal errors are visible only as JSON stderr lines viewed via docker logs / Coolify console, with no aggregation, deduplication, grouping or alerting. On a live PHI clinical system with no queue worker and QUEUE_CONNECTION=sync, a silent regression in admission, discharge, consultation or handover writes can go unnoticed until a clinician reports it manually; there is no MTTR signal or paging path independent of a human noticing broken behaviour.
   Fix:    Add composer require sentry/sentry-laravel (or a self-hosted GlitchTip/Sentry instance to keep error payloads in-Kingdom, matching the PDPL data-residency posture already applied to the DB and audit archive); publish its config; the package auto-registers via bootstrap/app.php exception handling once SENTRY_DSN is set; set environment from APP_ENV and release from config app.version (already populated at deploy time per .env.example:7-9) so errors are attributable to a build; confirm request-body and PHI scrubbing before enabling send_default_pii.
   Auto-fixable: yes

🟠 HIGH · [OBS-04] RED / golden-signal metrics as histograms            confidence: high   (Observability)
   Where:  missing: metrics client. No prom-client/promphp, opentelemetry, statsd or datadog package in laravel/composer.json or laravel/package.json; no /metrics route in laravel/routes/web.php or laravel/routes/public.php
   Why:    There is no request-rate, error-rate or duration-histogram signal for the Laravel app, and no saturation gauge for the DB connection pool, PHP process pool or disk. Tail latency (p95/p99) on the clinical board, registry and report pages and error rate cannot be graphed or alerted on, and no SLO can be computed for this system even though it is a live PHI application. The slo-query runtime hook has nothing in-repo to query.
   Fix:    Add a lightweight metrics exporter (e.g. promphp/prometheus_client_php behind a token-gated /metrics route, or the OTel PHP SDK exporting to a collector) with an HTTP-duration histogram middleware (bucketed, not an average), a request counter labeled by route and status (templated, not raw path), and a saturation gauge for the DB connection pool and PHP-FPM worker pool; wire the scrape/export target into the host monitoring stack.
   Auto-fixable: no

🟠 HIGH · [SEC-11] Multi-party authorization & tamper-evident audit on the control plane            confidence: high   (Security & App Hardening)
   Where:  GitHub branch protection on main (verified via gh api repos/ahmedsk2/dmc-patient-flow-hub/branches/main/protection): required_status_checks (4 jobs) + enforce_admins=true present, but no required_pull_request_reviews block and no repo rulesets exist -- no mandated second-approver count. Deploys are also triggered by the same single operator via Coolify (docs/DEPLOY-LARAVEL.md).
   Why:    A single compromised or rogue actor (the repo owner GitHub credentials, or a phished/stolen session) can author, merge and deploy arbitrary code straight to a live system holding real PHI with no human second reviewer -- only automated checks (tests/SAST/secrets) stand between a bad merge and production.
   Fix:    When a second trusted person is available, enable Require a pull request before merging with 1 or more required approving reviews on the main branch protection rule (self-approval does not satisfy this in GitHub). Until then, document this as a deliberate, reviewed risk-acceptance (owner-operated single-person project) in the compliance evidence pack, and consider a partial compensating control such as a scheduled external/second-party code review of high-risk changes (auth, schema, clinical logic).
   Auto-fixable: no

🟠 HIGH · [CICD-05] Supply-chain provenance: SBOM, signed attestations, pinned actions/images            confidence: high   (CI/CD & Release Engineering)
   Where:  .github/workflows/laravel-ci.yml (every third-party uses: is pinned to a full commit SHA, e.g. lines 61, 63, 139, 216, 238, 262, and the Semgrep image is pinned by digest at line 238, this half is done well; but a fresh grep of both laravel-ci.yml and ci.yml for syft, cyclonedx, cdxgen, spdx, cosign, sigstore, attest-build-provenance and slsa returned zero matches, no SBOM step and no signing or attestation step exists anywhere, unchanged since the last audit)
   Why:    No SBOM is generated per release and no signed provenance or attestation exists, so if a dependency or an upstream action were ever compromised there is no artifact record to detect it against, and no way to answer a PDPL or compliance or incident-response question about exactly what code and dependencies were in a given production release beyond composer.lock and package-lock.json diffing by hand.
   Fix:    Add a CI step that generates a CycloneDX or SPDX SBOM from composer.lock and package-lock.json (e.g. cyclonedx-php-composer, cyclonedx-npm or cdxgen) and archives it as a build artifact per release or tag; optionally sign the release with cosign or GitHub actions/attest-build-provenance so a future deploy-time check (CICD-06) has something to verify.
   Auto-fixable: yes

🟠 HIGH · [CICD-08] Fully scripted, idempotent deploy with post-deploy smoke test + auto-rollback            confidence: high   (CI/CD & Release Engineering)
   Where:  laravel/docs/DEPLOY-LARAVEL.md:98-113 (section 3.3: smoke.sh, audit:verify and the scheduler check are all documented as run manually by a human operator after the swap, step 4 is literally a human check by an admin with MFA, not invoked automatically by the deploy); laravel/scripts/deploy-on-green.sh line 70 area (re-read in full: even in this opt-in script, a failed smoke test only logs that the operator should roll back per section 4.1 and exits 2, it never calls the Coolify rollback API itself); the script header states it is NOT installed by default and the operator decides whether to enable it, unchanged since the last audit; Coolify Auto Deploy remains off by owner decision
   Why:    No automated gate exists anywhere in the pipeline that both runs the smoke test and rolls back a bad release; recovery depends entirely on a human noticing a problem, correctly reading the rollback-trigger criteria, and manually running the Coolify UI or API rollback, in a single-environment system holding real PHI a broken release could stay live for however long the deploying operator is unavailable or distracted. This is materially mitigated, not eliminated, by a mandatory detailed RELEASE-CHECKLIST.md, a comprehensive scripts/smoke.sh, and a rollback that has now been rehearsed and measured TWICE (50s app-only swap, plus a second restore drill), which is why this stays graded High rather than the category-default Critical for a true no-smoke or no-rollback scenario.
   Fix:    Wire an automatic post-deploy gate: extend scripts/deploy-on-green.sh final smoke-check branch (or a Coolify post-deploy webhook) so a failing scripts/smoke.sh actually calls the rollback API for the previous SHA instead of only logging, then install it once the operator decides to move off fully manual operator-triggered deploys.
   Auto-fixable: no

🟠 HIGH · [CICD-11] Privileged release/control-plane actions require multi-party authorization            confidence: high   (CI/CD & Release Engineering)
   Where:  .github/CODEOWNERS now exists (added PR 14, commit a1a64b6, 2026-09-03) but names only the single maintainer for every path, so it cannot provide a second reviewer; gh api repos/ahmedsk2/dmc-patient-flow-hub/branches/main/protection (re-checked live) still has no required_pull_request_reviews block at all, so CODEOWNERS is not even wired as an enforced gate; gh api repos/ahmedsk2/dmc-patient-flow-hub/environments (re-checked live) still returns 0 environments, no deployment protection rule or required reviewers for the prod deploy trigger; laravel/docs/CI.md:19 still states outright that reviews are not required because it is a single maintainer project
   Why:    A single actor, including a hijacked or mistaken automated coding session, can author code, merge it to main once CI is green, and separately trigger a production Coolify deploy carrying real PHI, all with no second human required to catch a mistake or a malicious change, including a change to the CI/CD pipeline definition itself. The new CODEOWNERS file documents accountability (who owns what) but does not add a second-party check, since it names only the same single maintainer and branch protection does not require CODEOWNERS review anyway.
   Fix:    This remains a known, documented trade-off of a genuinely one-person team, not an oversight, so treat the fix as risk-reduction rather than a hard gate: when a second maintainer exists, require their review via required_pull_request_reviews plus a non-self CODEOWNERS entry on the workflows and migration or auth-critical paths; consider a GitHub Environment with a short required wait-timer on the prod deployment trigger (self-review still possible but adds a deliberate pause plus an audit entry); keep using the existing Second person aware field in RELEASE-CHECKLIST.md line 9 as the lightweight compensating control in place today.
   Auto-fixable: no

🟠 HIGH · [RES-12] Correct graceful shutdown / disposability            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/docs/DEPLOY-LARAVEL.md:300 (No drain step on the container swap, marked RES-12, open. Coolify starts the new container, repoints Traefik and stops the old one; a request in flight on the old container at that instant is cut.)
   Why:    Every deploy (and this app deploys frequently - CI runs on every push/PR to the sole protected main branch) can silently cut an in-flight clinical write (admission, discharge, handover save) mid-request with no drain window; the project's own runbook already names this exact gap and the two settings that would close it, and confirms neither is applied yet.
   Fix:    Apply the two changes docs/DEPLOY-LARAVEL.md already specifies: set a stop grace period on the Coolify application (Advanced, stop timeout e.g. 30s, which becomes docker stop -t) and set process_control_timeout to 20s in the image's php-fpm.conf so in-flight PHP-FPM requests are given time to finish before SIGKILL. Until applied, keep deploying only in the agreed low-activity window as the runbook already instructs.
   Auto-fixable: no

🟠 HIGH · [DATA-02] Backups automated, encrypted, and stored off-region            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/BACKUP-AND-RESTORE.md:110-133 (S2.3: S3_ENDPOINT/S3_REGION=me-riyadh-1, same endpoint/credential family as AUDIT_S3_*, separate bucket only) and :445 (S10.2: binlog-ship.py explicitly reuses the same key, same format, same bucket as the nightly dump -- the new PITR archive shares the identical region/credential blast radius); laravel/docs/compliance/CONFIRMED-FACTS.md:32 (B4: OCI tenancy is Riyadh region me-riyadh-1 only)
   Why:    Backups (both the nightly full dump and, as of PR #19, the hourly binlog archive) remain automated, AES-256-CBC/PBKDF2 encrypted with an escrowed key, and genuinely off-box in a separate OCI Object Storage bucket -- this still closes the original risk of plaintext dumps sitting beside the database. But every copy, including the new point-in-time increment, lives in the SAME OCI region and reuses the SAME credential family as production. A region-wide OCI outage, tenancy-level compromise or account suspension could still take out primary, base backup and PITR archive together -- the correlated-loss pattern this check exists to catch. Held at High rather than the category-default Critical for the same reason as the prior audit: the likeliest loss vectors (disk failure, host compromise, bad migration, ransomware on the app host) are genuinely mitigated by the off-box design, and Saudi PDPL in-Kingdom residency is a real, documented regulatory constraint on true cross-region replication, not an oversight. Adding PITR did not change this gap either way -- it widened what is protected, not where it is protected.
   Fix:    Segregate the backup bucket (and the new binlogs prefix) into a separate OCI compartment/tenancy with independent IAM credentials not derived from the same administrative account as production. Ask OCI whether a second Kingdom-resident region exists and evaluate a second in-Kingdom copy under separate administrative control. Document the residual correlated-loss risk and get an explicit owner risk-acceptance given the PDPL residency constraint.
   Auto-fixable: no

🟠 HIGH · [CFG-10] Cloud & service-to-service auth via short-lived federated identity            confidence: high   (Config, Secrets & Environments)
   Where:  laravel/app/Support/S3SigV4.php, a hand-rolled AWS SigV4 client keyed by a static access-key and secret pair; laravel/config/services.php lines 41-47 (audit_archive using AUDIT_S3_ACCESS_KEY/AUDIT_S3_SECRET); laravel/.env.example lines 153-160
   Why:    audit:ship (hourly) and backup:verify (daily) authenticate to OCI Object Storage with a static, non-expiring access-key and secret pair injected via env vars, even though the app already runs on the same OCI compute instance where native Instance Principal workload identity is available at no extra cost. A leaked or misconfigured key is a durable, standing credential with no auto-expiry and, per CFG-03, unconfirmed IAM scoping. CI itself has no cloud-auth exposure; laravel-ci.yml uses no cloud credentials at all, only the built-in GITHUB_TOKEN.
   Fix:    Migrate audit-log shipping and backup verification to OCI native REST API plus Instance Principal (no standing credential needed on an OCI compute instance); if the S3-compatible path-style endpoint must be kept, split AUDIT_S3 and DB_BACKUP_S3 into two separately-scoped least-privilege keys and put them on the CFG-04 rotation cadence.
   Auto-fixable: no

🟠 HIGH · [OPS-02] On-call rotation staffed, sustainable, with escalation            confidence: high   (Operational Readiness & Incidents)
   Where:  laravel/docs/compliance/INCIDENT-RESPONSE.md:36-48 (roles table, every holder is [PLACEHOLDER]) and :418-433 (Appendix A contact tree, entirely blank/[PLACEHOLDER]); missing: CODEOWNERS anywhere in the repo; CLAUDE.md section 2 (the owner handles all secrets) and session facts confirm the owner is the sole GitHub collaborator and sole information-security lead
   Why:    Bus-factor 1 for the entire operation: deploys, secret rotation, incident response and DB restores all route through one person. If that person is unavailable during an incident (SEV1 patient-data exposure or an outage during clinical hours), there is no documented second responder or escalation path, so pages/alerts would go unanswered.
   Fix:    Name a primary and at least one deputy for each role in INCIDENT-RESPONSE.md section 3 / Appendix A; stand up a minimal 2-person escalation path (even a shared phone/chat alert reaching two people, short of a formal pager tool); add a CODEOWNERS entry for the deploy/CI/ops paths so review and ownership are structurally enforced the day a second trusted person joins.
   Auto-fixable: no

🟠 HIGH · [OPS-03] Alerts are actionable, severity-tied, and not noisy            confidence: medium   (Operational Readiness & Incidents)
   Where:  laravel/docs/compliance/INCIDENT-RESPONSE.md:52-76 (section 4 Detection sources) describes the actual mechanisms: audit.integrity_failure (in-app bell to active admins plus best-effort e-mail), security.failed_logins (in-app bell only), dq:notify digest (in-app), backup:verify (in-app); no PagerDuty/Opsgenie/Grafana-OnCall config, no runbook_url/severity-annotated alert rules found anywhere in the repo
   Why:    Assumption stated: the app has no true paging mechanism (SMS/phone/on-call tool) tying any signal to a human outside the app; every signal surfaces as an in-app bell notification or a best-effort e-mail. An audit-chain break, a stale backup, or a failed-login burst overnight or on a weekend can sit unseen until someone next logs in or until the next-morning manual log skim called for in RELEASE-CHECKLIST.md section 8. This is the inverse of the check's classic alert-fatigue framing (no pages fire at all, rather than too many), but the operability risk - a real signal going unactioned - is the same.
   Fix:    For the highest-severity signals (audit.integrity_failure, a future backup-stale alert, repeated failed-login bursts), add at least one out-of-band channel that reaches a human outside the app session (SMS/phone alert, a monitored external distribution list, or a lightweight webhook into a free-tier paging tool) rather than relying solely on an in-app bell and best-effort e-mail.
   Auto-fixable: no

🟠 HIGH · [I18N-02] Timestamps in UTC; future events keep wall-clock + IANA zone            confidence: medium   (i18n, l10n & Text)
   Where:  laravel/config/database.php:47-65 (mysql connection: no timezone key set); laravel/config/app.php:68 + laravel/.env.example:37 (APP_TIMEZONE=Asia/Riyadh, applied via PHP date_default_timezone_set at boot); evidenced by the workaround already required in app/Http/Controllers/Auth/AuthController.php:158-164, app/Http/Controllers/SecurityController.php:24-29 and app/Http/Controllers/DashboardController.php:423-427
   Why:    Every Eloquent-managed TIMESTAMP column (created_at/updated_at on roughly 40 tables via timestamps()) is written from the PHP app-clock now() in Asia/Riyadh (UTC+3) wall-clock, but MySQL TIMESTAMP columns convert on write/read using the session time_zone (SYSTEM, the DB host OS zone, confirmed UTC on this deployment by code comments in the app itself). With no explicit timezone pinned on the connection, MySQL treats the Riyadh wall-clock string as if it were already UTC, storing every instant about 3 hours ahead of its true UTC value. This currently round-trips invisibly because both the write and the ordinary Eloquent read go through the same double mislabeling and self-cancel, but it breaks the moment a raw SQL clock function (NOW, CURDATE, INTERVAL arithmetic) is compared against one of these columns, silently shifting any recent-window or day-boundary query by exactly 3 hours. This has already happened once in this codebase: audit_log.created_at had to be specially routed through a DB-native useCurrent default plus DB raw NOW-INTERVAL comparisons, with an explicit comment explaining the app/DB timezone mismatch, specifically to avoid this bug for the failed-login-cluster and security-anomaly queries. Any future raw SQL query against a normal Eloquent timestamp column, an easy and unremarkable pattern, will silently misbehave by 3 hours; the same risk applies to any BI tool, DB replica, or a restore performed on a host with a different default OS timezone.
   Fix:    Pin timezone to +00:00 (or UTC) explicitly on the mysql connection in config/database.php so MySQL TIMESTAMP UTC-conversion is anchored to true UTC regardless of the app display timezone; keep APP_TIMEZONE=Asia/Riyadh only for display-layer Carbon conversions at the view/serialization boundary. This is a live DB session-timezone change on a production PHI system, so test first, add a regression test asserting a freshly inserted row stored TIMESTAMP epoch matches now().utc(), and review whether any existing raw SQL depends on the current accidental behavior before flipping it.
   Auto-fixable: no

🟡 MEDIUM · [REL-05] Symptom-based SLO burn-rate alerting            confidence: high   (Reliability, SLOs & Error Budgets)
   Where:  laravel/app/Console/Commands/BackupVerify.php:94-96; laravel/app/Console/Commands/AuditVerifyDaily.php; laravel/app/Http/Controllers/Auth/AuthController.php:146-177 (notifyOnFailureBurst); laravel/app/Console/Commands/DataQualityNotify.php
   Why:    All four alerting paths that exist (in-app admin notifications for backup.stale, audit.integrity_failure, security.failed_logins, and data-quality thresholds) are single-threshold, cause-based checks, not multi-window multi-burn-rate alerts against an SLI, because no SLI/SLO exists to burn against. A slowly-degrading user-facing problem such as rising page latency or error rate on admissions, discharges, or the consultation ledger would raise no alert today. Downgraded from the category default High to Medium because the alerts that do exist are real, deduplicated, and reasonably well targeted at this system's top risks for a single-tenant clinical app at this scale.
   Fix:    Once SLIs/SLOs exist, add a small number of symptom-based multi-window burn-rate alerts against them, layered on top of the existing cause-based operational alerts which are worth keeping for their own non-SLO risks.
   Auto-fixable: no

🟡 MEDIUM · [OBS-05] Distributed tracing with W3C context propagation            confidence: medium   (Observability)
   Where:  missing: tracing SDK. No opentelemetry/opentracing/jaeger/zipkin/apm package in laravel/composer.json or laravel/package.json; no traceparent propagation in app/Support/S3SigV4.php, the mail transport, or app/Console/Commands/AuditShip.php
   Why:    Outbound calls this app makes, the MySQL connection, the hourly audit-log shipment to the S3-compatible dmc-audit-log bucket (app/Support/S3SigV4.php), the nightly backup upload, and outbound SMTP, carry no trace context, so a slow or failing call can only be diagnosed from its own ad hoc Log::error() line (e.g. app/Console/Commands/AuditShip.php:78-89, app/Console/Commands/BackupVerify.php:133-136), not correlated end-to-end with the request or scheduled run that triggered it. Severity is graded down from the category default because this is a single-instance Laravel monolith, not a service mesh; its only hops are to one DB, one S3 bucket and one SMTP relay, so the marginal value of full W3C traceparent propagation is real but bounded; most of the same debuggability is available more cheaply via a per-request correlation ID (see OBS-09), which is also absent.
   Fix:    Lower-cost interim step: generate a request-scoped correlation ID in middleware and include it in every Log:: call and in outbound-call log lines (AuditShip, BackupVerify, mail jobs) so failures can be pivoted back to the triggering request or scheduled run. If the system later grows beyond a single instance or gains real service-to-service hops, adopt the OTel PHP SDK with auto-instrumentation for the PDO/MySQL client and an HTTP-client wrapper around the S3 SigV4 and SMTP calls.
   Auto-fixable: no

🟡 MEDIUM · [PERF-03] Pagination & bounded result sets            confidence: medium   (Performance & Scalability)
   Where:  app/Http/Controllers/PatientsController.php:241,249-270 (boardGroups: view=longterm sets includeDischarged=true, dropping the whereNull discharge_date filter on line 250; the query then runs get() with no limit/paginate anywhere in the method); reachable via the Long-term filter chip in resources/js/Pages/Patients/Index.vue:292
   Why:    Every other list surface in the app is deliberately bounded (Registry paginate 20, Consultations paginate 15, Audit paginate 50, CSV/XLSX export via chunk 500) but the long-term board view is a monotonically-growing historical query with no page cap or date filter: it returns every long-term-flagged episode ever recorded, open or closed. As the flagged cohort accumulates over years of the hospital's operation this becomes an ever-larger single JSON response and an ever-larger table scan behind one click, unlike the default open-episode board which is naturally bounded by physical bed capacity.
   Fix:    Paginate the longterm board view (or add a date-range/status cap) the same way Registry already paginates history; leave the default open-episode and TB views alone since they stay bounded by discharge_date IS NULL.
   Auto-fixable: no

🟡 MEDIUM · [PERF-04] Core Web Vitals & API payload bounds            confidence: high   (Performance & Scalability)
   Where:  missing: web-vitals / reportWebVitals / lighthouserc wiring anywhere in laravel/ (grepped resources/js, resources/views/app.blade.php, package.json, .github/workflows -- none found)
   Why:    LCP/INP/CLS are never measured in the field or gated in CI, so a rendering or bundle-size regression on a heavy page (Dashboard charts, Statistics, Registry search) would ship and stay invisible until a clinician complains rather than being caught by a metric or a CI budget. API payload discipline itself is good (Registry/Statistics/Dashboard controllers select narrow column sets) and response compression is plausibly handled by the Cloudflare edge in front of the origin, so the concrete gap is specifically the absence of Vitals measurement, not payload bloat.
   Fix:    Wire the web-vitals package to report LCP/INP/CLS to an existing logging/audit endpoint, or add a Lighthouse CI budget step to the laravel-ci.yml frontend job (deepens under --lighthouse).
   Auto-fixable: yes

🟡 MEDIUM · [PERF-07] Horizontal scaling, statelessness & caching limits            confidence: medium   (Performance & Scalability)
   Where:  app/Jobs/GenerateMonthlyPdf.php:32 (Storage disk local put report file); app/Http/Controllers/ReportsController.php:470-474 (downloadGenerated reads the same local disk path in a later, separate request); docs/DEPLOY-LARAVEL.md:302 (project's own docs: storage/ persistence across a redeploy is unconfirmed, check the app's Storages tab)
   Why:    The dominant PERF-07 risk from the 2026-09-03 morning audit -- sessions/cache living only in one container's memory -- is closed: SESSION_DRIVER=database and CACHE_STORE=database (config/session.php:21, config/cache.php:18, .env.example) are verified live in production, and app/Support/DashboardCache.php uses a single-flight Cache lock with jittered TTL to avoid a cache-stampede. What remains is narrower: the queued monthly-PDF-booklet feature still writes to and reads from the container's private local disk. Because QUEUE_CONNECTION=sync, generation runs inline in whichever web-serving process handled the request; a container swap (redeploy) between generation and download loses the file (404 for the admin who requested it), and the same pattern would not work at all across two horizontally-scaled replicas without shared storage. Bounded: admin-only, roughly one generated PDF per admin per year, not a core clinical flow, and not an active problem on today's single-host deployment.
   Fix:    Store generated reports on the same S3-compatible bucket already used for audit shipping and backups (app/Support/S3SigV4.php gives a ready-made client) instead of the local disk, or explicitly confirm and document a persistent volume mount for storage/app/reports in Coolify before adding a second app replica.
   Auto-fixable: no

🟡 MEDIUM · [PERF-08] Resource pools, GC, deadlines & load shedding            confidence: medium   (Performance & Scalability)
   Where:  routes/web.php (only throttle:auth/register/register-email/stepup/email-verify/csp-report are rate-limited; no general throttle on Registry search/export, Dashboard, Statistics or the Patients/Consultations boards); bootstrap/app.php (no request-deadline middleware, no general 503/Retry-After overload-shedding path); no nixpacks.toml/php-fpm.conf/pool-sizing config committed anywhere in laravel/; docs/DEPLOY-LARAVEL.md:300 (project's own docs, RES-12: no drain step on the container swap; stop-timeout / process_control_timeout settings identified but not yet applied)
   Why:    PHP-FPM worker count versus MySQL connection capacity is not aligned anywhere visible in the repo (Nixpacks/Coolify defaults apply, opaque to the codebase); outside the auth-adjacent throttled routes there is no general request timeout or overload-shedding path, so a burst of slow requests (for example concurrent PDF generation, which per docs/DEPLOY-LARAVEL.md:311 holds the request open since QUEUE_CONNECTION=sync) would queue and degrade rather than shed predictably. Real-world risk is bounded today by the small, known user population (roughly 330 staff, one clinical unit) rather than internet-scale traffic, and this is unmeasured either way.
   Fix:    Document the Nixpacks/Coolify PHP-FPM worker count and MySQL max_connections against single-host capacity; add a general throttle on the heavier admin read paths (Registry/Statistics/Reports); apply the already-identified Coolify stop-timeout / process_control_timeout settings from docs/DEPLOY-LARAVEL.md section 10; consider a maintenance/overload 503 response with Retry-After for DB exhaustion.
   Auto-fixable: no

🟡 MEDIUM · [RES-01] Timeouts on every integration point            confidence: medium   (Resilience & Fault Tolerance)
   Where:  laravel/config/database.php:62-64 (mysql options sets only MYSQL_ATTR_SSL_CA, no PDO connect/read timeout); laravel/resources/js/Components/IcdTypeahead.vue:25, laravel/resources/js/Layouts/AppLayout.vue:297,314-315, laravel/resources/js/Pages/Registry/Index.vue:97 (raw fetch with no AbortController/timeout)
   Why:    The mysql connection has no explicit PDO connect/read timeout, so a DB-side stall (lock wait, saturated connections) blocks a PHP-FPM worker for however long MySQLs own server-side defaults allow, not a value this app controls; concurrently, browser fetch calls to notifications/typeahead/registry-count have no client-side timeout/abort, so a slow backend leaves the UI hung with no user-visible recovery short of a manual reload.
   Fix:    Set an explicit PDO connect timeout (PDO::ATTR_TIMEOUT) and consider PDO::MYSQL_ATTR_READ_TIMEOUT (or a bounded SET SESSION MAX_EXECUTION_TIME) in config/database.php's mysql options; add AbortController plus a several-second timeout to the polling/typeahead fetch calls so the UI fails visibly instead of hanging.
   Auto-fixable: yes

🟡 MEDIUM · [RES-09] Single points of failure & failure-mode enumeration            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/docs/DEPLOY-LARAVEL.md topology table section 0 (one OCI Ubuntu instance, one mysql:8 container on the same host, no replicas, no second AZ/region); CLAUDE.md: There is one Laravel environment. There is no staging.
   Why:    A single host or MySQL-container failure is a full-outage event for a live clinical PHI system with no automatic failover; recovery depends entirely on provisioning a new host and restoring the latest encrypted off-box backup (RPO under 24h), a path that is drilled for DB-content restore (7-8s for a 20MB dump per docs/BACKUP-AND-RESTORE.md section 8) but not for a full host rebuild, so the real host-failure RTO is undrilled and unknown.
   Fix:    Document and drill a full host-loss runbook (new OCI instance, Coolify redeploy, restore latest backup, re-point DNS/Cloudflare), even if true redundancy (second AZ/host) is judged not worth the cost at this scale; that at least converts an unknown RTO into a measured one.
   Auto-fixable: no

🟡 MEDIUM · [DATA-12] Business Impact Analysis, bus-factor SPOFs, and crisis comms            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:36 (B8: sole GitHub collaborator); .github/CODEOWNERS now names the maintainer (PR #14) but that is ownership attribution, not cross-training or a second credential holder; no BIA/BCP/crisis-communication document found anywhere under laravel/docs/
   Why:    No documented Business Impact Analysis backs the RPO/RTO numbers claimed in DATA-01 (now a real, measured <=1h/<=24h split), and the system owner remains the sole GitHub collaborator and sole information-security lead -- a self-acknowledged single point of failure for deploys, credentials (including the escrowed backup-encryption key) and incident response. No crisis-communication plan (declaration owner, notification templates, status-page ownership) exists. If that one person is unavailable during an incident on a live-PHI system, there is still no documented second path to deploy, restore a backup, replay a PITR window, or tell the clinical unit what data window was lost.
   Fix:    Conduct a lightweight BIA that explicitly derives the accepted RPO/RTO from clinical impact; cross-train or secret-share at least one additional person on deploy access and the backup-encryption-key escrow; write a short crisis-communication plan naming who declares an incident and how the unit is told what data window was lost.
   Auto-fixable: no

🟡 MEDIUM · [DATA-13] Per-release supply-chain evidence archived to immutable storage            confidence: high   (Data, Backups, DR & Correctness)
   Where:  missing: SBOM/provenance/attestation step; re-checked .github/workflows/laravel-ci.yml (repo root, governs laravel/) -- still no sbom, cyclonedx, spdx, attestation or provenance keyword found anywhere in the workflow, including the new backend-job Python step added by PR #19
   Why:    CI runs composer audit (arbitrated by scripts/composer-audit-gate.php), gitleaks/Semgrep as blocking gates, Dependabot, a coverage-gate, Pint and now 100 blocking Python backup-tooling tests -- a materially stronger gate set than before -- but still no SBOM, build provenance/attestation, or per-release dependency manifest is generated or archived to tamper-protected storage. Which shipped releases carried dependency X is still not answerable from the repo alone, which slows incident response if a dependency CVE surfaces later. Bounded impact: single-tenant internal system; the SHA-tagged image plus the committed composer.lock/package-lock.json at that commit is a weaker, non-immutable substitute for the same traceability.
   Fix:    Add a CycloneDX/SPDX SBOM generation step to laravel-ci.yml (e.g. cyclonedx-php-composer and a CycloneDX npm generator) and archive the SBOM plus the composer-audit-gate output per commit SHA to a retention-locked location, reusing the existing S3SigV4 pattern already used for the audit/backup archives.
   Auto-fixable: yes

🟡 MEDIUM · [DATA-14] Storage/data footprint minimized and lifecycled            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/compliance/DATA-RETENTION.md:61,102,104,194-199 (rows: no lifecycle rule confirmed on the 90-day backup-bucket placeholder, manual pre-deploy dumps accumulate unencrypted on the host with no expiry rule, legacy dump inventory and destruction still owed as of PR #18 rework); laravel/docs/compliance/CONFIRMED-FACTS.md:60 (D1: PHI-copy inventory -- legacy dumps on the owner workstation and plaintext dmc_demo.sql.gz plus leftover key files under /home/ubuntu/migrate/dmc/ on the OCI host -- still tagged VERIFIED gap, unchanged since the prior audit)
   Why:    The compliance documentation in this tree, current as of this re-audit (post PR #18 per-activity rework), still lists multiple ungoverned plaintext copies of regulated health data outside the controlled backup pipeline, and clinical rows are retained indefinitely with no purge or anonymisation mechanism; the backup bucket own 90-day lifecycle rule is still a placeholder not confirmed as applied. Severity held at Medium (raised from the category Low default) because these are unencrypted, unmanaged copies of PHI, not a mere cost/hygiene concern. As with the prior audit, an out-of-band claim exists that some of these host-level plaintext copies were shredded operationally, but that action is not visible from the repository (host filesystem is out of audit scope, read-only, no production access), is not restated as a documentary fact in this re-audit addendum (unlike the binlog-install fact, which explicitly is), and the compliance docs in the tree still describe the gap as open -- so it is scored on the tree own current state, not credited.
   Fix:    Inventory and securely delete every ad-hoc plaintext dump on the owner workstation and the OCI host; confirm and document the actual lifecycle rule configured on the dmc-db-backups bucket (now covering both the nightly-dump prefix and the new binlogs prefix); encrypt and auto-expire manual pre-deploy dumps; update DATA-RETENTION.md and CONFIRMED-FACTS.md once verified so the compliance paper trail matches reality.
   Auto-fixable: no

🟡 MEDIUM · [CFG-11] Environments rebuildable from IaC with drift detection & parity            confidence: high   (Config, Secrets & Environments)
   Where:  missing: Terraform, OpenTofu, Pulumi, CloudFormation, Ansible or nixpacks.toml anywhere in the repo; laravel/docs/DEPLOY-LARAVEL.md section 3.1 documents provisioning the OCI instance, Coolify application and MySQL container via manual SSH commands and the Coolify UI or hand-typed REST API calls
   Why:    Production infrastructure cannot be reliably reproduced from version-controlled definitions after a loss, and there is no drift detection to catch a manual console change diverging from the documented runbook. There is only one environment, which bounds but does not eliminate the parity-gap dimension of this check.
   Fix:    Codify the OCI instance, Cloudflare rules and object-storage buckets in Terraform or OpenTofu, or at minimum export and version the Coolify application definition; add a scheduled plan or drift-detection job; keep DEPLOY-LARAVEL.md as interim fallback documentation until then.
   Auto-fixable: no

🟡 MEDIUM · [CMP-02] Data-subject rights mechanisms (access, erasure, portability, rectification)            confidence: medium   (Compliance, Privacy & Legal)
   Where:  laravel/docs/compliance/DATA-RETENTION.md:21 (Nothing in this schedule is implemented as automatic destruction of clinical data today), :46-61 (new section 2.0 copy inventory C1-C7, most Destruction mechanism cells read None), :190 (records:prune still GAP), :199 (contract still the top action); laravel/docs/compliance/ROPA.md:352-370 (section 5, rewritten this session: Destruction is not real while the legacy copy and its backups survive; A correction applied in one system does not propagate to the other GAP); laravel/docs/compliance/CONFIRMED-FACTS.md:60 D1 (uninventoried PHI dump copies; owner workstation 7 legacy dumps still listed present; OCI host leftover files reported shredded today per operator, an operational claim not reflected in this doc and not independently verifiable from the repo); laravel/docs/compliance/DPO.md:57 R6 (DSAR/HIM-office workflow still an unagreed draft, file untouched this session)
   Why:    A patient or staff erasure/portability/rectification request still cannot be mechanically fulfilled or verified as complete: the Laravel app retains its audited admin hard-delete plus soft-delete/trash path for a live row, but no records:prune or anonymization mechanism was built this session, and the reworked ROPA/DATA-RETENTION docs now state explicitly, not just implicitly, that a correction or destruction executed in the Laravel copy does not reach the legacy daily database, SiteGround-held backups, or the still-uninventoried migration dumps. The documentation got more honest and more granular (seven named copies instead of one vague line) but the underlying gap is unchanged: no automated purge, no cross-system propagation, no logged SLA.
   Fix:    Build the records:prune / anonymization command DATA-RETENTION.md section 2.1 already scopes (dry-run default, --confirm gated, audited, mirroring audit:prune); finish the C1-C7 copy inventory and destruction certificates (G8), including confirming and documenting the reported OCI-host file shredding; formalize and log the DPO.md R6 HIM-office intake workflow with a tracked SLA.
   Auto-fixable: no

🟡 MEDIUM · [CMP-03] DPIA, ROPA, DPAs & 72-hour breach notification            confidence: high   (Compliance, Privacy & Legal)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:18 A5 (Controller-processor contract: None exists yet, unchanged this session) and :19 A6 (DPO gap, unchanged); laravel/docs/compliance/DPA-AND-TRANSFERS.md:19 P1, :21 P3 (No signed DPA on file for OCI and the US SMTP relay, file untouched this session); laravel/docs/compliance/DPIA.md:352-397 (three new risks this session: R13 un-hardened legacy daily build = 25 Critical inherent and residual, R14 clinical record hosted outside the Kingdom without a safeguard = 20 Critical, R15 second complete copy during the parallel period = 12 High, all explicitly unmitigated pending a cutover decision)
   Why:    DPIA.md, ROPA.md and INCIDENT-RESPONSE.md exist as substantive, now more precise drafts. The DPIA rework adds three new Critical/High risks (R13-R15) that make the cross-border and parallel-copy exposure explicit and quantified rather than implied, but no processor (OCI, Cloudflare, the SMTP relay) has a signed DPA, no controller-processor contract exists between the hospital and the operator company, and R13/R14 are stated in-repo as unmitigated by anything currently shipped. A regulator or governance review asking for Art. 28-equivalent processor agreements or a completed transfer risk assessment for the US relay still finds none.
   Fix:    Prioritize signing the controller-processor contract (G12, still flagged as the top action item in the docs themselves) and the OCI/Cloudflare/SMTP-relay DPAs; complete the Art. 7-style transfer risk assessment in DPA-AND-TRANSFERS.md for the US relay; decide and date the cutover (DPIA action 0a) since the document itself states this is the single measure that closes R13/R14/R15; have the DPO, once appointed, formally adopt DPIA.md/ROPA.md/INCIDENT-RESPONSE.md rather than leaving them perpetual v0.1 drafts.
   Auto-fixable: no

🟡 MEDIUM · [CMP-06] Privacy-by-design: notice, consent & DPO            confidence: medium   (Compliance, Privacy & Legal)
   Where:  PASS component, unchanged: routes/web.php plus app/Http/Controllers/LegalController.php plus resources/js/Pages/Auth/Login.vue:95 (a real, reachable, EN+AR /privacy notice linked from the login page). FAIL driver, unchanged this session since DPO.md was not touched by PR #18: laravel/docs/compliance/CONFIRMED-FACTS.md:19 A6 (Data Protection Officer: Not appointed yet); laravel/docs/compliance/DPO.md:119,144 (contact still the literal placeholder dpo-at-HOSPITAL-DOMAIN / dpo@[hospital-domain].sa)
   Why:    The projects own confirmed facts still treat a DPO as mandatory for this scale of health-data processing, yet none is appointed, and the published /privacy notice has no real DPO contact, an accountability and transparency gap that leaves data subjects and SDAIA with no named escalation point beyond the generic HIM office. This is unchanged from the prior audit; the compliance rework this session touched ROPA, DATA-RETENTION, DPIA, CONFIRMED-FACTS, DATA-CLASSIFICATION and OPEN-ITEMS but explicitly did not touch DPO.md.
   Fix:    Appoint or formally name an interim DPO or data-protection lead, and replace the placeholder DPO contact in DPO.md and, once published, on /privacy with a real, monitored contact.
   Auto-fixable: no

🟡 MEDIUM · [I18N-05] Unicode-correct string operations (NFC, graphemes, invariant case)            confidence: medium   (i18n, l10n & Text)
   Where:  app/Providers/AppServiceProvider.php:30 (RateLimiter::for(auth, ...) uses plain strtolower, not mb_strtolower)
   Why:    The app has an established, explicitly commented convention of using mb_strtolower for login-identity case folding (app/Http/Controllers/Auth/AuthController.php:41, tagged multibyte-safe I18N-05; app/Http/Controllers/Auth/UsernameReminderController.php:34) because usernames are validated with the alpha_dash rule, which is Unicode-letter-aware by default in this Laravel version, and the DB match relies on utf8mb4_unicode_ci case-insensitive collation. The auth rate-limiter key builder in AppServiceProvider.php was missed: plain strtolower under the PHP default C locale leaves non-ASCII bytes untouched, so a username differing only by the case of a non-ASCII letter would bucket into a different throttle key than the DB considers the same account, letting an attacker split brute-force attempts across multiple 5-per-minute buckets against one account. This is a throttle-evasion gap, not an authentication bypass or identifier collision (no evidence any actual account uses a non-ASCII username), so it does not meet the category Critical-escalation bar reserved for a confirmed auth or uniqueness case-fold bug.
   Fix:    Change AppServiceProvider.php:30 to mb_strtolower with UTF-8 encoding to match the rest of the codebase own I18N-05 convention for this exact login flow.
   Auto-fixable: yes

🟡 MEDIUM · [I18N-06] RTL/bidi layout, glyph coverage & text expansion            confidence: medium   (i18n, l10n & Text)
   Where:  resources/views/reports/annual-pdf.blade.php:22, consultant-pdf.blade.php:23, governance-pdf.blade.php:18, monthly-pdf.blade.php:12, statistics-pdf.blade.php:13 (each hardcodes font-family DejaVu Sans, sans-serif); vendor/dompdf/dompdf/lib/fonts/ ships only DejaVu Sans/Serif/Mono (Latin/Cyrillic/Greek, no Arabic glyphs) and no Arabic-capable font is registered in config/dompdf.php or storage/fonts
   Why:    Patient and consultant name fields are validated as free Unicode text with no ASCII-only restriction (app/Http/Requests/StoreAdmissionRequest.php:39, string/max:191), consistent with a Saudi Arabic-speaking hospital. If any name or other free-text field contains Arabic script, the five dompdf-rendered report PDFs, including the monthly report emailed externally to report_recipients, would render those characters as missing-glyph tofu boxes or blank text, since dompdf has no Arabic-covering font in its resolution chain for the declared font-family. This is a data-fidelity defect on official and compliance-facing documents, not a security issue. Separately, the one genuinely bilingual and RTL surface, Pages/Legal/Privacy.vue, is well built: dir=rtl and lang attributes, logical CSS (ps-6, text-start), and a Vitest+axe spec asserting dir=rtl rendering in Legal/__tests__/Privacy.spec.js; that part is not the failure.
   Fix:    Add an Arabic-capable font such as Noto Naskh Arabic or Amiri to storage/fonts and reference it via a font-family fallback chain in the five report Blade templates (dompdf resolves font-family lists in order; a name-based font substitution map in config/dompdf.php may also be needed), or transliterate/flag non-Latin names before PDF generation if Arabic rendering in dompdf proves unreliable even with a suitable font.
   Auto-fixable: no

🔵 LOW · [RES-03] Circuit breakers on flapping dependencies            confidence: medium   (Resilience & Fault Tolerance)
   Where:  no CircuitBreaker/breaker pattern anywhere in app/ - every call to the SMTP relay (app/Http/Controllers/Auth/RegisterController.php:106, EmailVerificationController.php:55) and the S3-compatible archive (app/Support/S3SigV4.php) waits its own bounded timeout on every single request instead of failing fast after repeated recent failures
   Why:    During a sustained SMTP relay or object-storage outage, every affected request still pays the full bounded wait (10s mail / 30s S3) instead of failing immediately once the dependency is known-down; at this app's modest concurrency (330 staff, one hospital unit) this degrades UX rather than cascading, which is why severity is kept low.
   Fix:    If outage frequency ever becomes a real nuisance, wrap the SMTP/S3 clients in a simple in-process breaker (a cache-backed failure counter that short-circuits to the existing friendly-error path for N seconds after M consecutive failures) rather than re-waiting the full timeout every time.
   Auto-fixable: no

🔵 LOW · [RES-04] Bulkheads & back-pressure (bounded resources)            confidence: medium   (Resilience & Fault Tolerance)
   Where:  laravel/.env.example:105 (QUEUE_CONNECTION=sync); laravel/config/mail.php:48-51 (comment self-documents that every OTP/reset/report send runs synchronously inside the web request, sharing the same PHP-FPM pool as all other traffic)
   Why:    There is no dedicated worker pool for mail-triggered requests; a burst of concurrent registration/verification/password-reset submissions during an SMTP brownout each occupy an FPM worker for up to the 10s mail timeout, competing with unrelated clinical-page requests for the same shared pool. Bounded and already mitigated by the explicit 10s cap (config/mail.php), but there is no partitioning if the pool is small.
   Fix:    If FPM pool sizing ever proves too small in practice, either move mail sends to a real queue connection with its own worker, or size the FPM pool with mail-outage bursts in mind. Low priority at current staff/traffic scale.
   Auto-fixable: no

🔵 LOW · [RES-06] Idempotent retry/redelivery targets            confidence: medium   (Resilience & Fault Tolerance)
   Where:  app/Http/Controllers/AdmissionsController.php:100-136 (store() has no server-side duplicate-submission guard); client-side mitigation only at resources/js/Pages/Admissions/Create.vue:128-130 (disabled bound to form.processing)
   Why:    No automated retry mechanism exists anywhere in this codebase (QUEUE_CONNECTION=sync, no webhook/queue redelivery), so this check's primary risk (automated retry double-applying a side effect) does not apply. The residual risk is a manual double-submit (double-click race, browser back-and-resubmit, or a client-side network retry) creating two admission rows for the same patient/day; mitigated client-side by disabling the submit button but not guarded server-side.
   Fix:    Add a server-side guard in the store() transaction: reject (or reuse) a second admission for the same patient with no existing open admission created in the last few seconds, or issue a short-lived per-form idempotency token.
   Auto-fixable: no

🔵 LOW · [CFG-06] Dev/staging/prod parity; complete .env.example            confidence: high   (Config, Secrets & Environments)
   Where:  laravel/config/mail.php line 51 reads env MAIL_TIMEOUT default 10, but MAIL_TIMEOUT is absent from laravel/.env.example; confirmed by extracting all env() keys referenced under app/ and config/ and diffing against .env.example, this was the only genuinely missing key
   Why:    An operator tuning SMTP timeout behavior has no way to discover the var exists without reading source; onboarding friction only, since the code default of 10s is safe and there is no functional risk today.
   Fix:    Add MAIL_TIMEOUT=10 next to the other MAIL_ keys in laravel/.env.example with a short comment matching config/mail.php lines 48-51.
   Auto-fixable: yes

🔵 LOW · [DOC-07] On-call, ownership and key links            confidence: high   (Documentation & Runbooks)
   Where:  laravel/docs/compliance/INCIDENT-RESPONSE.md lines 8, 40-45, 81 (the Appendix A role/contact table and the report-to-technical-lead escalation instruction still read [PLACEHOLDER - phone/chat])
   Why:    CODEOWNERS now names an accountable maintainer and SECURITY.md/security.txt now carries a real published disclosure contact, so who-owns-this and how-to-report-a-vulnerability are answered. But the in-repo incident-response runbook, the document a responder is meant to follow during a live PHI incident, still has no filled-in escalation channel even for the Technical Lead role, which maps to the same single maintainer named in CODEOWNERS. A responder following the runbook literally cannot page anyone.
   Fix:    Fill the Technical Lead / Incident Lead rows of INCIDENT-RESPONSE.md Appendix A with the maintainer's real contact and channel, mirroring the SECURITY_CONTACT approach, so the runbook is self-consistent with CODEOWNERS. The remaining rows (DPO, Clinical owner, Executive sponsor, Communications) depend on organisational appointments the owner has explicitly parked - track those via the intake item below, not as part of this fix.
   Auto-fixable: no

## What's Good (per auditor)
- (Reliability, SLOs & Error Budgets) app/Http/Controllers/HealthController.php is a genuine deep liveness probe (fresh-connection DB check with a 2s bound, storage-writable check, and a scheduler heartbeat that goes stale after 5 minutes) built specifically because the scheduler had previously died silently.
- (Reliability, SLOs & Error Budgets) scripts/smoke.sh runs a post-deploy check against /up, /health and the build manifest, so a broken deploy is caught quickly rather than discovered by a user report.
- (Reliability, SLOs & Error Budgets) Cause-based operational alerting exists and is reasonably well targeted at this system's real top risks: stale or missing off-box backups, a broken audit hash-chain, and failed-login bursts all raise deduplicated in-app notifications to active admins.
- (Reliability, SLOs & Error Budgets) docs/DEPLOY-LARAVEL.md section 0 documents the production topology clearly enough to serve as the starting point for a real dependency map if REL-08 is picked up.
- (Reliability, SLOs & Error Budgets) docs/BACKUP-AND-RESTORE.md section 1 states RPO and RTO in plain English and records a real, measured restore drill rather than an untested claim.
- (Observability) Production logging is genuinely structured: the stderr channel (config/logging.php:98-111) emits JSON lines via Monolog JsonFormatter, matches 12-factor logs-as-event-streams, and LOG_LEVEL is a runtime env var (confirmed set to warning live as of 2026-09-03 per the detection summary) rather than baked into the image.
- (Observability) No secret or PII leakage found in any server-side Log:: call site across the codebase (7 files); narrative/PHI columns are never logged even on the EncryptedNarrative decrypt-fallback path, and the CSP-report sink explicitly allow-lists a fixed, PHI-free field set with length caps.
- (Observability) /health (app/Http/Controllers/HealthController.php) is a real readiness probe, not a static 200: it opens a fresh, 2-second-bounded DB connection (not the request own, so it proves the server still accepts new connections), checks storage writability, and tracks a scheduler-heartbeat staleness window (this project has previously had a silently-dead scheduler, so the check is motivated by a real incident), correctly kept separate from Laravel stock /up liveness route that Coolify actually polls.
- (Security & App Hardening) Zero real secrets in tracked files or full git history (489 commits scanned); the only gitleaks matches are AWS-published SigV4 documentation test vectors, narrowly allow-listed by exact string and path in laravel/.gitleaks.toml
- (Security & App Hardening) composer audit and npm audit both report 0 advisories today; both are blocking CI gates (scripts/composer-audit-gate.php enforces a reasoned, dated .composer-audit-ignore.json; npm audit --omit=dev --audit-level=moderate)
- (Security & App Hardening) Disciplined query building: bound parameters throughout, sort key/direction whitelisted in RegistryController::resolveSort, route params force-typed to int, and CSV/XLSX exports are formula-injection-guarded (leading equals/plus/minus/at characters quoted in RegistryController.php:491 and AuditController.php:177)
- (Security & App Hardening) Mandatory TOTP MFA for every account (not just admins), bcrypt password hashing, session regeneration and remember-me disabled post auth-hardening, host-prefixed session cookie, uniform anti-enumeration responses on login and password-reset, and full session plus trusted-device invalidation on password reset
- (Security & App Hardening) SecurityHeaders middleware ships an A-grade header set: per-request nonce CSP, frame-ancestors none plus X-Frame-Options DENY, HSTS with preload, Permissions-Policy, Cache-Control no-store on authenticated pages, applied even to CSRF-error responses (prepended to the web group)
- (Security & App Hardening) CI runs Semgrep (p/php, p/owasp-top-ten, p/javascript, ERROR-severity blocking) and gitleaks as required, admin-enforced status checks alongside PHPUnit/Vitest
- (Security & App Hardening) Identity-keyed (username/session/user-id plus IP, not IP-only) rate limiters with per-flow tuning on every abuse-prone auth endpoint (login, registration, the email-relay step, step-up, email-verify), backed by a shared database cache store
- (Testing & Code Quality) All 4 required CI jobs (frontend, backend, secrets, sast) are green at current HEAD 9f81bb9, verified live via gh run view job list, and main is branch-protected with those 4 checks required and enforced for admins.
- (Testing & Code Quality) Executed locally this session: ESLint clean, Pint clean, and the full Vitest suite (83 files, 754 tests) green with coverage above all four configured thresholds (83.28 percent statements vs 80 floor, 78.14 branches vs 76, 45.89 functions vs 44).
- (Testing & Code Quality) Backend enforces an explicit statement-coverage floor via scripts/coverage-gate.php (83 percent, measured 86.15 percent), fed from PHPUnit own Clover output, a real enforced gate rather than a suite that merely happens to run.
- (Testing & Code Quality) Critical clinical paths (mandatory MFA, login by username or email, admission and transfer and two-phase discharge, consultation ledger, handovers) each have dedicated, substantial Feature test files, not just incidental coverage.
- (Testing & Code Quality) A deliberate global unhandledrejection net in app.js is explicitly comment-labelled as addressing TST-10, showing the team is already tracking this exact audit dimension.
- (Testing & Code Quality) Zero TODO/FIXME/HACK/XXX markers and zero stray debug statements anywhere in tracked PHP/Vue source, a genuinely clean codebase after repeated hardening rounds.
- (Testing & Code Quality) Dependency surface is small and deliberately curated on both sides; every sample-checked dependency is actually used in source.
- (CI/CD & Release Engineering) All 4 Laravel CI jobs (frontend, backend, secrets, sast) are required, admin-enforced status checks on main with no force-push and no delete allowed, re-verified live via gh api during this re-audit, and a live run (HEAD 64a76a6, run 33792783208) was watched going green on 3 of 4 jobs during the audit itself.
- (CI/CD & Release Engineering) Every third-party GitHub Action is pinned to a full commit SHA (with the resolved tag as a comment) and the Semgrep scanner is pinned by image digest, so a re-tagged upstream release cannot silently change what CI runs; re-confirmed unchanged.
- (CI/CD & Release Engineering) Blocking composer-audit gate (scripts/composer-audit-gate.php) with an expiring, per-advisory reasoned ignore-list (.composer-audit-ignore.json, review_by 2026-12-03 dates), so unknown-severity and high or critical advisories cannot be silently swallowed.
- (CI/CD & Release Engineering) Build-plane hardening remains genuinely tight: workflow-level permissions default to contents read, narrowly-scoped per-job widening, GitHub-hosted ephemeral runners only, no pull_request_target usage despite the repo being public.
- (CI/CD & Release Engineering) A dedicated CI step fails the build if the committed public/build directory differs at all from a fresh npm run build, a real enforced reproducible-build guarantee, not just committed lockfiles.
- (CI/CD & Release Engineering) Migration and rollback discipline deepened since the last audit: the rollback rehearsal (50s app-only swap, 121s roll-forward) and the restore drill (7-8s for a 20MB dump) were each repeated a second time on 2026-09-03, both logged with dated rows and matching counts in DEPLOY-LARAVEL.md section 4.4 and BACKUP-AND-RESTORE.md section 8, now measured twice, not once.
- (CI/CD & Release Engineering) The legacy CI pipeline (ci.yml) was switched to workflow_dispatch-only, removing a chronically-red, non-required workflow from the repo's automatic signal, a small but real pipeline-hygiene improvement credited this round.
- (CI/CD & Release Engineering) A CODEOWNERS file was added naming the accountable maintainer for every path, useful documentation of accountability even though it does not by itself satisfy CICD-11's multi-party-authorization bar for a solo maintainer.
- (Performance & Scalability) Per-page code splitting via Inertia resolvePageComponent + import.meta.glob, plus an explicit defineAsyncComponent for ChartCanvas (app.js:8) and a dynamic import() for zxcvbn (PasswordMeter.vue:23) -- this closes the ChartCanvas-in-every-bundle finding from the 2026-09-03 evening re-score.
- (Performance & Scalability) No full-library barrel imports anywhere in resources/js (no lodash/moment/rxjs); fonts are self-hosted via laravel-vite-plugin's bunny() helper, no third-party render-blocking script tags.
- (Performance & Scalability) Deliberate, documented anti-N+1 discipline: PatientsController::boardGroups batch-loads handovers, pending signatures and ICD-10 names in a few grouped queries before any foreach, with an inline comment noting no per-card queries; ReportsController's docblock states its metric families never use per-month/per-day query loops.
- (Performance & Scalability) Registry, Consultations and Audit list endpoints all use hard, non-client-controllable server-side page sizes (paginate 20/15/50); CSV/XLSX exports stream via chunk(500) with one batched ICD-10 lookup per chunk instead of materializing the full result set.
- (Performance & Scalability) Migrations add explicit single and composite indexes on every column actually filtered in hot queries, plus Laravel's automatic FK index on consultant_id.
- (Performance & Scalability) app/Support/DashboardCache.php implements a single-flight Cache lock with jittered TTL specifically to prevent a cache-stampede when many ward workstations hit an expired dashboard cache at once.
- (Performance & Scalability) SESSION_DRIVER=database and CACHE_STORE=database (shipped 2026-09-03) externalize session and cache state so they survive a redeploy and would be shareable across a future second replica -- closing what was the dominant PERF-07 risk in the morning audit.
- (Resilience & Fault Tolerance) S3SigV4 client (app/Support/S3SigV4.php) has an explicit, deliberate 30s timeout on every putObject/headObject/get call, and get() distinguishes not-there (404) from could-not-read (throws) so callers can never confuse the two.
- (Resilience & Fault Tolerance) config/mail.php:48-51 sets an explicit 10s SMTP timeout with a code comment reasoning through exactly the RES-01 cascading-hang scenario (QUEUE_CONNECTION=sync pinning FPM workers) - resilience reasoning is visibly baked into the code, not incidental.
- (Resilience & Fault Tolerance) Registration/email-verification mail sends (RegisterController, EmailVerificationController) are try/caught with a friendly, freely-retryable error and deliberately send before arming any cooldown/state, so a transient SMTP failure never locks a user out of retrying.
- (Resilience & Fault Tolerance) GenerateMonthlyReport.php isolates each recipient's send in its own try/catch so one bad address or a relay hiccup cannot silently abort the whole monthly distribution - with an explicit comment naming this as a QUEUE_CONNECTION=sync-specific risk.
- (Resilience & Fault Tolerance) DashboardCache.php implements textbook single-flight/dogpile protection: a cache lock with block(), jittered TTL, and a re-read-before-recompute fallback on lock-wait timeout to avoid a stampede-on-timeout.
- (Resilience & Fault Tolerance) Every scheduled command in routes/console.php uses withoutOverlapping() except the intentionally lock-free heartbeat (with a code comment explaining why); audit:ship is genuinely idempotent via a high-water-mark column that only advances after a verified upload.
- (Resilience & Fault Tolerance) Registry/export code paths are uniformly bounded: paginate(20) for browsing, chunk(500) for CSV/XLSX export, limit(50) for the ICD-10 typeahead - no unbounded get-all query on a growth table was found anywhere in app/Http/Controllers.
- (Resilience & Fault Tolerance) The project's own deploy runbook (docs/DEPLOY-LARAVEL.md section 10) is unusually candid about its own open resilience gap (no drain step on container swap), naming it explicitly as RES-12, open and specifying the exact fix - genuine self-awareness rather than a blind spot discovered by this audit.
- (Data, Backups, DR & Correctness) Point-in-time recovery is now a real, tested, production-verified capability, not a plan: scripts/backup/binlog-ship.py ships MySQL binary log off-box hourly with the same encryption as the nightly dump, backed by 72 new unit tests (100 total with test_db_backup.py, all passing, re-run locally in 22.8s) that are a BLOCKING CI step, and per the orchestrator documented facts it is installed and running on the production host with a verified --restore-check (25,900 events, roughly a 17h window). This closes DATA-04, previously a real gap (RPO capped at 24h).
- (Data, Backups, DR & Correctness) Nightly encrypted (AES-256-CBC/PBKDF2, escrowed key) off-box backup automation with a heartbeat object and a daily in-app staleness alert (backup:verify, 06:30), extended by PR #19 to also watch the hourly binlog heartbeat and alert on staleness or per-file shipping failures -- a genuinely more complete monitoring story than the prior audit found, even though the extended alerting has not yet reached the running production container (a pending deploy, not a code gap).
- (Data, Backups, DR & Correctness) Two real, measured restore drills have now been executed on production data and logged with row-count reconciliation against the live DB (BACKUP-AND-RESTORE.md S8), plus a real production app-rollback rehearsal (DEPLOY-LARAVEL.md S4.4: 50s/121s, smoke green both times) -- recovery paths are exercised, not assumed.
- (Data, Backups, DR & Correctness) All 47 migrations are reversible (down()) except a small, explicitly documented set of data-fix migrations, and the team enforces an additive-only, expand-never-contract convention with a mandatory pre-deploy dump as the real DB rollback path -- now demonstrated end-to-end by the rollback rehearsal above.
- (Data, Backups, DR & Correctness) Clinical narrative columns are encrypted at rest at the application layer (AES-256-CBC+HMAC under APP_KEY) with a well-reasoned scope decision, an idempotent and reversible data migration, and a documented key-rotation procedure.
- (Data, Backups, DR & Correctness) Strong DB-level invariant enforcement: unique constraints on MRN, email and admission-diagnoses, CHECK constraints for date-ordering and age range with a self-healing pre-migration cleanup step, pervasive use of DB transactions, and a hash-chained audit log written under a row lock.
- (Data, Backups, DR & Correctness) The bulk CSV import (ImportController) is a genuine two-step preview-then-commit pipeline: every row is validated, invalid rows are rejected with a specific reason and never written, and the commit runs in one transaction.
- (Data, Backups, DR & Correctness) DashboardCache documents and implements a deliberate read-your-writes consistency decision directly in code comments: the cache busts on every clinically-relevant write so the user who just wrote data never sees it appear to have been lost.
- (Config, Secrets & Environments) laravel/.env.example is exceptionally thorough at 194 lines; diffing every env() key referenced under app/ and config/ against it turned up exactly one gap, MAIL_TIMEOUT; nearly every optional var is documented with a safe placeholder or a clear comment.
- (Config, Secrets & Environments) A live gitleaks detect run over the full 489-commit history found zero real secrets in the audited laravel/ tree, corroborating the claim that history was purged of the three historically leaked credentials; the only two raw hits were false positives in the unrelated retired legacy app.
- (Config, Secrets & Environments) The client bundle never reads import.meta.env beyond the app name; no path for a secret to leak into shipped JS.
- (Config, Secrets & Environments) docs/compliance/INCIDENT-RESPONSE.md section 7.4 is a concrete, stack-specific emergency-rotation runbook naming exact commands and post-rotation checks for every long-lived credential, including the easy-to-miss APP_KEY re-encryption trap for the encrypted narrative and MFA-secret columns.
- (Config, Secrets & Environments) Supply-chain defaults are solid for the project size: npm install-scripts disabled, composer allow-plugins is an explicit allowlist rather than a wildcard, package.json is private, and CI enforces locked installs against committed lockfiles.
- (Config, Secrets & Environments) APP_DEBUG defaults to false in code, no source maps ship in public/build, and config is granular per-variable throughout with no monolithic feature-flag blob.
- (Compliance, Privacy & Legal) The ROPA/DATA-RETENTION/DPIA rework merged this session (main at 912e11c, PR #18, commits c1e1e5e, 2977ac7, 92d5f08) is a genuine, verified improvement in accuracy, not just volume: every activity in ROPA.md now carries a System(s) today / where row and a Controller / processor row, a new activity A10 documents the parallel migration copy honestly, and DATA-RETENTION.md new section 2.0 names all seven known copies of the dataset (C1-C7) with a per-copy destruction-mechanism column instead of one vague line.
- (Compliance, Privacy & Legal) DPIA.md three new risks (R13 un-hardened legacy daily build, R14 clinical record hosted outside the Kingdom, R15 duplicate dataset during the parallel period) are scored 25/20/12 and explicitly say cutover is the only thing that closes them; the document does not soften or hide the highest live risk to make the score look better.
- (Compliance, Privacy & Legal) CONFIRMED-FACTS C12/C13 were correctly reconciled to VERIFIED status against the actually-shipped code (export audit rows in StatisticsController, AuditController, ReportsController; SECRET-/CONFIDENTIAL- filename prefixes; PDF classification footers) rather than left as stale GAP entries, and every dependent cross-reference (ROPA A4/A6, DATA-CLASSIFICATION section 6/7, EVIDENCE-PACK G1/G2) was updated to match in the same pass.
- (Compliance, Privacy & Legal) OPEN-ITEMS.md was regenerated from a documented, self-validated scan rule (it reproduced the prior 529-row catalogue byte-for-byte before trusting the new scan) rather than hand-edited, and grew to 575 markers honestly reflecting the larger reworked files rather than being left to drift.
- (Compliance, Privacy & Legal) Dependency licensing remains clean across both ecosystems on a fresh re-parse: zero AGPL/GPL/SSPL/unknown-license findings; the only weak-copyleft entries (dompdf LGPL family) are used compatibly with closed-source SaaS/internal distribution.
- (Cost, FinOps & Sustainability) Every guest-reachable, cost-relevant surface (login, registration incl. the email-send step, MFA challenge, step-up, email-verify, CSP report sink, /health, security.txt) carries an explicit named RateLimiter or inline throttle (app/Providers/AppServiceProvider.php:29-75), including a session-independent, IP-keyed cap added specifically to close a cookie-drop bypass on the registration email-send endpoint that would otherwise be an unbounded outbound-mail relay.
- (Cost, FinOps & Sustainability) Fixed single-host architecture (Coolify + Nixpacks on one OCI Ampere instance; no autoscaler, no serverless, no per-token model API -- LLM/AI is None per detection) means there is no consumption-scaled billing surface for an attacker or a retry storm to inflate in the first place; the worst case of an abused export/report path is an availability incident on a flat-price box, not a runaway invoice.
- (Cost, FinOps & Sustainability) Real lifecycle/TTL discipline already exists for the storage that does grow over time: local backup pruning at 2 days (scripts/backup/db-backup.py), an off-box bucket lifecycle rule at 90 days (docs/BACKUP-AND-RESTORE.md section 6), and a defensive, non-zero-guarded audit-log retention/prune command (app/Console/Commands/AuditPrune.php) -- nothing here grows storage cost unbounded by default.
- (Cost, FinOps & Sustainability) The dashboard heaviest aggregate queries are cached with a jittered TTL, write-triggered invalidation, and a single-flight lock so a stampede of ward workstations does not multiply the same recompute (app/Support/DashboardCache.php) -- a genuine compute/energy-cutting pattern, not just a rightsizing afterthought.
- (Cost, FinOps & Sustainability) No duplicate non-prod environment running 24/7 to waste spend on: the project deliberately runs a single Laravel environment (CLAUDE.md section 1), so there is no forgotten staging box burning money in the background.
- (Cost, FinOps & Sustainability) All exports, reports and the registry search -- the only endpoints that could otherwise become a compute/egress cost sink -- are admin-only, authenticated and MFA-gated (routes/web.php:208-266), and the registry export streams in 500-row chunks rather than materializing unbounded result sets.
- (Documentation & Runbooks) laravel/README.md remains exemplary: purpose, per-module feature list, stack table, working clone-to-running quick-start, operational endpoints, full documentation index, Security posture section, every referenced command verified against package.json/phpunit.xml.
- (Documentation & Runbooks) laravel/docs/adr/ shipped since the last pass (PR #17, commit 9aa049f): a README index plus ten well-structured MADR records (0001-0010), every one Accepted, each with Context/Decision/Consequences/References sourced only from what the repo actually records (migrations, commits, other docs); the record explicitly says not recorded rather than guessing where the trail is silent. Linked from both CLAUDE.md and HANDOFF.md. This closes what was the most defensible fail in the prior report.
- (Documentation & Runbooks) DOC-07 partially closed (PR #14, commit a1a64b6): .github/CODEOWNERS now names the accountable maintainer with an explanatory comment, and laravel/SECURITY.md plus the identical root SECURITY.md replaced the literal SET-THIS placeholder with a real pointer to the RFC 9116 security.txt contact, sourced from SECURITY_CONTACT and served at /.well-known/security.txt.
- (Documentation & Runbooks) docs/DATABASE-AND-BEHAVIOR.md was fully resynced (PR #16, commit 0a392ab) with the consultation ledger, encryption, export-audit rows, dashboard cache and middleware chain that shipped across the 2026-08-21/22 and 2026-09-03 work; it now carries an explicit freshness note naming the last migration it was checked against, closing the concrete drift the prior audit found.
- (Documentation & Runbooks) Operational runbook depth continues to be unusually strong for a project this size: the DEPLOY-LARAVEL.md rollback procedure and the BACKUP-AND-RESTORE.md restore drill both now have a second, dated real-world rehearsal recorded (PR #13, commit 02314b9), and docs/compliance/INCIDENT-RESPONSE.md has concrete, stack-specific containment playbooks.
- (Operational Readiness & Incidents) docs/compliance/INCIDENT-RESPONSE.md is an unusually thorough, stack-specific incident runbook: SEV1-SEV4 matrix with concrete examples drawn from this system's actual audit events, defined roles, a first-hour checklist, real containment playbooks (session revocation, secret rotation, audit-chain-break triage), a regulatory-notification decision tree, and a blameless-postmortem template with an owner/due-date/tracking-link action-item table.
- (Operational Readiness & Incidents) RELEASE-CHECKLIST.md is a genuine go/no-go release gate exercised on every deploy (not a one-time launch review): CI gates, mandatory pre-deploy backup, deploy watch, smoke test, audit-chain and scheduler verification, and explicit rollback triggers with a named rollback-owner field.
- (Operational Readiness & Incidents) The rollback mechanism is well designed (Coolify image-based app-only rollback vs. an explicit DB-restore procedure for data-changing releases, with a hard, well-justified rule against migrate:rollback citing specific destructive migrations) and was genuinely rehearsed on production with strong measured results (50s app-only swap, 121s roll-forward, 7-8s scratch-DB restore) - verified via git history even though the recording commit is not yet merged to main.
- (Operational Readiness & Incidents) RFC 9116 security.txt is dynamically served with auto-computed Expires and a real default contact, backed by a substantive SECURITY.md VDP with concrete response-time SLAs and a safe-harbour clause.
- (Operational Readiness & Incidents) The legacy-to-Laravel replatform uses a governed cutover flag (settings.consultations_source_of_truth) with explicit ON/OFF semantics and destructive-action warnings - a real incremental-migration technique (parallel run plus flag-gated cutover) rather than a big-bang switch.
- (Visual QA & Accessibility) PR #15 (merged to main, verified in the tree) closed the one real defect the prior audit found: all 5 report/census pages that previously rendered a competing masthead h1 alongside AppLayout page-title h1 now render that masthead as h2, with an explicit inline comment citing UX-03 at each of the 5 sites - every page in the app now has exactly one h1.
- (Visual QA & Accessibility) A shared focus-management composable (resources/js/composables/useModalA11y.js) gives every modal focus-on-open, Tab/Shift-Tab wrap trapping, and focus-restore-on-close, plus a skip-to-content link at the top of AppLayout.vue.
- (Visual QA & Accessibility) scripts/contrast.mjs is a rigorous, CI-blocking WCAG AA contrast plus CIEDE2000 perceptual-distance gate, self-tested against a published reference dataset before it is trusted.
- (Visual QA & Accessibility) The CI YAML shows ESLint (eslint-plugin-vue) and vitest-axe both running as their own named, ordered, blocking steps in the required frontend job, ahead of the general Vitest run, with the workflow triggering on every push/PR (no path filter) so an a11y regression cannot slip past an unrelated-looking diff.
- (Visual QA & Accessibility) prefers-reduced-motion is honored in both CSS and a reactive composable that gates Chart.js animation; prefers-color-scheme is honored with a persisted, no-flash theme bootstrap; zero positive tabindex and zero unlabeled form controls or icon buttons found repo-wide.
- (i18n, l10n & Text) MySQL connection is utf8mb4/utf8mb4_unicode_ci end to end (app and legacy connections); the unused stock pgsql/sqlsrv/mariadb boilerplate in config/database.php still shows Laravel defaults but nothing wires them up.
- (i18n, l10n & Text) Today/day-boundary logic is deliberately bound from the PHP app-timezone clock (Carbon::today, now().toDateString) rather than MySQL CURDATE, with explicit I18N-02-tagged comments across DashboardController, DataQualityController, ConsultationDashboardController and RegistryController, a real, already-shipped fix for the DB-host-is-UTC / app-is-Asia/Riyadh day-boundary bug documented in CLAUDE.md.
- (i18n, l10n & Text) The one bilingual surface (privacy notice) is well engineered: dir=rtl and lang attributes, logical CSS (ps-6, text-start) for mirroring, a committed EN/AR catalog, and a CI-enforced shape-parity test (tests/Feature/PrivacyNoticeTest.php) plus a Vitest+axe spec asserting dir=rtl rendering.
- (i18n, l10n & Text) legacy:import member-email dedup is explicitly tested against an accent-only duplicate to prove the importer ASCII-fold logic, not just a naive lower/trim, is needed to satisfy utf8mb4_unicode_ci accent-insensitive uniqueness (tests/Feature/LegacyImportTest.php:241-264).
- (i18n, l10n & Text) The login and username-reminder flows already use multibyte-safe mb_strtolower for identity case-folding, explicitly tagged I18N-05 in comments; the one gap found (rate-limiter key) is a narrow miss against the codebase own established convention, not a systemic pattern.

## Intake — could not verify from the repo
❓ [REL-01] Are user-facing SLIs (availability plus latency percentiles) defined and measured anywhere outside the repo for the production URL?  (Reliability, SLOs & Error Budgets)
❓ [REL-02] Is there an agreed SLO target and rolling window per SLI, and how was it chosen?  (Reliability, SLOs & Error Budgets)
❓ [REL-03] Is there an agreed error-budget policy for what happens when the currently-undefined error budget is exhausted?  (Reliability, SLOs & Error Budgets)
❓ [REL-07] Once SLOs exist, is there an intention to track them per critical clinical journey rather than only as a single site-wide signal?  (Reliability, SLOs & Error Budgets)
❓ [REL-08] Are the hard dependencies' own availability figures known and reconciled against any future reliability target, given the app runs on one host with no documented failover?  (Reliability, SLOs & Error Budgets)
❓ [OBS-01/OBS-08] Production logs land only on the container stderr today (no shipper/collector found in-repo; the detection summary confirms logs die with the container, no log sink yet). Where, if anywhere, are they captured before a redeploy discards the previous container, and what is the intended retention? Scored N/A here only because the deployment is single-instance.  (Observability)
❓ [OBS-03] Is an error-tracking SDK (Sentry/GlitchTip, ideally self-hosted or in-Kingdom to match the PDPL data-residency posture already applied to backups and the audit archive) planned before or after the dmc-im.com cutover, given the Laravel app already holds a live parallel copy of real PHI?  (Observability)
❓ [SEC-11] Is there a plan or documented risk-acceptance for the lack of a required second PR-approver on main, given this is a solo-operator repo shipping directly to a live PHI system? Verified via GitHub API: no required_pull_request_reviews rule exists.  (Security & App Hardening)
❓ [SEC-09/general] Has an external pentest or DAST assessment ever been run against the live app? Out of scope for this static audit; flagged as a standard follow-up per the category's runtime-hooks note.  (Security & App Hardening)
❓ No blocking intake items for this category. Non-blocking suggestion: neither PHP (PHPStan/Larastan) nor JS (TypeScript) has a static type checker configured (TST-05/TST-09 marked N/A); worth a low-priority backlog item to adopt one incrementally given this is a live PHI system.  (Testing & Code Quality)
❓ [CICD-04/CICD-11] Given this is a regulated PHI system, is a second human ever brought in to review high-risk PRs (auth, schema, clinical logic) even though the GitHub required-review setting is off for the solo maintainer, and is that accepted as a standing risk?  (CI/CD & Release Engineering)
❓ [CICD-12] Are deployment frequency, lead time, change-failure-rate and MTTR tracked anywhere, even informally from the release checklist and the Coolify deployment history, or only theoretically derivable? The release-tag convention documented in the checklist has still not been used in practice (git tag -l is empty).  (CI/CD & Release Engineering)
❓ [PERF-05] Has any critical path (patients board, admission, registry search/export, shift-change concurrent load) been load-tested to its knee point? What RPS/concurrent-user figure and p95/p99 latency was observed, if any?  (Performance & Scalability)
❓ [PERF-06] Is there a documented capacity/growth model (launch peak vs 6-month vs 10x) for the roughly 17k patient / 37k episode / 330 staff dataset on the single OCI host, and a named first scaling bottleneck (DB CPU, PHP-FPM workers, disk I/O)?  (Performance & Scalability)
❓ [PERF-10] Have OCI VM (vCPU/network) limits, MySQL max_connections, and the Cloudflare Free-plan request/rate-limit ceilings been inventoried against projected peak concurrent staff usage?  (Performance & Scalability)
❓ [Q] RES-11: Is there (or should there be) an operational vendor-outage runbook for Cloudflare/SiteGround-SMTP/OCI beyond the existing PDPL-transfer vendor list, given SMTP relay failures already degrade gracefully but Cloudflare/OCI outages have no documented app-level fallback?  (Resilience & Fault Tolerance)
❓ [Q] RES-13: Is a scoped fault-injection GameDay (for example, block outbound SMTP/S3 in a test window) planned, or are the existing DB-restore and rollback drills considered the resilience-testing baseline at this scale?  (Resilience & Fault Tolerance)
❓ [Q] RES-09: Is a full host-loss recovery runbook (new OCI instance, redeploy, restore, re-point DNS) planned/drilled, or is the current single-host design an accepted, cost-driven risk for this hospital-unit scale?  (Resilience & Fault Tolerance)
❓ [DATA-02] Given the PDPL in-Kingdom residency constraint, is a second in-Kingdom OCI region or a separate administrative compartment/tenancy for the backup bucket (now also holding the binlog archive) feasible, and has the residual same-region correlated-loss risk been explicitly risk-accepted by the owner?  (Data, Backups, DR & Correctness)
❓ [DATA-04] The binlog-aware backup:verify alerting (checkBinlogHeartbeat) is merged to main but, per the orchestrator own note, the currently-running production container predates that deploy -- please confirm the deploy date so an operator knows the in-app stale-shipper alert is genuinely live and not just installed in code.  (Data, Backups, DR & Correctness)
❓ [DATA-12] Is there a BIA behind the now-measured RTO/RPO figures, who else besides the sole GitHub collaborator can deploy or access the backup-encryption key, and is there a written crisis-communication plan?  (Data, Backups, DR & Correctness)
❓ [CFG-03] Is the OCI IAM customer-secret key pair used for AUDIT_S3_ACCESS_KEY/SECRET scoped to only the two buckets it needs, dmc-audit-log and dmc-db-backups, or does it carry broader OCI account access? The Coolify env-var store is confirmed as the injection mechanism; per-workload least-privilege scoping is not verifiable from the repo.  (Config, Secrets & Environments)
❓ [CFG-04] Is a periodic or scheduled rotation cadence planned for long-lived credentials such as DB password, AUDIT_S3 keys and SSH keys, beyond the existing emergency-only runbook, or is emergency-only rotation an accepted risk? Separately, were the legacy DB password and the info@dmc-im.com SMTP password, found in a now fully purged historical commit, actually rotated at the provider or DB end?  (Config, Secrets & Environments)
❓ [CMP-01] What is the final, counsel-confirmed retention period for clinical rows (patients, admissions, consultations, handovers), and does it differ between the Laravel copy and the legacy daily record per DATA-RETENTION.md section 6.4?  (Compliance, Privacy & Legal)
❓ [CMP-02] Now that the copy inventory is explicit (C1-C7), who owns and dates the destruction of each, in particular the owner-workstation legacy dumps and the reported but not yet documented OCI-host file shredding, and is the DPO.md R6 HIM-office DSAR workflow actually operative today independent of DPO appointment?  (Compliance, Privacy & Legal)
❓ [CMP-03] What is the target cutover date (DPIA action 0a) that the document itself identifies as the single measure closing R13/R14/R15, and what is the target date for signing the controller-processor contract (G12) and the OCI/Cloudflare/SMTP-relay DPAs?  (Compliance, Privacy & Legal)
❓ [CMP-06] Does counsel confirm the PDPL DPO-appointment trigger applies here, and if so, who will be designated and by when?  (Compliance, Privacy & Legal)
❓ [COST-01] Is cost-allocation tagging/labelling enforced on the OCI/Coolify/Cloudflare/S3-bucket resources at the console level, since there is no IaC in this repo to check?  (Cost, FinOps & Sustainability)
❓ [COST-02] Is a billing budget with an alert threshold configured for the OCI account, and is any per-patient/per-episode unit-cost metric tracked?  (Cost, FinOps & Sustainability)
❓ [COST-03] Is OCI cost-anomaly detection (or an equivalent) enabled and routed to an on-call/owned channel?  (Cost, FinOps & Sustainability)
❓ [COST-04] Is the single OCI Ampere instance on a committed-use/reserved/always-free rate, or is on-demand an intentional, reviewed decision?  (Cost, FinOps & Sustainability)
❓ [COST-06] Who owns hosting-cost review for this system and at what cadence; has a crawl/walk/run FinOps maturity stage ever been named?  (Cost, FinOps & Sustainability)
❓ [DOC-07] The remaining incident-response contact-tree rows (DPO/legal, Clinical owner, Executive sponsor, Communications) in docs/compliance/INCIDENT-RESPONSE.md Appendix A are still placeholders, pending organisational appointments HANDOFF.md marks as parked by the owner. When will these be assigned, and should the Technical Lead row at least be filled now since it maps to the same person already named in CODEOWNERS?  (Documentation & Runbooks)
❓ [DOC-09] No CI-enforced doc-freshness or link-check exists (no markdownlint/lychee/doc-build step in laravel-ci.yml) despite two manual staleness fixes this cycle (DATABASE-AND-BEHAVIOR.md, resolved in this audit). Is a lightweight automated check or a periodic review cadence worth adding so drift is caught without a full audit pass?  (Documentation & Runbooks)
❓ [OPS-07] Beyond the now-automated backup/restore drill, is manual operational toil (secret rotation, deploy verification, legacy data reloads - all heavily manual per DEPLOY-LARAVEL.md) tracked anywhere, and is there a plan to automate the highest-frequency manual steps?  (Operational Readiness & Incidents)
❓ [OPS-06] Will the ops/rehearsal-records branch (commit 02314b9, real rollback and restore-drill measurements) be merged into main, and will these rehearsals repeat on a schedule rather than as a one-off?  (Operational Readiness & Incidents)
❓ [OPS-12] Is security@<hospital domain> (SECURITY.md's literal placeholder) the intended final VDP contact, or should SECURITY.md be reconciled with security.txt's real default (mailto:info@dmc-im.com) before go-live?  (Operational Readiness & Incidents)
❓ [UX-01] Have the key clinical screens (patient board, admissions, registry, dashboard) been visually verified at 375px and 1440px with the lighthouse or preview runtime hook? Static analysis found no fixed-width overflow traps and confirmed the data-table min-width pattern is correctly wrapped in overflow-x-auto, but only a rendered scan can ground-truth reflow and tap-target sizing.  (Visual QA & Accessibility)
❓ [I18N-01] Is there a Unicode round-trip test that POSTs emoji, CJK and an RTL string through the real storage path (patient, handover, consultation text) and reads it back byte-identical? None found in tests/.  (i18n, l10n & Text)
❓ [I18N-06] Has the Privacy notice RTL rendering (and any future RTL surface) been verified with an actual rendered screenshot and with pseudo-localized/expanded text, beyond the computed dir/lang assertions in Legal/__tests__/Privacy.spec.js?  (i18n, l10n & Text)

## Waived (risk-accepted)
None — no .prod-ready/waivers.yml.
