PRODUCTION READINESS — DMC Internal Medicine patient-flow hub (laravel/)          Verdict: 🔴 BLOCKED (58/100)
Stack: Laravel 13 · PHP 8.3 · Inertia + Vue 3 · MySQL 8.4 · Coolify/Nixpacks on 1× OCI (me-riyadh-1) · Cloudflare Free · GitHub Actions
Run:   fresh · 15 categories audited · 3 N/A · 2026-09-03T15:11:44   |   Emphasis view (security · privacy/compliance · data · config · observability): 70/100

## Context and orchestrator notes (read before the numbers)

- **Scope.** This audit covers the Laravel application in `laravel/` only. It does not cover the unit's
  **current daily system**, the legacy PHP app at `www.dmc-im.com` on SiteGround (US), which a
  separate external header/TLS audit the same day graded **F** (no HSTS, `PHPSESSID` without
  Secure/HttpOnly/SameSite, canonical redirect through plaintext HTTP) — see
  `docs/compliance/evidence/sec-web-2026-09-03.md`. The Laravel app graded **A** in that audit.
  Whole-unit readiness is therefore bounded by the legacy system until cutover, whatever this card says.
- **Comparison with the prior run (2026-09-02T20:23:33, before the remediation shipped):** overall
  27 → **58**, emphasis view 37 → **70**, Critical findings 2 → **2** (different ones: the prior
  Criticals in Data/CI were closed by the encrypted off-box backups and the working, SHA-pinned CI;
  today's two are both CI/CD process gaps). Several prior findings were re-verified and reversed:
  denial-of-wallet (rate limiters present), ApexCharts licence (removed), CI "billing-blocked" (CI runs
  and gates; four green jobs), leaked-secret commit (history purge confirmed by a 474-commit gitleaks scan).
- **Weights.** Each category's *own* declared default weight was used (authoritative per `scoring.md`);
  where a category file differs from the summary table it wins: Reliability 9, Resilience 8, Testing 8,
  Performance 6, Compliance 6, Docs 4, i18n 3. Everything else matches the table.
- **Near-binary categories.** Reliability scores 0 on a *single* scorable Medium check (five checks are
  intake because no SLO practice exists to verify; two n/a); Cost scores 100 on three checks. Read the
  counts columns, not just the score, for those rows.
- **On the two Criticals (CI/CD).** `CICD-04`: verified directly — `main` has **no required status
  checks and no review requirement** (`gh api …/branches/main/protection`), so the four CI gates block
  nothing. The auditor's added premise that Coolify deploys on raw push rests on the *Auto Deploy*
  toggle, which the runbook leaves conditional and this audit did not read from Coolify; if deploys are
  operator-triggered after the release checklist, this reads as **High** rather than Critical. `CICD-08`:
  a documented, drilled rollback path exists (redeploy previous image; DB restore drill logged) but the
  smoke test and rollback are **manual**, not pipeline-automated — "no safe rollback" is not literally
  true, so this too is arguably High. Both are scored **verbatim** as the auditor returned them; the
  verdict is honest about process, and the fix for both is the same and cheap (Top fix 1).
- **Linked findings.** `SEC-04` (session revocation is a no-op) and `PERF-07` (single-container
  coupling) and the runbook's own "redeploy signs everyone out" gotcha are **one root cause**:
  `SESSION_DRIVER=file`. One env change closes all three (Top fix 2).
- **Runtime hooks** were not enabled; runtime checks were assessed as wiring only.
- **Saudi regulatory overlay.** Not re-run as a category; superseded by the sourced, counsel-facing
  `docs/compliance/PROPOSED-CITATIONS.md` and the control map in `docs/compliance/EVIDENCE-PACK.md`.


## Scorecard
| Category | W | Score | ● | Crit | High | Med | Low | Pass | N/A | Intake |
|---|--:|--:|:-:|--:|--:|--:|--:|--:|--:|--:|
| Security & App Hardening | 10 | 86 | 🟡 | 0 | 2 | 0 | 0 | 10 | 0 | 0 |
| Reliability, SLOs & Error Budgets | 9 | 0 | 🟡 | 0 | 0 | 1 | 0 | 0 | 2 | 5 |
| CI/CD & Release Engineering | 9 | 49 | 🔴 | 2 | 2 | 0 | 1 | 6 | 1 | 0 |
| Data, Backups, DR & Correctness | 9 | 73 | 🟡 | 0 | 2 | 3 | 0 | 8 | 1 | 0 |
| Observability | 8 | 43 | 🟡 | 0 | 4 | 0 | 0 | 2 | 5 | 0 |
| Resilience & Fault Tolerance | 8 | 50 | 🟡 | 0 | 1 | 3 | 0 | 3 | 3 | 3 |
| Testing & Code Quality | 8 | 57 | 🟡 | 0 | 2 | 2 | 0 | 6 | 1 | 0 |
| Config, Secrets & Environments | 8 | 76 | 🟡 | 0 | 1 | 2 | 0 | 6 | 2 | 1 |
| Operational Readiness & Incidents | 7 | 60 | 🟡 | 0 | 2 | 0 | 0 | 4 | 3 | 3 |
| Performance & Scalability | 6 | 25 | 🟡 | 0 | 1 | 4 | 0 | 1 | 1 | 3 |
| Compliance, Privacy & Legal | 6 | 68 | 🟡 | 0 | 0 | 2 | 0 | 3 | 2 | 1 |
| Visual QA & Accessibility | 5 | 79 | 🟡 | 0 | 1 | 1 | 0 | 8 | 0 | 0 |
| Documentation & Runbooks | 4 | 75 | 🟡 | 0 | 0 | 2 | 1 | 5 | 1 | 0 |
| Cost, FinOps & Sustainability | 4 | 100 | 🟢 | 0 | 0 | 0 | 0 | 3 | 2 | 5 |
| i18n, l10n & Text | 3 | 71 | 🟡 | 0 | 1 | 0 | 1 | 5 | 0 | 0 |

N/A: AI/LLM & Agentic Readiness (no model usage) · API & Contract Lifecycle (self-contained app, no versioned API or events) · Infrastructure, Containers & IaC (No Dockerfile/Containerfile, docker-compose file, Kubernetes/Helm manifest, IaC (Terraform/CloudFormation/Pulumi/Bicep/CDK/ARM), or platform-deploy config (vercel.json, netlify.toml, fly.toml, render.yaml, Procfile, app.yaml, railway.json, nomad files, nixpacks.toml) exists anywhere in the repository (verified by exhaustive filename search across the whole DMC repo excluding vendor and node_modules). The Laravel app deploys via Coolify v4 plus Nixpacks on a single OCI host (laravel/docs/DEPLOY-LARAVEL.md): Coolify auto-detects the build pack from composer.json and package.json, and all platform configuration -- build pack selection, base directory, domain, health-check path (/up), env vars and their build-time versus runtime split, and the deploy-time migrate --force step -- lives in the Coolify UI/API, not as a committed artifact. This is exactly the categorys own carve-out: an app deployed only via an opaque managed PaaS with no committed config. Every one of the 10 checks self-skips per the categorys own N/A logic (no Dockerfile means INFRA-01 is skipped; no k8s manifests means INFRA-02/03/04/05/07/09 are skipped; no IaC means INFRA-06 is skipped; no platform-deploy file means INFRA-08 is skipped; CI here builds no container or IaC artifact, only runs composer/npm install, PHPUnit, Vitest, gitleaks and Semgrep, so INFRA-10 is skipped too), so the whole category is N/A and excluded from the weighted score.)

## Findings
🔴 CRITICAL · [CICD-04] Pipeline gates: branch protection, required checks, in-CI scanning            confidence: high   (CI/CD & Release Engineering)
   Where:  gh api branches/main/protection returns no required_status_checks and no required_pull_request_reviews key (verified live); gh api rulesets returns empty array; DEPLOY-LARAVEL.md:79 notes Coolify Auto Deploy on push starts the build; docs/CI.md:13-21 acknowledges required-status-checks are not yet turned on
   Why:    The real scanning that runs in CI (gitleaks, Semgrep, composer-audit-gate, npm audit) has no technical hook into what reaches production. This is a direct-to-main, no-PR workflow, so required status checks provide little practical protection -- nothing stops a push to main from being built and deployed by the Coolify push-triggered webhook before or independent of the CI result. A red or vulnerable commit can reach the single production environment holding real PHI without any automated block.
   Fix:    Decouple the deploy trigger from the raw push event: disable Coolify push-triggered auto-deploy and instead call the Coolify deploy API only from a workflow_run job that fires after Laravel CI succeeds on main, so a red CI run can never result in a deploy. Also turn on the four Laravel CI job names as GitHub required status checks per the plan already stated in docs/CI.md.
   Auto-fixable: yes

🔴 CRITICAL · [CICD-08] Fully scripted, idempotent deploy with post-deploy smoke test + auto-rollback            confidence: high   (CI/CD & Release Engineering)
   Where:  DEPLOY-LARAVEL.md:98-113 section 3.3 (smoke test, audit-chain check, scheduler check and human check are each run manually by a person after watching the deploy log); DEPLOY-LARAVEL.md:116-187 section 4 (rollback is a human judgment call, triggered by hand, both rows of the section 4.4 rehearsal table unrun); RELEASE-CHECKLIST.md:54-57 section 6 and 71-85 section 9 (smoke test and rollback are checklist items a human ticks, not pipeline steps)
   Why:    Nothing in the actual deploy pipeline runs scripts/smoke.sh or decides to roll back. If the person deploying is unavailable, distracted, or an on-call handoff is unclear, a broken release carrying real PHI can stay live indefinitely with no automatic recovery -- the documented under-5-minute rollback target is unverified since both drills in section 4.4 are blank.
   Fix:    A working smoke-test script already exists (scripts/smoke.sh) -- wire it into an automated post-deploy step (a workflow_run or Coolify post-deploy webhook hitting /up and /health) that calls the Coolify rollback API on a non-2xx or FAIL line. At minimum, script the manual verification steps into one command, and run the two rehearsals in DEPLOY-LARAVEL.md section 4.4.
   Auto-fixable: yes

🟠 HIGH · [OBS-01] Structured logging to stdout/stderr            confidence: high   (Observability)
   Where:  laravel/config/logging.php:21,56-71; laravel/.env.example:27-31; laravel/docs/DEPLOY-LARAVEL.md:216,298
   Why:    Default channel is stack -> single (config/logging.php:21,56-64), a Monolog line-formatted file at storage_path(logs/laravel.log), and production is documented to run LOG_CHANNEL=daily (still file-based, DEPLOY-LARAVEL.md:216), not JSON to stdout/stderr. The project's own ops doc states that sessions and logs live in the container and that a redeploy discards the previous container's log files unless storage/ is mounted as a persistent volume (DEPLOY-LARAVEL.md:298) - on this single-instance, no-staging, frequently-redeployed host, every deploy silently discards the prior container's log history unless a volume happens to be mounted. Logs are also not JSON, so they are not directly machine-parseable in aggregation.
   Fix:    Set LOG_CHANNEL=stderr (or add a JSON formatter to the daily/single channel) so Monolog emits JSON lines to stdout/stderr per 12-factor; keep LOG_LEVEL configurable via env as today. Optionally mount storage/ as a persistent Coolify volume as a stopgap, but stdout-based shipping is the durable fix.
   Auto-fixable: yes

🟠 HIGH · [OBS-03] Error tracking wired with a real DSN            confidence: high   (Observability)
   Where:  laravel/composer.json:8-15 (no sentry/bugsnag/rollbar/honeybadger package); laravel/.env.example:37-39
   Why:    No error-tracking SDK is present anywhere in the stack (composer.json lists only dompdf/inertia-laravel/framework/tinker/openspout - zero APM or error-tracking packages; package.json has no frontend error tracker either). .env.example:37-39 documents SENTRY_DSN as a not-yet-done step (comment: composer require sentry/sentry-laravel, then set the DSN) and DEPLOY-LARAVEL.md:216 lists SENTRY_DSN as optional/inert while empty. Combined with OBS-01's file-based, ephemeral logging, unhandled exceptions in this PHI-holding clinical system have no aggregation, no alerting and no MTTR signal - they only exist in a per-container log file that a redeploy can discard.
   Fix:    Run composer require sentry/sentry-laravel (or an equivalent), publish its config, set SENTRY_DSN/environment/release from env to a real non-empty production value, and verify it captures a test exception before relying on it.
   Auto-fixable: yes

🟠 HIGH · [OBS-04] RED / golden-signal metrics as histograms            confidence: high   (Observability)
   Where:  missing: metrics client (no prom-client/prometheus/opentelemetry-metrics/micrometer/statsd package in laravel/composer.json or laravel/package.json)
   Why:    No request-rate, error-count or duration-histogram instrumentation exists anywhere in the app or its scheduled console commands (audit:ship, backup:verify, dq:notify, the monthly report job). Tail latency (p95/p99) and saturation (DB pool, disk, CPU/mem) are invisible; no SLO can be computed for the clinical board/registry/reports pages, and a slow-query or resource-exhaustion incident has no metric signal, only whatever an operator happens to notice manually.
   Fix:    Add a metrics client (e.g. an OpenTelemetry PHP SDK or a Prometheus PHP exporter) recording request duration as a histogram, an error counter labeled by outcome, and saturation gauges for the DB connection pool and disk; expose a scrape endpoint or push to a backend.
   Auto-fixable: no

🟠 HIGH · [OBS-05] Distributed tracing with W3C context propagation            confidence: high   (Observability)
   Where:  missing: tracing SDK (no opentelemetry/opentracing/jaeger/zipkin/ddtrace/elastic-apm package); outbound hops confirmed at laravel/app/Support/S3SigV4.php (audit shipping + backups to S3-compatible buckets), config/database.php (MySQL), and SMTP mail sends via RuntimeConfigServiceProvider-configured mail settings
   Why:    The app makes real outbound calls (MySQL, an SMTP relay on US hosting, S3-compatible buckets for audit shipping and backups) with zero tracing. A slow or failing dependency (e.g. the SMTP relay, or the audit-ship S3 call) cannot be isolated from application-side slowness; there is no span to show which hop an incident lives in.
   Fix:    Adopt an OpenTelemetry PHP SDK with auto-instrumentation (or manual spans) for the DB client, the SMTP send, and S3SigV4 HTTP calls; register a W3C traceparent propagator; set SpanKind and error status on failures.
   Auto-fixable: no

🟠 HIGH · [SEC-04] Authentication, sessions & access control            confidence: high   (Security & App Hardening)
   Where:  app/Http/Controllers/Auth/PasswordResetController.php:74, app/Http/Controllers/ControlController.php:376 (resetMfa), and app/Http/Controllers/ProfileController.php:146 each delete rows from the sessions database table keyed by user_id, to kill a user's other live sessions on password reset, admin MFA reset (the documented lost-or-compromised-device recovery path), and self password change. Production SESSION_DRIVER is file (docs/DEPLOY-LARAVEL.md:298 and .env.example:81), under which Laravel's file session driver never touches the sessions database table at all, so these three deletes are silent no-ops in production. Two of the three call sites carry a stale code comment asserting the opposite, that SESSION_DRIVER is database in production, which directly contradicts the deploy doc and .env.example and suggests a real regression: the driver appears to have been changed to file after this logic was written and never reconciled. Everything else in this check is strong: bcrypt hashing via Laravel's Hash facade with 12 rounds and no md5/sha1 anywhere in app/; session cookie flags correctly wired to env, with production values documented in CLAUDE.md section 9 as secure cookies enabled; CSRF enforced on every mutation except the one documented, justified, throttled csp-report sink; consistent server-side object-level authorization via User canManageAdmission and canManageConsultation plus FormRequest authorize on every risky write, all ten FormRequests checked carry a real gate, none a bare return true; mandatory TOTP MFA for every account including admins, with remember-me disabled specifically to close an MFA-bypass path.
   Why:    A session cookie stolen via a shared or unattended clinical workstation, or any other hijack vector, keeps authenticating the attacker even after the legitimate user resets their password, or an admin resets a lost-or-compromised MFA device on their behalf, defeating the exact recovery action the code comments say it performs, on a system holding real PHI.
   Fix:    Switch production SESSION_DRIVER to database (a sessions table and migration already exist) or redis, so the existing session-delete calls actually take effect; or, if file must be kept, add an explicit file-session sweep that enumerates storage/framework/sessions and removes entries belonging to the target user_id, and correct the stale in-code comments. Add a regression test: log in as one user on two sessions, reset the password on one, and assert the other session no longer authenticates.
   Auto-fixable: no

🟠 HIGH · [SEC-11] Multi-party authorization & tamper-evident audit on the control plane            confidence: high   (Security & App Hardening)
   Where:  laravel/docs/CI.md lines 13-21 state that the repository currently has only no-force-push and no-delete protection on main, with no required reviewers and no required status checks; even the four Laravel CI jobs are not yet wired as required checks. Root CLAUDE.md section 2 confirms this is the standing, owner-consented operating model, stating that main is the single Laravel dev branch and direct-to-main is the consented workflow. Coolify then builds and deploys straight from the main branch head to the single production environment holding real PHI, with no second-approver gate between a push and a live deploy. The in-app audit log, App/Support/Audit, hash-chained and shipped hourly, is a strong tamper-evident trail for in-application actions but does not cover the control plane itself, meaning who pushed what to main or who triggered a deploy is not captured.
   Why:    A single compromised or mistaken credential, such as the owner's GitHub account or a leaked deploy token, can ship arbitrary code straight to the one production environment serving real patient data, with no independent review gate and no dedicated audit trail of who authorized the deploy versus who authored the change.
   Fix:    Once GitHub Actions billing and CI are confirmed stable, turn on the four Laravel CI jobs as required status checks on main, under Settings, Branches, main, Require status checks to pass, per docs/CI.md's own stated next step. If the team grows beyond a single maintainer, add a required-reviewer rule for pushes and pull requests touching laravel; until then this is effectively a risk-accepted single-operator posture and should be recorded explicitly, for example as a waiver, rather than left implicit.
   Auto-fixable: no

🟠 HIGH · [TST-02] Coverage thresholds + critical-path coverage            confidence: high   (Testing & Code Quality)
   Where:  laravel/phpunit.xml (no coverage block or threshold); laravel/vitest.config.js (no coverage config); laravel/package.json (no coverage script, no @vitest/coverage-v8 devDependency); .github/workflows/laravel-ci.yml line 134 (coverage: none on the PHP setup step)
   Why:    No coverage percentage is measured or gated in CI for PHP or JS, so a change that quietly deletes or bypasses tests for a risky module (admission mutation, MFA, the audit hash-chain, encrypted-narrative casts) would not be caught by a coverage regression; the project currently relies entirely on manually maintained test breadth rather than an enforced floor. Critical-path breadth itself is good: AuthorizationTest, MandatoryMfaTest, StepUpTest, ClinicalFlowTest, PatientMergeTest, AuditHashChainTest, ClinicalNarrativeEncryptionTest and more all exist; it is the threshold/gate that is missing, not the tests.
   Fix:    Add a coverage driver (pcov or xdebug) plus a coverage flag or minimum-coverage check to the PHPUnit CI step, and add @vitest/coverage-v8 with a coverage.thresholds block to vitest.config.js; start with a realistic floor (for example 60 to 70 percent) and fail the build below it rather than chasing 100 percent.
   Auto-fixable: no

🟠 HIGH · [TST-04] Lint passes            confidence: high   (Testing & Code Quality)
   Where:  laravel/composer.json (laravel/pint ^1.27 is a dev dependency with no composer script and no CI step); .github/workflows/laravel-ci.yml (no lint, pint or phpcs step in any of the 4 jobs); no .eslintrc or eslint.config file anywhere under laravel/ and no eslint devDependency in laravel/package.json, so JS/Vue has no linter at all; verified by running php vendor/bin/pint --test locally read-only: it reports style violations across roughly 140 of about 150 scanned app, tests and database files, for example app/Http/Controllers/AdmissionsController.php, app/Models/User.php, app/Support/Audit.php, app/Console/Commands/LegacyImport.php
   Why:    Neither PHP nor the roughly 75-file Vue frontend is gated by a linter. Pint is installed but has evidently never been run as a gate, since it flags almost the entire tree, so inconsistent style and simple mistakes merge unchecked; there is no ESLint at all for JS/Vue, so obvious errors such as unused vars or undefined refs are not statically caught either.
   Fix:    Auto-fixable: run php vendor/bin/pint to apply the safe mechanical fixes, then add php vendor/bin/pint --test as a required CI step in the backend job. Add ESLint (flat config plus eslint-plugin-vue) with an npm run lint script wired into the frontend CI job. Consider Larastan or PHPStan for real static analysis beyond style.
   Auto-fixable: yes

🟠 HIGH · [CICD-05] Supply-chain provenance: SBOM, signed attestations, pinned actions/images            confidence: high   (CI/CD & Release Engineering)
   Where:  No syft, cyclonedx, SPDX, cosign, Sigstore or SLSA-generator step anywhere in the workflow file. Positive: every third-party uses line IS pinned to a full commit SHA (lines 58,60,130,182,188,212,234) and the Semgrep image is pinned by manifest digest (line 210).
   Why:    No SBOM and no signed build provenance exist for the image Coolify actually deploys. If an upstream dependency is compromised between the audited commit and the live container, or the container composition is disputed during an incident or a compliance review, there is nothing to check it against.
   Fix:    Add an SBOM-generation step producing a CycloneDX or SPDX report per CI run, archived against the commit SHA. Longer-term, add cosign/Sigstore keyless signing of the built image.
   Auto-fixable: yes

🟠 HIGH · [CICD-11] Privileged release/control-plane actions require multi-party authorization            confidence: high   (CI/CD & Release Engineering)
   Where:  gh api branches/main/protection shows no required reviewers; gh api environments returns total_count 0 (no protected deployment environment for the Coolify prod deploy trigger); no CODEOWNERS file found anywhere in the repo
   Why:    A single compromised or mistaken credential (including the Coolify API token, which DEPLOY-LARAVEL.md:304 notes has gone stale once) can push to main, trigger a production deploy, or alter pipeline or workflow config with no second person review -- for a system holding real PHI for roughly 17k patients, this is a real single-point-of-compromise risk. Sized honestly: this is currently a one-person team (git log shows a single committer), so a second reviewer does not yet organizationally exist -- noted to size the risk correctly, not to excuse it.
   Fix:    Add a CODEOWNERS entry for .github/workflows, DEPLOY-LARAVEL.md and any future pipeline or IaC-as-code, so the day a second trusted person joins, review is structurally required. When that second party exists, turn on a GitHub protected deployment environment with required reviewers for the prod deploy trigger. Until then, track this explicitly as an accepted single-operator risk rather than a silent gap.
   Auto-fixable: no

🟠 HIGH · [PERF-07] Horizontal scaling, statelessness and caching limits            confidence: high   (Performance & Scalability)
   Where:  laravel/.env.example:81 SESSION_DRIVER=file; laravel/.env.example:111 CACHE_STORE=file (overriding Laravel own config/session.php:21 and config/cache.php:18 defaults of database); laravel/app/Support/DashboardCache.php (the dashboard heavy-tier cache rides this same file store)
   Why:    Sessions and the dashboard cache live on the single container local disk. This is not an active incident today (CLAUDE.md and the Detection Summary confirm one OCI host, no autoscaling configured), but it is a structural blocker: adding a second app replica for redundancy or scale-out would silently desync sessions (random logouts or CSRF mismatches on requests that land on the other replica) and serve a stale or inconsistent dashboard cache per replica, with no error to signal why.
   Fix:    Switch SESSION_DRIVER and CACHE_STORE to database (the Laravel framework default already coded in config/session.php and config/cache.php) or Redis before any horizontal scale-out; the sessions and cache tables already exist via the stock 0001_01_01_000000/0001_01_01_000001 migrations, so this is a pure env change with no schema work.
   Auto-fixable: yes

🟠 HIGH · [RES-01] Timeouts on every integration point            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/config/mail.php:48
   Why:    The SMTP mailer's timeout is left at the Laravel default null, so every outbound mail send falls back to PHP's ini default_socket_timeout (commonly ~60s) rather than an explicit, tuned bound. Because QUEUE_CONNECTION=sync, every Mail::send()/queue() call (registration OTP, password reset, forgot-username, audit-integrity alert email, monthly report) runs synchronously inside the HTTP request or console command that triggered it. A slow/unresponsive SMTP relay (the relay is SiteGround in the US, per CONFIRMED-FACTS.md B6) can hold a PHP-FPM worker for up to that default window per request; a burst of registration/reset traffic during a relay brownout can exhaust the worker pool and hang the whole app for unrelated users. By contrast, the S3-compatible client in app/Support/S3SigV4.php DOES set an explicit ->timeout(30) on every call, good practice that was not carried over to mail.
   Fix:    Set an explicit finite MAIL_TIMEOUT (e.g. 5-10s) and wire it into config/mail.php's smtp mailer (timeout => env(MAIL_TIMEOUT, 10)), or set it at runtime alongside the other mail.* overrides in RuntimeConfigServiceProvider. Longer term, moving mail sends off the sync request path (a real queue worker) would remove the request-blocking risk entirely, but the timeout alone closes the worker-exhaustion risk.
   Auto-fixable: yes

🟠 HIGH · [DATA-02] Backups automated, encrypted, and stored off-region            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/BACKUP-AND-RESTORE.md:76-93 (section 2.3 config: S3_REGION=me-riyadh-1, same endpoint as AUDIT_S3_*); laravel/config/services.php:41,54; laravel/.env.example:149-157
   Why:    Backups ARE automated (nightly cron), encrypted (AES-256-CBC/PBKDF2, key escrowed) and genuinely off-box (a separate OCI Object Storage bucket, not the DB host disk) - this closes the prior real risk of plaintext dumps sitting next to the database. But the backup bucket is in the SAME OCI region (me-riyadh-1) and the SAME tenancy/credentials family as production, in a DIFFERENT bucket only. A region-wide OCI outage or a compromise/suspension at the tenancy level could still take out primary and backup together - real but bounded correlated-loss risk. Downgraded from the category default Critical to High because: (a) the most likely loss vectors (disk failure, host compromise, bad migration) are genuinely mitigated by the off-box design; (b) Saudi PDPL mandates in-Kingdom residency, which constrains true cross-region replication as an option - this is a deliberate, documented regulatory trade-off, not an oversight.
   Fix:    Segregate the backup bucket into a separate OCI compartment/tenancy with independent IAM credentials not derived from the same root/administrative account as production, as defense-in-depth against tenancy-level compromise. Confirm with OCI whether a second Kingdom-resident region exists and evaluate a second in-Kingdom copy under separate administrative control. Document the residual correlated-loss risk and get an explicit owner risk-acceptance given the PDPL residency constraint.
   Auto-fixable: no

🟠 HIGH · [DATA-04] Point-in-time recovery and DR drift detection            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/BACKUP-AND-RESTORE.md:28-29
   Why:    No PITR/binlog shipping exists - recovery granularity is capped at the previous night 02:15 dump, so up to ~24h of admissions, discharges, transfers and consultations can be permanently lost in a worst-case incident (explicitly acknowledged in the doc). Clinicians would have to re-enter that window from paper/HIS records. Separately, there is no IaC for the DR/backup infrastructure (Coolify plus hand-run host cron/scripts) and therefore no automated drift detection between what is documented and what is actually configured on the host.
   Fix:    Enable MySQL binary logging with off-box shipping (or a managed point-in-time-recovery mechanism) if the hospital actual RPO tolerance is tighter than 24h (confirm via the DATA-12 BIA intake first). If binlog shipping is out of scope, increase backup frequency and/or document DR-infra-as-code with a scheduled drift check so the host configuration cannot silently diverge from the runbook.
   Auto-fixable: no

🟠 HIGH · [CFG-10] Cloud & service-to-service auth via short-lived federated identity            confidence: medium   (Config, Secrets & Environments)
   Where:  laravel/app/Support/S3SigV4.php (static AWS-SigV4 signing using AUDIT_S3_ACCESS_KEY/AUDIT_S3_SECRET); laravel/config/services.php:41-57 (the db_backup bucket reads with the SAME long-lived key pair as the audit_archive bucket)
   Why:    audit:ship, backup:verify and any future S3-compatible call authenticate with a static, indefinitely-valid OCI IAM customer-secret-key pair rather than a short-lived/federated identity. If the key pair leaks (env dump, container compromise), the attacker gets standing read/write on BOTH the audit archive and the backup-verification bucket until someone manually rotates it - no auto-expiry limits the window. Mitigating: CI itself uses no static cloud keys (no deploy step); the hourly audit NDJSON copies are stated to be WORM/retention-locked so this key alone cannot delete them (docs/compliance/INCIDENT-RESPONSE.md:197); OCI S3-Compatibility API itself requires a customer secret key rather than instance-principal auth, which is a real platform constraint, not pure oversight; a rotation runbook exists (INCIDENT-RESPONSE.md section 7.4).
   Fix:    Either migrate audit/backup object-storage access to OCI native REST API plus Instance Principal (the app already runs on an OCI compute instance, so no long-lived credential would be needed), or, if the S3-compatible endpoint must stay, put AUDIT_S3_ACCESS_KEY/SECRET on a documented rotation cadence (e.g. 90 days) and split it into two separately-scoped keys (audit vs. backup) so a leak of one does not expose the other.
   Auto-fixable: no

🟠 HIGH · [OPS-02] On-call rotation staffed, sustainable, with escalation            confidence: high   (Operational Readiness & Incidents)
   Where:  laravel/docs/compliance/INCIDENT-RESPONSE.md:36-48 (Roles table - every holder is [PLACEHOLDER]); Appendix A:418-433 (contact tree, every cell blank); corroborated by laravel/docs/compliance/EVIDENCE-PACK.md:126 (G9: named roles not designated)
   Why:    No named, staffed responder or 2-tier escalation path exists for this live-PHI system. If an incident happens today it depends on one person (the developer/operator) being reachable - a bus-factor-1 single point of failure with no documented backup, and the incident runbook's own severity-based paging table (section 2) has no one to actually page.
   Fix:    Name primary and deputy holders for every role in INCIDENT-RESPONSE.md section 3 and Appendix A (incident lead, technical lead, DPO/legal, clinical owner). At minimum stand up a 2-person escalation path (e.g. a shared phone/chat alert that reaches two people) even without a formal pager tool, and keep it current.
   Auto-fixable: no

🟠 HIGH · [OPS-06] Progressive delivery with a fast, practiced rollback / kill-switch            confidence: high   (Operational Readiness & Incidents)
   Where:  laravel/docs/DEPLOY-LARAVEL.md:180-187 (section 4.4 rehearsal table - both the app-only Coolify rollback drill and the scratch-DB restore drill are marked as not yet done); laravel/docs/RELEASE-CHECKLIST.md:85 (last rollback drill date / measured time left blank)
   Why:    The rollback mechanism itself is real and specific (immutable per-commit-SHA Coolify images make app-only rollback a genuine ~2-3 min swap; DB rollback is a documented dump-restore, correctly never migrate:rollback - section 4.3), which is why this is High rather than Critical. But it has never been exercised end-to-end, so the 5-minute target and the DB-restore path are unverified under real conditions - a bad release could take materially longer to revert than assumed, at exactly the moment speed matters.
   Fix:    Run the two drills already scaffolded in DEPLOY-LARAVEL.md section 4.4 (app-only Coolify Rollback + smoke.sh; restore a current-size dump into the scratch DB + audit:verify) at least once, record the measured times in the table, and repeat periodically (e.g. quarterly or after significant deploy-pipeline changes).
   Auto-fixable: no

🟠 HIGH · [UX-04] Accessible names: alt text, form labels, control labels            confidence: high   (Visual QA & Accessibility)
   Where:  resources/js/Pages/Auth/Login.vue:61-71 (Username/email + Password - the sign-in page); Auth/ForgotPassword.vue:21-22; Auth/ForgotUsername.vue:21-22; Auth/ResetPassword.vue:27-29; Admissions/Create.vue:41-101 (10 fields: MRN, Full name, Age, Gender, Nationality, Admit date, Admitted from, Location, Bed, Consultant); Profile/Edit.vue:59-60,78-80 (5 fields); Audit/Index.vue:98-99; Registry/Index.vue:263
   Why:    In each of these the label element is a plain sibling of its input/select, with no for/id pair and no wrapping, so the control has no programmatic accessible name. A screen-reader user tabbing into the username or password field on the login page, or any Admissions/Create field, or the password-change form, hears only edit text with no field name - the app own front door and its core admission workflow are effectively unusable non-visually. This is a regression against the codebase own established pattern: PatientForm.vue and the Consultations/Index.vue edit forms already do this correctly via a shared unique-id helper plus aria-describedby.
   Fix:    Add matching id/for pairs (or wrap each input/select inside its label) on every field listed; reuse the fid unique-id helper and aria-describedby wiring already implemented in resources/js/Components/PatientForm.vue as the house pattern.
   Auto-fixable: yes

🟠 HIGH · [I18N-02] Timestamps in UTC; future events keep wall-clock + IANA zone            confidence: high   (i18n, l10n & Text)
   Where:  laravel/app/Http/Controllers/DataQualityController.php:42,61; laravel/app/Http/Controllers/DashboardController.php:68,248,251,293-294; laravel/app/Http/Controllers/RegistryController.php:223 (raw CURDATE()); laravel/tests/TestCase.php:22-26 (fix applied to tests only); laravel/app/Http/Controllers/ConsultationDashboardController.php:206-268 (developer comments confirming the exact bug)
   Why:    config/database.php pins no MySQL session time_zone, so raw CURDATE()/WEEK(CURDATE())/DATE_SUB(CURDATE()...) used in several controllers resolve to the DB hosts day (UTC on the OCI box), not the apps Asia/Riyadh day set via APP_TIMEZONE/RuntimeConfigServiceProvider. Between 00:00 and 03:00 Riyadh time, CURDATE() is still yesterday in the DB, so DataQualityControllers overdue/inverted-date flags, DashboardControllers medically-discharged delay-days, top-diagnosis-this-week rollup, prior-week occupancy trend, and RegistryControllers LOS-based sort can silently disagree by one calendar day with the rest of the app (exactly the class of bug ConsultationDashboardControllers own comments describe and were fixed for, in that one file only). tests/TestCase.php explicitly pins the MySQL session timezone to match PHPs clock, comment: Test-process only, never touches production, so this drift is invisible to the test suite and only manifests in production during the 3-hour post-midnight window.
   Fix:    Either pin the MySQL connections session time_zone to the app timezone (e.g. add PDO::MYSQL_ATTR_INIT_COMMAND with SET time_zone = +03:00 to the mysql connection in config/database.php, kept in sync with RuntimeConfigServiceProviders runtime timezone override), or replace every raw CURDATE()/WEEK(CURDATE())/DATE_SUB(CURDATE()...) call with a PHP-bound now()->toDateString() parameter the way ConsultationDashboardController::ageing()/openDays() already do. Add a regression test that runs a controller assertion with the app clock frozen just after local midnight while the DB session stays on the default (unpinned) timezone.
   Auto-fixable: no

🟡 MEDIUM · [REL-05] Symptom-based SLO burn-rate alerting            confidence: medium   (Reliability, SLOs & Error Budgets)
   Where:  laravel/app/Console/Commands/BackupVerify.php:94-96 (backup age threshold), laravel/app/Console/Commands/AuditVerifyDaily.php (hash-chain break alert), laravel/app/Http/Controllers/Auth/AuthController.php:147-171 (failed-login-count threshold), laravel/app/Console/Commands/DataQualityNotify.php (data-quality threshold)
   Why:    The alerting that does exist (in-app admin notifications: backup.stale, audit.integrity_failure, security.failed_logins, data-quality thresholds) is entirely cause-based and single-threshold, not tied to any SLO or symptom of user-facing impact. There is no burn-rate or multi-window alert because there is no SLI/SLO to burn against (REL-01/REL-02). A slowly-degrading user-facing problem, such as rising page latency or a rising error rate on admissions or discharges, would not raise any alert today, even though the existing operational alerts (backup, audit chain, brute force, data quality) are real and reasonably well targeted for a system at this scale.
   Fix:    Once SLIs/SLOs exist, add a small number of symptom-based alerts (e.g. error-rate or latency burn against the SLO) on top of the existing cause-based operational alerts, which are worth keeping since they cover genuine non-SLO risks (backup integrity, audit tamper-evidence, brute force).
   Auto-fixable: no

🟡 MEDIUM · [TST-10] Async-pattern safety            confidence: medium   (Testing & Code Quality)
   Where:  resources/js/composables/useHandover.js lines 22 to 44; resources/js/Layouts/AppLayout.vue lines 292 to 303 and 308 to 320; resources/js/Components/QuickJump.vue line 99; resources/js/Pages/Auth/Register.vue line 26; resources/js/Pages/Admin/PatientMerge.vue lines 52 and 248: async functions wrapping a raw fetch() call in try/finally (state reset only) with no catch clause, and no global unhandledrejection listener anywhere in resources/js
   Why:    If one of these fetch calls fails (network drop, 500, JSON parse error), the promise rejection is unhandled: the loading or saving flag still resets because the finally block guarantees that, but the user gets no error message and the failure surfaces only as a browser console warning, meaning a silently failed handover save or notification refresh with no operator or telemetry visibility (no error-tracking SDK is installed per the Detection Summary). Severity is bounded: the browser does not crash on an unhandled rejection, and the Inertia router calls used for most writes have their own built-in error handling, so this affects only the smaller set of raw-fetch composables and handlers.
   Fix:    Add a catch branch to each raw-fetch async helper that surfaces a toast or error state to the user, and add one top-level unhandledrejection listener in resources/js/app.js as a last-resort net, consistent with the existing pattern of centralizing fetch and XSRF handling in composables such as useHandover.js.
   Auto-fixable: no

🟡 MEDIUM · [TST-11] Verification-before-completion gate enforced            confidence: high   (Testing & Code Quality)
   Where:  no .husky directory, .pre-commit-config.yaml or lefthook.yml anywhere in the repo; no CONTRIBUTING.md; laravel/docs/CI.md line 16 states that the repository currently has only no-force-push and no-delete protection on main, and that required status checks still need to be added once billing is confirmed restored; corroborated by root CLAUDE.md section 12, which states required status checks on main are not yet configured
   Why:    CI runs 4 jobs (PHPUnit tests, Vitest plus a11y, gitleaks, Semgrep) on push or PR to main, but nothing in GitHub branch protection currently requires them to pass before a merge. Combined with the projects documented workflow of direct-to-main being the consented workflow (CLAUDE.md section 2) and Coolify deploying straight from main HEAD, a red or skipped CI run does not block a merge or a deploy. There is also no local pre-commit or pre-push hook for fast feedback before push.
   Fix:    Once GitHub Actions billing is confirmed stable, turn on Require status checks to pass on main for the four Laravel CI job names, per the next step already stated in docs/CI.md. Optionally add a lightweight pre-push hook (husky or a plain git hook) running the Pint check and the Vitest run for fast local feedback, since there is no staging branch to catch issues before main.
   Auto-fixable: no

🟡 MEDIUM · [PERF-01] Static performance anti-patterns            confidence: high   (Performance & Scalability)
   Where:  laravel/resources/js/app.js:4,39 (static top-level import of ChartCanvas plus app.component(ChartCanvas, ChartCanvas)); laravel/resources/js/Components/ChartCanvas.vue:20 (static import of at-lib/chartjs); laravel/resources/js/lib/chartjs.js:15-23 (Chart.js plus chartjs-plugin-datalabels registration)
   Why:    ChartCanvas is registered as a global Vue component from a static top-level import chain in the single-entry app.js, with no manualChunks or defineAsyncComponent split in vite.config.js. Chart.js and chartjs-plugin-datalabels therefore ship in the shared entry bundle every page pays for on load, including Login, Registry, Patients and every admin page that never renders a chart, inflating parse and execute time and time-to-interactive on the majority of page views, even though only Dashboard, Statistics and ConsultationDashboard actually chart anything.
   Fix:    Register ChartCanvas locally (import plus components: ChartCanvas) only on the pages that render charts, or wrap it in defineAsyncComponent(() to import Components/ChartCanvas.vue) so Vite code-splits it into its own lazily-loaded chunk instead of the shared entry bundle.
   Auto-fixable: yes

🟡 MEDIUM · [PERF-03] Pagination and bounded result sets            confidence: medium   (Performance & Scalability)
   Where:  laravel/app/Http/Controllers/PatientsController.php:243-265 (boardGroups: no LIMIT or pagination on the assigned-active query) and 234-235,250 (view=longterm/tb: includeDischarged drops the whereNull discharge_date filter, so the query returns every long-term or TB-flagged episode ever recorded, open or closed, with no cap); 187-197 (activeList uses the same unbounded call). Registry, Consultations and Audit list endpoints are correctly bounded (RegistryController.php:290,382,418 paginate(20); ConsultationsController.php:100 paginate(15); AuditController.php:30 paginate(50), all server-fixed page sizes, never client-controlled).
   Why:    The default board and printable census are bounded in practice by physical bed capacity (self-limiting), but the view=longterm/view=tb board variant is a monotonically growing historical query with no page cap. As the long-term-flagged cohort accumulates over the years it will push an ever-larger single-response payload to the browser and do an ever-larger scan, unlike every other list surface in the app which is deliberately paginated.
   Fix:    Paginate the longterm/TB board view (or add a date-range/status filter with a hard cap) the same way Registry already paginates history; leave the default open-episode board unbounded since it is naturally capacity-limited by physical bed count.
   Auto-fixable: no

🟡 MEDIUM · [PERF-04] Core Web Vitals and API payload bounds            confidence: high   (Performance & Scalability)
   Where:  missing: no web-vitals, reportWebVitals or lighthouserc.* found anywhere in laravel/ (grepped resources/js and repo root); no RUM wiring in resources/js/app.js
   Why:    LCP, INP and CLS are never measured in the field or gated in CI, so a regression in bundle size or render performance (for example the PERF-01 chart-bundle issue) would ship and stay invisible until a clinician complains. API payload discipline itself is good (Registry/Statistics/Dashboard controllers explicitly select narrow column sets, for example RegistryController.php:238 patient:id,mrn,name,gender,age,nationality) and response compression is plausibly handled by the Cloudflare edge in front of the origin (documentary fact, not verifiable from this repo); the gap is specifically the absence of any Vitals measurement.
   Fix:    Wire the web-vitals package to report LCP/INP/CLS to the existing audit/log pipeline or a lightweight endpoint, or add a Lighthouse CI budget step to laravel-ci.yml frontend job.
   Auto-fixable: yes

🟡 MEDIUM · [PERF-08] Resource pools, GC, deadlines and load shedding            confidence: medium   (Performance & Scalability)
   Where:  routes/web.php (only throttle:auth/register/register-email/stepup/email-verify/csp-report are rate-limited; no general-purpose throttle on Registry search or export, Dashboard, or the Patients/Consultations boards); no PHP-FPM worker-count or MySQL max_connections tuning, and no per-request deadline middleware, found anywhere in laravel/ (Nixpacks/Coolify-managed build, not committed to the repo)
   Why:    Beyond the narrow auth-related endpoints, the app has no explicit request deadlines and no 503/Retry-After overload-shedding path; under a burst of concurrent clinical-page load (for example shift-change) PHP-FPM would queue or the process would hang rather than degrade predictably, and pool sizing is entirely opaque to anyone reading this repo. Actual risk is bounded today by the small user base (about 330 staff, one unit) rather than internet-scale traffic.
   Fix:    Document the Nixpacks/Coolify PHP-FPM worker count and MySQL max_connections against the single-host capacity; add a general per-route throttle on the heavier admin read paths (Registry/Statistics/Reports); consider a maintenance/overload 503 response with Retry-After for the rare case of DB exhaustion.
   Auto-fixable: no

🟡 MEDIUM · [RES-05] Graceful degradation, fail-fast & load shedding            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/app/Jobs/GenerateMonthlyReport.php:38-40
   Why:    The monthly-report mailer loops every active ReportRecipient and calls Mail::to($r->email)->queue(...) with no try/catch; since QUEUE_CONNECTION=sync this runs inline, so an SMTP failure while emailing the Nth recipient throws and aborts the loop, recipients after the failing one silently never receive that month's booklet, and (unlike audit:verify-daily and backup:verify, which both fan out an in-app admin notification plus a best-effort email on failure) nothing surfaces this to an admin beyond a log line, so the gap can go unnoticed for a month. This is a non-critical/optional-dependency path, not a core clinical flow, which is why this is scored Medium rather than the category's default High.
   Fix:    Wrap the per-recipient send in try/catch, continue the loop on failure, collect the failed addresses, and log/notify admins (mirroring the pattern already used in AuditVerifyDaily::emailReportRecipients and BackupVerify::raiseIncident) so a partial mail outage degrades gracefully instead of silently dropping the rest of the batch.
   Auto-fixable: no

🟡 MEDIUM · [RES-08] Cache stampede / thundering-herd protection            confidence: medium   (Resilience & Fault Tolerance)
   Where:  laravel/app/Http/Controllers/DashboardController.php:95; laravel/app/Support/DashboardCache.php:19-24
   Why:    The dashboard heavy tier (per-consultant board, 6-month chart, YTD, top-dx, LOS buckets) is cached under one global key with a fixed 300s TTL and no jitter, request-coalescing/single-flight, or dogpile lock. Cache::remember() is not atomic across concurrent PHP-FPM processes on the file cache driver (CACHE_STORE=file per .env.example), so if several dashboard requests land at the exact moment the key expires, each can independently recompute the same heavy multi-query aggregation against MySQL simultaneously. Real-world blast radius is bounded by the small staff headcount (about 330 users, modest realistic concurrency), which lowers likelihood/impact versus an internet-scale service.
   Fix:    Add TTL jitter (e.g. 300 +/- random(0,30) seconds) and wrap the recompute in a Cache::lock()-guarded single-flight so only one process recomputes on expiry while others serve the still-warm value or wait briefly.
   Auto-fixable: yes

🟡 MEDIUM · [RES-09] Single points of failure & failure-mode enumeration            confidence: high   (Resilience & Fault Tolerance)
   Where:  laravel/docs/DEPLOY-LARAVEL.md:19-34 (there is one environment, no staging); CLAUDE.md sections 1 and 3
   Why:    The app container and the MySQL 8 container both run on a single OCI Ubuntu host with no replica, no multi-AZ, and no documented region-evacuation runbook; a single host/container failure is a full outage with no automatic recovery. This is partially mitigated for DATA LOSS by nightly encrypted off-box backups plus a drilled restore (RPO under 24h, RTO measured and recorded in BACKUP-AND-RESTORE.md section 8), but there is no equivalent mitigation for AVAILABILITY. Context, not a defect in this codebase: as of 2026-09-03 this Laravel app is not yet the unit's daily system, the legacy app on separate SiteGround hosting is, so today an outage here would not itself stop clinical care, but this SPOF becomes decisive risk at cutover.
   Fix:    Document the accepted-risk decision explicitly (single-host is a deliberate cost/complexity tradeoff for this scale) and/or add a second app replica behind Traefik and a replicated/standby MySQL instance before cutover from the legacy system makes this host the sole daily system of record; write a one-page failure-mode/runbook note (host loss, disk failure, OCI regional incident) alongside the existing backup runbook.
   Auto-fixable: no

🟡 MEDIUM · [DATA-12] Business Impact Analysis, bus-factor SPOFs, and crisis comms            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:20 (A7) and :36 (B8, sole collaborator on the GitHub repo); no BIA/BCP/crisis-comms document found under laravel/docs/
   Why:    No documented Business Impact Analysis backs the RPO/RTO numbers in DATA-01; the system owner is explicitly the sole GitHub collaborator and sole information-security lead (a documented, self-acknowledged single point of failure for deploys, credentials and incident response); no crisis-communication plan (declaration owner, notification templates, status-page ownership) was found in INCIDENT-RESPONSE.md or elsewhere. If that one person is unavailable during an incident, there is no documented second path to deploy, restore, or communicate.
   Fix:    Conduct a lightweight BIA that explicitly derives the accepted RPO/RTO from clinical impact; cross-train or secret-share at least one additional person on deploy access and the backup-key escrow; write a short crisis-communication plan naming who declares an incident and how the unit is told what data window was lost.
   Auto-fixable: no

🟡 MEDIUM · [DATA-13] Per-release supply-chain evidence archived to immutable storage            confidence: high   (Data, Backups, DR & Correctness)
   Where:  .github/workflows/laravel-ci.yml (no sbom/provenance/attestation/object-lock/WORM step found)
   Why:    CI runs composer audit (arbitrated by scripts/composer-audit-gate.php) and gitleaks/Semgrep as blocking gates, and Dependabot is enabled, but no SBOM, build provenance/attestation, or per-release dependency manifest is generated or archived to tamper-protected storage. Which shipped releases carried dependency X is not answerable from the repo alone, which slows incident response if a dependency CVE surfaces later.
   Fix:    Add a CycloneDX/SPDX SBOM generation step to laravel-ci.yml (composer and npm SBOM tooling) and archive the SBOM plus the composer-audit-gate output per commit SHA to a retention-locked location, reusing the existing S3SigV4 pattern already used for audit/backup archives.
   Auto-fixable: no

🟡 MEDIUM · [DATA-14] Storage/data footprint minimized and lifecycled            confidence: high   (Data, Backups, DR & Correctness)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:60 (D1, PHI-copy inventory); laravel/docs/compliance/DATA-RETENTION.md:58-66 (section 2.3 backups/copies table, multiple GAP rows)
   Why:    Multiple ungoverned plaintext copies of regulated health data currently exist outside the new controlled pipeline: legacy dumps on the owner personal workstation, plaintext dumps and leftover credential files under /home/ubuntu/migrate/dmc/ on the OCI host, and manual pre-deploy dumps that accumulate unencrypted on the host with no expiry rule. The backup bucket own 90-day lifecycle rule is a placeholder not yet confirmed as actually applied. Clinical rows themselves are retained indefinitely with no purge/anonymisation mechanism built yet. Severity raised from the category Low default to Medium because the accumulating copies are unencrypted PHI on a multi-project shared host and a personal workstation, not merely a cost/hygiene issue.
   Fix:    Inventory and securely delete (shred) every ad-hoc plaintext dump on the owner workstation and the OCI host; confirm and document the actual lifecycle rule configured on the dmc-db-backups bucket; encrypt and auto-expire manual pre-deploy dumps.
   Auto-fixable: no

🟡 MEDIUM · [CFG-06] Dev/staging/prod parity; complete .env.example            confidence: high   (Config, Secrets & Environments)
   Where:  .github/workflows/laravel-ci.yml:113 (mysql:8.0) and :132 (php-version: 8.4) vs laravel/docs/DEPLOY-LARAVEL.md:43 (Nixpacks builds PHP 8.3) and CLAUDE.md section 3 (MySQL 8.4 in production) and composer.json:9 (php: ^8.3)
   Why:    CI validates every change against PHP 8.4 + MySQL 8.0, while production runs PHP 8.3 + MySQL 8.4 (Nixpacks-built). A green CI run does not prove the code behaves identically on the runtime/database versions actually serving PHI - dialect or deprecation differences (the two-pass PHPUnit setup explicitly tests DATEDIFF/DATE_FORMAT/JSON dialect behaviour) could pass in CI and misbehave in prod, or vice versa.
   Fix:    Pin the php-version in laravel-ci.yml to 8.3 and the mysql service image to mysql:8.4 so CI matches the Nixpacks/production stack; add a laravel/.nvmrc (22) for local/CI Node parity.
   Auto-fixable: yes

🟡 MEDIUM · [CFG-11] Environments rebuildable from IaC with drift detection & parity            confidence: high   (Config, Secrets & Environments)
   Where:  missing: Terraform/Pulumi/CloudFormation/Ansible/nixpacks.toml anywhere in the repo; laravel/docs/DEPLOY-LARAVEL.md documents manual SSH commands, hand-typed Coolify REST API curl calls, and manual host-crontab edits as the actual provisioning path
   Why:    The only OCI compute instance, its firewall/security-list rules, the host cron entry that drives the Laravel scheduler, and the Coolify app configuration all exist purely as click-ops / manually-run commands. If the instance is lost, there is no codified, automated way to reproduce it - recovery depends entirely on a human correctly re-running the runbook steps from docs/DEPLOY-LARAVEL.md and docs/compliance/INCIDENT-RESPONSE.md, and any configuration drift between what the runbook says and what is actually running is invisible until an incident exposes it.
   Fix:    Codify the OCI instance, security list, and Cloudflare DNS/WAF rules in Terraform (or Ansible for host-level config: cron, firewall), export/commit the Coolify app definition where possible, and add scheduled drift detection (even a simple terraform plan on a schedule) once codified.
   Auto-fixable: no

🟡 MEDIUM · [CMP-03] DPIA, ROPA, DPAs & 72-hour breach notification            confidence: high   (Compliance, Privacy & Legal)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:18 (A5 - no controller-processor contract); laravel/docs/compliance/DPA-AND-TRANSFERS.md:18-21 (P0-P3 rows: No DPA on file / No signed DPA on file, status OPEN); laravel/docs/compliance/EVIDENCE-PACK.md:129 (G12 top-priority gap)
   Why:    DPIA.md, ROPA.md and INCIDENT-RESPONSE.md exist as substantive drafts (progress, not absent), but the dated (2026-09-03) project documentation directly confirms zero signed DPAs exist with any processor (OCI, Cloudflare, the SMTP relay) and no controller-processor contract exists between the hospital and the operator company at all. Real PHI for roughly 17k patients is processed today under this gap: no contractual audit rights, breach-notification duty, or liability allocation with any processor.
   Fix:    Execute the controller-processor contract (IR Art. 17 minimum content per PROPOSED-CITATIONS.md) as the top action (G12); obtain OCI DPA, Cloudflare Customer DPA, and a DPA for the SMTP relay (or move it in-Kingdom, G13); then route DPIA.md/ROPA.md/INCIDENT-RESPONSE.md through DPO/governance sign-off so they move from draft to adopted.
   Auto-fixable: no

🟡 MEDIUM · [CMP-06] Privacy-by-design: notice, consent & DPO            confidence: medium   (Compliance, Privacy & Legal)
   Where:  laravel/docs/compliance/CONFIRMED-FACTS.md:19 (A6 - DPO not appointed, tagged CONFIRMED (gap)); laravel/docs/compliance/DPO.md:22-24 (the hospital should assume it is in scope unless counsel concludes otherwise); laravel/docs/compliance/EVIDENCE-PACK.md:126 (G9)
   Why:    The privacy notice itself is implemented and reachable (routes/web.php:86 GET /privacy, no session required, EN+AR via resources/lang/{en,ar}/privacy.php), which is real credit -- but the hub processes sensitive health data at hospital scale for roughly 17k patients and its own compliance drafts self-assess a DPO appointment as effectively mandatory (IR Art. 32(1)(c) [PROPOSED]), yet none has been named. There is no independent, designated contact for data-subject or regulator escalation today.
   Fix:    Have DMC / Eastern Health Cluster issue a written, dated DPO designation (internal or external, per DPO.md section 2) and publish the contact on /privacy and in the privacy-notice text (currently placeholders).
   Auto-fixable: no

🟡 MEDIUM · [DOC-07] On-call, ownership & key links            confidence: high   (Documentation & Runbooks)
   Where:  laravel/SECURITY.md:16 (security@<hospital domain> [SET THIS] placeholder) vs laravel/.env.example:47 (SECURITY_CONTACT=mailto:info@dmc-im.com, the value actually served at /.well-known/security.txt)
   Why:    The human-readable vulnerability-disclosure and escalation contact published in SECURITY.md is a literal unfilled placeholder, inconsistent with the real address already configured as the SECURITY_CONTACT env default. A researcher or auditor reading SECURITY.md rather than fetching security.txt has no working contact. There is also no CODEOWNERS file or named on-call rotation anywhere in the repo; ownership is only informally the owner throughout CLAUDE.md and HANDOFF.md, with no documented escalation path for an incident when the owner is unavailable.
   Fix:    Replace the [SET THIS] placeholder in laravel/SECURITY.md with the real contact (matching SECURITY_CONTACT, or the intended production address if different) so the human-facing doc and the machine-readable security.txt agree. Consider adding a minimal CODEOWNERS or an Operations block naming the accountable owner/team and an escalation path.
   Auto-fixable: yes

🟡 MEDIUM · [DOC-09] Documentation freshness / sync with code            confidence: high   (Documentation & Runbooks)
   Where:  laravel/README.md:114-115; laravel/docs/DEPLOY-LARAVEL.md:31,50,314-319; laravel/docs/RELEASE-CHECKLIST.md:17; laravel/docs/CI.md:130 vs laravel/composer.lock:1781; laravel/.github/workflows/laravel-ci.yml (4 jobs); laravel/routes/public.php:20 plus app/Http/Controllers/HealthController.php
   Why:    Three separate docs, README.md, DEPLOY-LARAVEL.md and RELEASE-CHECKLIST.md, still say CI is a two-job (frontend, backend) pipeline that is billing-blocked and proves nothing, and that GET /health is being added in a parallel workstream or still 404. All of this is stale: CI was hardened to four jobs (frontend, backend, secrets=gitleaks, sast=semgrep) and confirmed green on 2026-09-03 per CLAUDE.md section 12 and HANDOFF.md, and the CI-hardening commit 4b75aca and the health-endpoint commit dea9dff both predate a later edit to DEPLOY-LARAVEL.md (ed704db) that touched the same file without correcting these lines; /health has been live since dea9dff. RELEASE-CHECKLIST.md is the literal pre-deploy gate, if a box cannot be ticked the release does not proceed, and its own header line tells every deployer CI proves nothing, so an operator following it verbatim would distrust or ignore a real CI signal. Separately, docs/CI.md line 130 itself claims league/commonmark 2.8.2 still carries eight HIGH advisories and keeps the backend job red by design, but composer.lock already locks league/commonmark at 2.10.0, the very version CI.md itself says resolves the advisories, so that paragraph is also out of date and overstates a currently-nonexistent CI failure.
   Fix:    Do a pass over DEPLOY-LARAVEL.md sections 0, 1 and 11, RELEASE-CHECKLIST.md gates header, README.md Tests-and-gates section, and CI.md commonmark paragraph, replacing the billing-blocked, two-job, health-not-shipped language with the current four-job, CI-green, health-live state, matching CLAUDE.md section 12 and HANDOFF.md which already have it right. Consider a short doc-freshness check in CI (a grep for known-stale phrases, or a periodic review) so operational runbooks do not drift from code again.
   Auto-fixable: yes

🟡 MEDIUM · [UX-03] Semantic HTML: landmarks, heading order, no clickable div/span            confidence: high   (Visual QA & Accessibility)
   Where:  resources/js/Layouts/AppLayout.vue:497 (unconditional h1 title in the app chrome) co-rendered with a second, on-screen h1 in: ActiveList.vue:56, Consultations/Handover.vue:76, Consultations/Dashboard.vue:95, Reports/Governance.vue:31, Reports/Index.vue:43, Reports/Monthly.vue:39
   Why:    Six pages render two h1 elements at once on screen (not print-only) - the sticky app-chrome title and the page own report letterhead. Screen-reader users navigating by heading level get a confusing document outline with two competing top-level headings - WCAG 1.3.1. Landmarks (header, nav, main) are otherwise consistent app-wide, and the div/span click handlers found elsewhere are all modal/dropdown backdrop-dismiss patterns backed by real focus-trap plus Escape handling, not the clickable-div antipattern this check targets, so severity is bounded.
   Fix:    Demote the inner report-letterhead heading to h2 or below, or mark it screen-reader-hidden and only expose it as h1 in a print media context, so exactly one h1 exists on screen per page.
   Auto-fixable: yes

🔵 LOW · [CICD-12] Value-stream flow health & DORA metrics measurable            confidence: medium   (CI/CD & Release Engineering)
   Where:  git tag -l returns 0 tags, despite RELEASE-CHECKLIST.md:33-34 prescribing an annotated v-YYYY.MM.N tag per release and despite multiple real production deploys already having happened per CLAUDE.md history
   Why:    Deployment frequency and lead-time-for-changes are not mechanically derivable from the git repo as designed -- the documented tagging convention exists on paper but has never been used.
   Fix:    Start tagging releases per the documented convention, or have the Coolify deploy step create a lightweight tag or GitHub deployment record automatically on each real deploy, so the four DORA keys become queryable later.
   Auto-fixable: yes

🔵 LOW · [DOC-05] Changelog & versioning            confidence: medium   (Documentation & Runbooks)
   Where:  laravel/docs/RELEASE-CHECKLIST.md:33 (documents there is no CHANGELOG file, the tag message is the release note); repo has 0 git tags (git tag -l)
   Why:    The one documented substitute for a changelog, an annotated git tag per release (v<YYYY.MM.N>), has never actually been used. The repo has zero tags despite many production releases. An operator or auditor trying to answer what changed between release X and Y, and when, has only raw git log on main with no release boundaries, and GET /health app.version reports the deploy SHA, not a human-meaningful version.
   Fix:    Either start tagging releases per the RELEASE-CHECKLIST.md convention (retroactively tag recent deploy SHAs so the practice has a starting point), or add a lightweight CHANGELOG.md / rely explicitly on conventional commit messages plus git log and remove the unused tag instruction from RELEASE-CHECKLIST.md so the doc matches practice.
   Auto-fixable: yes

🔵 LOW · [I18N-05] Unicode-correct string operations (NFC, graphemes, invariant case)            confidence: medium   (i18n, l10n & Text)
   Where:  laravel/app/Providers/AppServiceProvider.php:30; laravel/app/Http/Controllers/Auth/AuthController.php:41; laravel/app/Http/Controllers/Auth/UsernameReminderController.php:34
   Why:    The login identity lookup (username-or-email) and the forgot-username lookup fold case with plain strtolower() instead of the mb_strtolower()/Str::lower() pattern used correctly elsewhere in the codebase (LegacyImport.php, ConsultationsController.php:617,631, ConsultationRequest.php:191,197, RegistryController.php:508). Plain strtolower() only case-folds ASCII bytes, so a non-ASCII username or email would not be folded the same way on the PHP side as it is by MySQLs utf8mb4_unicode_ci collation, which is what actually arbitrates the WHERE match; this is a latent inconsistency, not a demonstrated bypass. No setlocale() call exists anywhere in the app (verified by search), so the classic Turkish-I locale-flip is not reachable, which is why this stays Low rather than the categorys auth-bypass escalation.
   Fix:    Replace the three strtolower() calls on identity fields with Str::lower() or mb_strtolower for consistency with the rest of the codebase and to avoid relying solely on DB collation for correct Unicode case-folding. Separately, no NFC normalization step was found before storing or comparing user-entered text anywhere in the app; confirm whether this has ever produced a duplicate or lookup defect (raised as intake, not a fail, since MRNs are digits-only and no concrete incident was found).
   Auto-fixable: yes

## What's Good (per auditor)
- (Reliability, SLOs & Error Budgets) laravel/app/Http/Controllers/HealthController.php is a genuine deep liveness probe: fresh-connection DB check, storage-writable check, and a scheduler heartbeat that goes stale after 5 minutes, built specifically because the scheduler had previously died silently (the monthly report, audit shipping and the integrity check all quietly stopped). That is real reliability instinct even without formal SLOs.
- (Reliability, SLOs & Error Budgets) laravel/scripts/smoke.sh runs a post-deploy check against /up and /health and the build manifest, so a broken deploy is caught immediately rather than discovered by a user report.
- (Reliability, SLOs & Error Budgets) Cause-based operational alerting exists and is reasonably well targeted for the risks that matter most here: stale or missing off-box backups (BackupVerify.php), a broken audit hash-chain (AuditVerifyDaily.php), and failed-login bursts (AuthController.php) all raise deduplicated in-app notifications to active admins.
- (Reliability, SLOs & Error Budgets) laravel/docs/DEPLOY-LARAVEL.md section 0 documents the production topology (Cloudflare, one OCI host, MySQL container, scheduler cron, audit off-box shipping) clearly enough to serve as the starting point for a real dependency map.
- (Reliability, SLOs & Error Budgets) laravel/docs/BACKUP-AND-RESTORE.md section 1 states RPO and RTO in plain English and records a real, measured restore drill dated 2026-09-03 rather than an untested claim.
- (Observability) OBS-07 is a genuinely strong implementation: the stock Laravel /up liveness route is left untouched for the Coolify poll (bootstrap/app.php:12), while a separate, deliberately-engineered /health readiness probe (app/Http/Controllers/HealthController.php) opens a fresh, 2-second-bounded DB connection rather than reusing the request's lazily-opened one, checks storage/framework writability, and checks a scheduler heartbeat that goes stale after 5 minutes - the code comment explains this was added specifically because the scheduler had previously died silently and taken audit shipping, backups and the monthly report with it. It correctly returns 503 on any failure and deliberately carries no PHI, secrets or hostnames.
- (Observability) OBS-02 discipline is real and intentional, not accidental: CspReportController allow-lists only 7 non-PHI CSP fields and truncates them; EncryptedNarrative logs table/id/column on a decrypt failure but never the value; code comments in MfaController explicitly state the audit trail must never record the selector or validator when logging MFA device trust/revocation events. No Log:: or AuditLog::create() call site was found dumping request bodies, headers, passwords, or tokens.
- (Observability) The audit_log hash-chain (app/Support/Audit.php) is a well-designed structured event trail for security-relevant actions (login, password change, MFA, admission/consultation writes) with actor and IP always session-sourced - not a substitute for OBS-01/03/04/05 but a solid adjacent control worth noting.
- (Security & App Hardening) Login and password or username reset flows use identical, generic responses for valid versus invalid accounts and are throttled by account plus IP, not IP alone, a strong anti-enumeration and brute-force posture (AuthController.php, PasswordResetController.php, AppServiceProvider.php).
- (Security & App Hardening) Mandatory TOTP MFA for every account including admins, with a replay-guarded, attempt-capped challenge, and remember-me deliberately disabled to close an MFA-skip bypass (AuthController.php lines 68-116).
- (Security & App Hardening) Consistent server-side, per-object authorization: every risky write goes through a FormRequest authorize gate or User canManageAdmission or canManageConsultation, and no controller passes raw request input into a mass-assignable Eloquent create, update or fill.
- (Security & App Hardening) Nonce-based CSP prepended to the web middleware stack so even CSRF-error responses are covered, plus a full modern header set including HSTS preload, X-Frame-Options DENY, nosniff, Referrer-Policy, Permissions-Policy and no-store on authenticated pages, in SecurityHeaders.php.
- (Security & App Hardening) CI enforces gitleaks for secrets, Semgrep with ERROR-severity break-the-build for SAST, and a genuinely blocking composer-audit gate that requires a reason plus a review-by date for any accepted High or Critical CVE, not a report-only pipeline.
- (Security & App Hardening) A CSV and XLSX export formula-injection guard exists in RegistryController and AuditController.
- (Security & App Hardening) No file-upload feature exists at all, eliminating an entire class of amplification, denial-of-service and path-traversal risk.
- (Security & App Hardening) The PDF and SVG report generator, App/Support/ReportSvg, consistently HTML-escapes every interpolated label before embedding it in dompdf output.
- (Testing & Code Quality) Extensive, well-organized test corpus: 105 Feature test files plus 2 Unit files (PHPUnit) covering auth and MFA, admissions, consultations, the audit hash-chain, handovers, backups and patient merge; 75 Vitest spec files including a dedicated axe-core accessibility gate run both standalone and inside the full run.
- (Testing & Code Quality) CI runs the real suite against a real MySQL 8 service instead of sqlite or mocks, so SQL-dialect-specific behavior such as DATEDIFF, DATE_FORMAT and JSON handling is genuinely exercised, per phpunit.xml own documented rationale.
- (Testing & Code Quality) Deliberate handling of a known crashing condition: the pdf PHPUnit group is isolated into its own process to dodge a documented dompdf segfault, rather than being ignored or blindly retried.
- (Testing & Code Quality) Zero stray debug statements such as var_dump, dd, dump, console.log or debugger found anywhere in shipped application code.
- (Testing & Code Quality) Zero orphaned TODO, FIXME, HACK, XXX or BUG markers; the only bug-related hits found are explanatory comments documenting already-fixed historical defects, exactly the disciplined pattern this check rewards.
- (Testing & Code Quality) Strong type-hint discipline in PHP: roughly 95 percent of public controller and model methods declare explicit return types, and FormRequests type and validate the highest-risk writes (admission, consultation, merge, exports).
- (Testing & Code Quality) CI is genuinely comprehensive for a project this size: a gitleaks secret scan, Semgrep SAST with ERROR-severity blocking uploaded to the Security tab, a blocking composer-audit gate with a reasoned and dated ignore-list, and a build-reproducibility check that fails if the committed public/build drifts from source.
- (CI/CD & Release Engineering) CI genuinely executes on every push/PR to main with four real, independent jobs (frontend, backend, secrets=gitleaks, sast=semgrep) -- verified live via gh run list, not just configured on paper; this directly overturns the prior audit's core belief that GitHub Actions was billing-blocked and not running (see notes).
- (CI/CD & Release Engineering) Every third-party GitHub Action is pinned to a full commit SHA and the Semgrep container is pinned by manifest digest, with a documented bump procedure.
- (CI/CD & Release Engineering) scripts/composer-audit-gate.php is a genuinely sophisticated blocking SCA gate: high/critical advisories block unless explicitly ignored with a reason plus an expiring review date; verified in the live CI log that it actually blocks (0 blocking in the latest run because league/commonmark was upgraded to 2.10.0, not just ignored).
- (CI/CD & Release Engineering) public/build is committed and the CI build-reproducibility check gives a real hermetic-build guarantee for the frontend without needing extra tooling.
- (CI/CD & Release Engineering) Least-privilege workflow permissions (contents:read by default, narrowly widened per job) and no pull_request_target usage anywhere.
- (CI/CD & Release Engineering) An actual production database restore drill was executed and recorded (2026-09-03, BACKUP-AND-RESTORE.md) with matching row counts and a measured RTO, not just a paper policy.
- (CI/CD & Release Engineering) Migrations are disciplined as additive/expand-only by explicit, well-reasoned team convention, naming the specific migrations whose down() would be destructive and explaining why migrate:rollback is banned in production.
- (CI/CD & Release Engineering) Trunk-based in practice: only two branches exist repo-wide (main, renovation), no stale long-lived feature branches, no blanket test-retry masking.
- (Performance & Scalability) N plus 1 discipline is genuinely strong across the codebase: PatientsController::boardGroups, RegistryController results, and ConsultationDashboardController all batch-load related data (patient/consultant/diagnoses eager-loads, one grouped handovers lookup, one whereIn icd10 lookup) before their foreach or map, with inline comments explicitly documenting the no per-row queries invariant.
- (Performance & Scalability) Every date-range filter that participates in a hot aggregate query is deliberately kept sargable; ConsultationDashboardController::volumeDateFilter documents in its docblock exactly why it avoids wrapping the filter column in COALESCE (would force a full scan) and uses an OR of two indexed ranges instead.
- (Performance & Scalability) Registry, Consultations and Audit list endpoints all use hard server-side page sizes (paginate 20/15/50) that are never client-controlled, and CSV/XLSX exports stream via chunk(500) rather than materializing the full result set in memory.
- (Performance & Scalability) Migrations add explicit indexes on every column actually filtered in hot queries (admit_date, discharge_date, current_location, consultation_date, signoff_date, status, requested_at) and a migration comment correctly notes that constrained foreign keys are auto-indexed by InnoDB.
- (Performance & Scalability) The frontend replaced the previously-flagged ApexCharts (500KB, revenue-gated commercial license) with MIT-licensed Chart.js on 2026-09-03, using tree-shaken per-controller/element imports (lib/chartjs.js) rather than the whole library, resolving both the old bundle-size and licensing findings, even though the remaining global-registration scoping issue (PERF-01) persists at much smaller scale.
- (Performance & Scalability) Pages are individually code-split via import.meta.glob(./Pages/**/*.vue) with no eager option, and no full-library barrel imports (lodash, moment, rxjs) exist anywhere in resources/js.
- (Resilience & Fault Tolerance) app/Support/S3SigV4.php sets an explicit ->timeout(30) on every outbound call (put/head/get) used by audit shipping and backup verification, the right pattern that just was not extended to the mail transport.
- (Resilience & Fault Tolerance) Every side-effecting scheduled command (monthly-report dispatch, dq:notify, audit:verify-daily, backup:verify, audit:ship) is registered with ->withoutOverlapping() in routes/console.php, preventing double-processing from overlapping cron runs; scheduler:heartbeat's deliberate omission is justified in-code (its only effect, a cache stamp, is idempotent and a stuck lock would itself fake a dead scheduler).
- (Resilience & Fault Tolerance) audit:ship (app/Console/Commands/AuditShip.php) is genuinely idempotent: it advances a persisted high-water mark only after a successful upload, so a failed/partial ship never loses a row and a re-run never re-ships already-shipped rows.
- (Resilience & Fault Tolerance) backup:verify and audit:verify-daily both de-duplicate their admin-facing incident notifications (skip if an unresolved notification of that type is already open) and fan out through log plus in-app plus best-effort email, wrapped in try/catch so a mail outage cannot crash the incident path itself, a solid graceful-degradation pattern that GenerateMonthlyReport (RES-05 finding) does not yet share.
- (Resilience & Fault Tolerance) StoreAdmissionRequest has a real duplicate-active-MRN guard (a patient cannot be admitted twice while an episode is open) plus client-side Inertia form.processing disabling the submit button, meaningful idempotency protection on the highest-volume write path, with no payment/charge operations anywhere in the app to create a double-charge class of risk.
- (Resilience & Fault Tolerance) Registry search and admin candidate-list queries are consistently bounded (paginate(20), limit(50), or narrow business-filtered querysets) rather than loading full 17k-patient/37k-admission tables into memory.
- (Resilience & Fault Tolerance) Sensitive auth endpoints (login, register, step-up, email-verify, MFA challenge) all carry named RateLimiter throttles that return 429 under abuse, giving real load-shedding on the highest-risk paths.
- (Data, Backups, DR & Correctness) A genuinely new (shipped 2026-09-03) nightly encrypted off-box backup pipeline (scripts/backup/db-backup.py) replaced six ad-hoc plaintext on-host mysqldump gzips - the runbook is explicit about what risk this closed.
- (Data, Backups, DR & Correctness) A real end-to-end restore drill has already been executed and logged with object key, timings, and row-count comparison against the live database (BACKUP-AND-RESTORE.md section 8), not just a script that has never been run.
- (Data, Backups, DR & Correctness) backup:verify runs daily, alerts every active admin exactly once per open incident, auto-resolves, and never silently passes on a storage/credential error (app/Console/Commands/BackupVerify.php).
- (Data, Backups, DR & Correctness) A deliberate, well-reasoned migration-rollback policy: migrations are additive-only by convention, and the team explicitly documents why migrate:rollback is unsafe here and uses tested database restore instead.
- (Data, Backups, DR & Correctness) Real DB-level uniqueness constraints protect the identity invariants that matter (MRN, staff email, one diagnosis per admission) instead of racy application checks.
- (Data, Backups, DR & Correctness) Application-layer AES-256 encryption of clinical narrative columns, shipped with a transactional, idempotent, chunked, resumable migration and an honest what-is-NOT-encrypted-and-why section.
- (Data, Backups, DR & Correctness) The bulk CSV import boundary uses a real preview-validate-then-commit-only-valid-rows pattern, with invalid rows surfaced rather than silently written.
- (Data, Backups, DR & Correctness) Documentation quality across BACKUP-AND-RESTORE.md, DATA-RETENTION.md and ENCRYPTION-AT-REST.md is unusually candid - it self-flags placeholders, gaps and unmeasured numbers instead of overclaiming them.
- (Config, Secrets & Environments) .env.example is now exhaustively complete: every one of the roughly 149 distinct env() keys referenced across app/, config/, routes/, bootstrap/ and database/ has a placeholder entry (independently diffed, not just spot-checked) - this closes a gap the 2026-09-02 audit explicitly flagged (missing AUDIT_S3_*, SESSION_SECURE_COOKIE, MAIL_* keys).
- (Config, Secrets & Environments) Full git-history gitleaks scan (474 commits) found zero live secrets; the two flagged strings are confirmed false positives (a SQL doc comment describing the mfa_secret column, and the published RFC-6238 Appendix-B test vector). Commit 6c647a5, which the 2026-09-02 audit found still held a legacy DB password and the info@dmc-im.com SMTP password in history, no longer resolves in this repository - the history purge CLAUDE.md claims is independently verified here.
- (Config, Secrets & Environments) A genuinely usable, per-secret emergency-rotation runbook exists (docs/compliance/INCIDENT-RESPONSE.md section 7.4), covering DB password, APP_KEY (correctly flagging the re-encryption trap for encrypted columns), AUDIT_S3 keys, SMTP password, SSH keys and Cloudflare/GitHub tokens - this converts what the 2026-09-02 audit raised as an open intake question into a verifiable pass.
- (Config, Secrets & Environments) Every backing service (MySQL, mail, cache, queue, S3-compatible object storage) is wired exclusively through env()-driven single config values with no hardcoded hosts/ports/credentials found anywhere in config/ or app/Support/S3SigV4.php.
- (Config, Secrets & Environments) Supply-chain install hygiene is deliberate: .npmrc sets ignore-scripts=true and audit=true, composer.json restricts allow-plugins to two named plugins, no internal/private package names exist to typosquat, and CI enforces locked installs (npm ci, composer install against committed lockfiles) plus a blocking composer-audit gate.
- (Config, Secrets & Environments) Production security posture is explicit and matches the documented CI/CD hardening: no source maps ship in the committed public/build, APP_DEBUG/LOG_LEVEL/SESSION_SECURE_COOKIE/CSP_MODE production values are written down in DEPLOY-LARAVEL.md rather than left to memory, and the one .gitleaks.toml allow-list entry is narrowly scoped (path AND exact-string) to a genuinely public test vector.
- (Compliance, Privacy & Legal) ROPA.md is a real, schema-derived data inventory (not boilerplate) built table-by-table from the actual migrations and tagged SENSITIVE, cross-checked against DATABASE-AND-BEHAVIOR.md and HANDOVER-COMPLIANCE.md.
- (Compliance, Privacy & Legal) GET /privacy is implemented, reachable without a session, and served in both English and Arabic from resources/lang (LegalController.php, routes/web.php:86), with no PHI in the route.
- (Compliance, Privacy & Legal) An unusually thorough, dated (2026-09-03), owner-confirmed compliance paper trail exists: 9 PDPL drafts plus CONFIRMED-FACTS.md (facts tagged CONFIRMED/PLACEHOLDER/DECISION) plus PROPOSED-CITATIONS.md (sourced legal citations for counsel) plus EVIDENCE-PACK.md (control-to-evidence map, P1-P20, gap register G1-G13) plus a tracked OPEN-ITEMS.md (529 markers catalogued by file:line) -- this is a genuinely rare level of audit-readiness discipline for a project this size.
- (Compliance, Privacy & Legal) Dependency licensing is clean: 119 Composer packages and 264 unique npm packages, no AGPL/GPL/SSPL/unknown-license findings; all licenses present (MIT/BSD/Apache-2.0/MPL-2.0/weak-copyleft) are compatible with closed-source SaaS distribution.
- (Compliance, Privacy & Legal) INCIDENT-RESPONSE.md has a concrete SEV1-4 severity matrix keyed to this system actual detection signals (audit:verify-daily, /csp-report, failed-login alerts) rather than generic boilerplate text.
- (Compliance, Privacy & Legal) The documentation is candid about its own gaps -- no processor DPAs, no DPO, a public GitHub repo, and a legacy system still un-hardened on US hosting are all stated plainly with CONFIRMED/gap tags rather than glossed over, which is what makes this category auditable at all.
- (Cost, FinOps & Sustainability) Every guest-reachable, cost-relevant surface (login, registration including the email-send step, MFA challenge, step-up, email-verify, CSP report sink, /health, security.txt) carries an explicit named RateLimiter or inline throttle (app/Providers/AppServiceProvider.php:29-75); the classic denial-of-wallet vector, an unbounded outbound-mail/compute relay from an unauthenticated form, is closed, including a session-independent IP-keyed cap added specifically to stop a cookie-drop bypass on the registration email-send endpoint.
- (Cost, FinOps & Sustainability) Fixed single-host architecture (no autoscaler, no serverless, no per-token model API; LLM/AI is None per detection) means there is no consumption-scaled billing surface for an attacker to inflate in the first place; worst case is an availability incident, not a runaway invoice.
- (Cost, FinOps & Sustainability) Real lifecycle/TTL discipline already in code and docs for ephemeral cost-bearing artifacts: local backup pruning (scripts/backup/db-backup.py), an off-box bucket lifecycle rule (docs/BACKUP-AND-RESTORE.md section 6), and a defensive, non-zero-guarded audit-log retention/prune command (app/Console/Commands/AuditPrune.php); nothing here grows storage unbounded by default.
- (Cost, FinOps & Sustainability) The dashboard heaviest queries are cached with explicit, write-triggered invalidation (app/Support/DashboardCache.php) rather than recomputed on every page view, cutting both DB load and compute-proportional cost/energy.
- (Cost, FinOps & Sustainability) No duplicate non-prod environment running 24/7 to waste spend on; the project deliberately runs a single environment (CLAUDE.md section 1).
- (Documentation & Runbooks) laravel/README.md is exceptionally thorough and verified accurate: every referenced command, class and path (DemoSeeder, LegacyImport, .env.example, artisan commands) actually exists in the tree.
- (Documentation & Runbooks) CLAUDE.md functions as a comprehensive, currently-accurate architecture document. Its component counts (20 models, 47 migrations, the 8-file middleware list) were cross-checked against the code and matched exactly.
- (Documentation & Runbooks) laravel/docs/BACKUP-AND-RESTORE.md is an exemplary runbook: a symptom-meaning-action table (section 7), a full restore procedure with sequencing and gotchas (section 5), and a monthly drill with a real logged entry and measured timings (section 8) rather than a template left blank.
- (Documentation & Runbooks) laravel/docs/compliance/INCIDENT-RESPONSE.md has concrete, stack-specific containment playbooks (session revocation, per-user disable, a secrets-rotation table) rather than generic advice.
- (Documentation & Runbooks) laravel/.env.example fully covers every environment variable actually referenced by application code under app, routes and bootstrap. No gap found.
- (Documentation & Runbooks) Load-bearing decisions are consistently recorded with context and consequences even without a formal docs/adr folder (DEPLOY-LARAVEL.md section 4.3 on why migrate:rollback is banned, section 7 on the consultations cutover flag, docs/CI.md per-advisory ignore rationale).
- (Operational Readiness & Incidents) INCIDENT-RESPONSE.md is a genuinely thorough, stack-specific runbook: a SEV1-4 matrix with concrete examples tied to this system's own signals (audit.integrity_failure, registry.export_xlsx row_count, trusted-device cookies), a first-hour checklist, containment playbooks for real scenarios (session revoke, user disable, Cloudflare edge block, secret rotation table, audit-chain-break localisation, PHI-exfil-via-export), an evidence-preservation table with chain-of-custody, and a PDPL/SDAIA breach-notification decision tree - unusually complete for a system this size.
- (Operational Readiness & Incidents) Blameless-postmortem template (section 11) enforces owner, due-date and tracking-link per action item, and section 12's tabletop-exercise schedule uses concrete, stack-specific scenarios (compromised-admin exfil, ransomware, audit-chain break, auth bypass) rather than generic drills.
- (Operational Readiness & Incidents) The rollback mechanism in DEPLOY-LARAVEL.md is mechanically real, not aspirational: immutable per-commit-SHA Coolify images make app-only rollback an actual ~2-3 minute swap, and every migration whose down() is destructive or a no-op is explicitly enumerated so migrate:rollback is correctly banned in favour of dump-restore.
- (Operational Readiness & Incidents) RELEASE-CHECKLIST.md gives every release a concrete go/no-go gate (gates green, backup taken, smoke passed, audit chain verified, rollback owner named) - real, repeatable release-readiness discipline even though it is not a one-time PRR.
- (Operational Readiness & Incidents) Backup/restore automation (scripts/backup/db-backup.py, db-restore-drill.sh) is genuinely automated, encrypted, off-box, and was already drilled once for real on 2026-09-03 with measured timings recorded in BACKUP-AND-RESTORE.md section 8 - real toil reduction on the highest-stakes runbook.
- (Operational Readiness & Incidents) RFC 9116 security.txt and SECURITY.md VDP exist, are exercised by a real test (VulnerabilityDisclosureTest.php), and are deliberately session-less so a scanner hitting the well-known path cannot mint sessions.
- (Operational Readiness & Incidents) The consultations_source_of_truth flag is a well-documented incremental-migration seam (strangler/dark-launch pattern) for the legacy-to-Laravel cutover, backed by a parallel-run architecture rather than a big-bang switch, on a single mainline branch with no long-lived divergent branches.
- (Visual QA & Accessibility) scripts/contrast.mjs is a genuinely rigorous, self-validated WCAG plus CIEDE2000 perceptual-distance gate blocking CI - well above what most codebases ship for color a11y.
- (Visual QA & Accessibility) A real, tested focus-trap composable (useModalA11y.js) is shared by every modal, with Escape-to-close, role=dialog/aria-modal, and focus restore-on-close.
- (Visual QA & Accessibility) A skip-to-content link, a correctly implemented global focus-visible ring, and zero positive tabindex anywhere in the app.
- (Visual QA & Accessibility) prefers-reduced-motion is handled globally and prefers-color-scheme drives the theme with a persisted, no-flash toggle.
- (Visual QA & Accessibility) vitest-axe runs as its own named, blocking CI step ahead of the full Vitest run, so an axe regression is legible in the run list, not buried.
- (Visual QA & Accessibility) PatientForm.vue and the Consultations/Index.vue edit forms already demonstrate the correct house pattern (unique id/for pairs plus aria-describedby) that the UX-04 fail should be brought in line with.
- (Visual QA & Accessibility) Strong design-token discipline; almost no stray hardcoded hex values in components.
- (i18n, l10n & Text) All real database connections (mysql, mariadb, the read-only legacy connection) are pinned to utf8mb4/utf8mb4_unicode_ci in config/database.php and .env.example, and none of the 47 migrations override that with a 3-byte utf8 charset or a different collation.
- (i18n, l10n & Text) Timestamp and instant displays in the Vue UI go through Intl-backed toLocaleString() (ActivityPanel, HandoverModal/Capture, PatientCard, Audit/Index, Handovers/Index) and numeric counters use toLocaleString() rather than manual formatting; the app has no currency values to get wrong.
- (i18n, l10n & Text) The one genuinely bilingual surface (the PDPL privacy notice) is done well: resources/lang/en and ar privacy.php ship as a real committed catalog, the Vue page sets dir and lang dynamically and uses Tailwind logical properties (text-start, ps-6) instead of physical left/right, embedded Arabic fragments elsewhere (sidebar trust badge, login footer) are correctly isolated with lang=ar dir=rtl spans, and PrivacyNoticeTest::test_the_two_languages_have_the_same_shape runs in CI as a structural missing-key/shape-parity gate between EN and AR.
- (i18n, l10n & Text) Patient and staff identity validation is Unicode-friendly by construction: StoreAdmissionRequests name rule has no ASCII-only regex, nationality is constrained to a canonical countries reference table instead of free text, and the patients table carries no phone/address/postal-code columns that could impose US-centric formats; the only regex in the entire Requests directory is the digits-only MRN rule.
- (i18n, l10n & Text) The team is already unusually self-aware of the CURDATE-vs-app-timezone risk that this audit flags in I18N-02: ConsultationDashboardControllers own comments name the exact mechanism and consequence, and it was fixed there; the finding below is that the same fix was not carried to DataQualityController, DashboardController and RegistryController, and tests/TestCase.phps timezone pin (explicitly test-only) shows the gap was known, not overlooked.

## Intake — could not verify from the repo
❓ [REL-01] Are user-facing SLIs (availability + latency percentiles) defined and measured anywhere outside the repo (Cloudflare Analytics, an external uptime monitor, APM) for https://dmc-new.towardpcc.com?  (Reliability, SLOs & Error Budgets)
❓ [REL-02] Is there an agreed SLO target and rolling window per SLI, and how was it chosen (clinical-user need vs. whatever the app currently hits)?  (Reliability, SLOs & Error Budgets)
❓ [REL-03] Is there an agreed error-budget policy, i.e. what happens (release freeze, reliability-first prioritization) when the currently-undefined error budget is exhausted?  (Reliability, SLOs & Error Budgets)
❓ [REL-07] Are reliability expectations tracked per critical clinical journey (admission, assignment, discharge, consultation ledger, handover) rather than only as a single site-wide up-or-down signal?  (Reliability, SLOs & Error Budgets)
❓ [REL-08] Are the hard dependencies' own availability figures known (Cloudflare Free plan, single OCI host me-riyadh-1, SiteGround US SMTP relay) and reconciled against any reliability target, given the app currently runs on one host with no documented failover?  (Reliability, SLOs & Error Budgets)
❓ [SEC-07] Is there a documented threat-model or secure-design review step for new clinical features, for example before shipping changes to ward-to-ICU transfer, the consultation ledger, or patient merge? Not visible in the repo; SAST and secret-scan gates are present and satisfy the tooling half of this check.  (Security & App Hardening)
❓ [SEC-11] Now that CI has reportedly run green, will the four Laravel CI jobs be turned on as required status checks on main, and is a second-approver review step planned for production-affecting pushes as, or if, the team grows beyond a single maintainer? docs/CI.md itself flags this as an open next step.  (Security & App Hardening)
❓ [SEC-09] Is a breached-password HIBP k-anonymity check planned at signup or password change, or is mandatory MFA considered the intentional, sufficient compensating control here?  (Security & App Hardening)
❓ [TST-01] laravel/docs/CI.md and laravel/docs/RELEASE-CHECKLIST.md still assert GitHub Actions is billing-blocked and CI proves nothing, while root CLAUDE.md, dated the same day (2026-09-03), says CI ran green via gh run list. Which is current: has the full CI pipeline actually executed green end-to-end since the 2026-09-03 hardening, and can the stale doc language be reconciled so a deployer following RELEASE-CHECKLIST.md is not told to distrust a working gate?  (Testing & Code Quality)
❓ [TST-05] No static type checker (phpstan or larastan for PHP, TypeScript for the Vue frontend) is configured for either stack. Is adopting one in scope for a future hardening pass, given the app handles PHI and roughly 95 percent of PHP methods are already return-typed?  (Testing & Code Quality)
❓ [CICD-04/08] Is Coolify Auto Deploy on push actually enabled for the dmc-new app, and if so, does it wait for the GitHub Actions Laravel CI result before building, or can it deploy a commit before or regardless of CI finishing?  (CI/CD & Release Engineering)
❓ [CICD-04] Now that CI is verifiably running (contra the billing-blocked claims in docs/CI.md and DEPLOY-LARAVEL.md section 11), will those two docs be corrected, and will the four Laravel CI job names actually be turned on as GitHub required status checks per the plan docs/CI.md already states?  (CI/CD & Release Engineering)
❓ [CICD-08] Given this is a single production environment holding real PHI, is there appetite to script the post-deploy smoke test and rollback decision rather than relying entirely on a human-run checklist?  (CI/CD & Release Engineering)
❓ [CICD-11] Is a second reviewer or operator planned for this project, or is solo operation (with its single-point-of-compromise risk on deploy or pipeline-config changes) an accepted, permanent risk?  (CI/CD & Release Engineering)
❓ [CICD-12] Will the RELEASE-CHECKLIST.md release-tagging convention actually start being used, given zero tags exist despite multiple recorded production deploys?  (CI/CD & Release Engineering)
❓ [PERF-05] Has this service been load-tested to its knee point (for example simulated shift-change concurrent board/registry access)? What RPS/latency was observed?  (Performance & Scalability)
❓ [PERF-06] Is there a documented peak/6-month/10x growth plan for patient volume and concurrent clinician sessions on the single OCI host, and a named first bottleneck (DB CPU, PHP-FPM workers, disk I/O)?  (Performance & Scalability)
❓ [PERF-10] Which platform quotas sit on the critical path (Cloudflare Free-plan request/rule limits, OCI instance vCPU/network caps, MySQL max_connections) and what is their headroom versus projected peak concurrent usage?  (Performance & Scalability)
❓ [RES-11] Beyond the processor list in CONFIRMED-FACTS.md (OCI, Cloudflare Free, SiteGround SMTP relay), is there a vendor SLA reconciliation against this app's own SLOs, a documented fallback/outage runbook per critical vendor, and a concentration-risk note?  (Resilience & Fault Tolerance)
❓ [RES-12] DEPLOY-LARAVEL.md documents that Coolify starts the new container, health-checks it, repoints Traefik, then stops the old one, which materially reduces but does not eliminate dropped-request risk. Is there a documented SIGTERM grace period or drain deadline for the outgoing container, and does the Nixpacks-generated start command (not checked into this repo) trap SIGTERM to finish in-flight requests before the process exits?  (Resilience & Fault Tolerance)
❓ [RES-13] Is any fault-injection or GameDay practice planned for this system beyond the existing monthly DB restore drill (which proves backup/restore, not live fault tolerance)?  (Resilience & Fault Tolerance)
❓ [DATA-01] Is the current <=24h RPO (nightly-dump-only, no PITR) an accepted business trade-off for this clinical unit, or does a hospital-level BIA require tighter recovery for admissions/discharges/consultations?  (Data, Backups, DR & Correctness)
❓ [DATA-12] Is there a written Business Impact Analysis and a crisis-communication plan, and is there a second person (beyond the sole GitHub collaborator / system owner) who can deploy, restore a backup, or access the backup encryption key if the owner is unavailable?  (Data, Backups, DR & Correctness)
❓ [DATA-14] What lifecycle/retention rule is actually configured on the dmc-db-backups OCI bucket today - the runbook marks the 90-day figure as a legal placeholder set outside these scripts and not yet confirmed?  (Data, Backups, DR & Correctness)
❓ [CFG-03] Coolify env-var store is the documented production secret store and secrets are confirmed not committed - but the repo cannot show whether Coolify env access is itself access-controlled/audited, or whether the OCI IAM customer-secret-key pair behind AUDIT_S3_ACCESS_KEY/SECRET is scoped by IAM policy to only the dmc-audit-log and dmc-db-backups buckets rather than broader account access. Please confirm.  (Config, Secrets & Environments)
❓ [CFG-02] The 2026-09-02 audit found a legacy DB password and the info@dmc-im.com SMTP password committed in a now-purged commit (6c647a5). Purging history proves the current tree/clone is clean; it does not prove those two credentials were rotated. Were they rotated, and is info@dmc-im.com (which still carries live OTP/reset mail per CONFIRMED-FACTS.md B6) confirmed to be running with a password never exposed in that commit?  (Config, Secrets & Environments)
❓ [CFG-07] The 2026-09-02 audit reported (via live SSH, orchestrator-verified) that production .env actually had LOG_LEVEL=debug and expose_php=On, diverging from the documented/intended LOG_LEVEL=warning - both were noted as bounded-impact since APP_DEBUG was already correctly false. This auditor is static-only and cannot reach the live host to confirm whether that drift was corrected in the 2026-09-03 remediation; recommend a one-time operator check of the live .env and php.ini.  (Config, Secrets & Environments)
❓ [CMP-02] How are patient/staff access, erasure, portability and rectification requests actually fulfilled today, within what time bound, and is there a request log? DPO.md R6 names the HIM office as the verification point but EVIDENCE-PACK.md marks this OWNER (not yet supplied).  (Compliance, Privacy & Legal)
❓ [CMP-03] What is the target date for signing the controller-processor contract (G12, top priority) and the OCI / Cloudflare / SMTP-relay DPAs, and who (DPO once appointed, or the owner in the interim) will formally sign off DPIA.md / ROPA.md / INCIDENT-RESPONSE.md as adopted rather than draft?  (Compliance, Privacy & Legal)
❓ [CMP-06] Does counsel confirm the PDPL Implementing-Regulations DPO-appointment trigger applies here (health data at hospital scale), and if so, who will be designated and by when?  (Compliance, Privacy & Legal)
❓ [general] EVIDENCE-PACK.md section 2 row P8 and ROPA.md section 2 both describe the GitHub repo as private, while CONFIRMED-FACTS.md D3/G10 state it is deliberately PUBLIC until go-live -- worth reconciling so the evidence pack does not misstate this to an external auditor.  (Compliance, Privacy & Legal)
❓ [COST-01] Is cost-allocation tagging/labelling enforced on the OCI/Coolify/Cloudflare/S3-bucket resources at the console level, since there is no IaC in this repo to check?  (Cost, FinOps & Sustainability)
❓ [COST-02] Is a billing budget with an alert threshold configured for the OCI account, and is any per-patient/per-episode unit-cost metric tracked?  (Cost, FinOps & Sustainability)
❓ [COST-03] Is OCI cost-anomaly detection (or an equivalent) enabled and routed to an on-call/owned channel?  (Cost, FinOps & Sustainability)
❓ [COST-04] Is the single OCI Ampere instance on a committed-use/reserved rate, or is on-demand an intentional, reviewed decision?  (Cost, FinOps & Sustainability)
❓ [COST-06] Who owns hosting-cost review for this system and at what cadence; has a crawl/walk/run FinOps maturity stage ever been named?  (Cost, FinOps & Sustainability)
❓ [DOC-07] Is there, or should there be, a named on-call or escalation path beyond the owner for an incident when the owner is unavailable, and should that be written down (CODEOWNERS or an Operations block)?  (Documentation & Runbooks)
❓ [DOC-09] Is there a plan to periodically re-check operational runbooks (DEPLOY-LARAVEL.md, RELEASE-CHECKLIST.md) against the live CI and feature state after changes like the 2026-09-03 CI hardening, so they do not silently drift the way they did here?  (Documentation & Runbooks)
❓ [OPS-01] Has a formal production-readiness / launch review (architecture, capacity, failure modes, monitoring, rollout) been completed and signed off, particularly ahead of the planned cutover from the legacy dmc-im.com system to this app as the daily system of record? RELEASE-CHECKLIST.md and DEPLOY-LARAVEL.md cover release mechanics but not a one-time go-live/cutover review.  (Operational Readiness & Incidents)
❓ [OPS-03] Do the existing in-app signals (backup.stale, audit.integrity_failure, security.failed_logins, dq:notify) reach anyone outside business hours, or are they only seen when an admin happens to check the bell/e-mail? Is any external paging/alerting tool in use that is not reflected in the repo?  (Operational Readiness & Incidents)
❓ [OPS-07] Beyond the backup/restore automation already shipped, is manual operational toil (secret rotation, deploy verification, data reloads - all heavily manual per DEPLOY-LARAVEL.md) tracked anywhere, and is there a plan to automate the highest-frequency manual steps?  (Operational Readiness & Incidents)
❓ [OPS-12] Is the SECURITY_CONTACT / security.txt mailbox (currently defaults to mailto:info@dmc-im.com, the legacy US-hosted general inbox) actively monitored as a security-report inbox, or should a dedicated address be created before go-live as SECURITY.md itself flags?  (Operational Readiness & Incidents)
❓ [UX-01] No lighthouse/screenshot runtime hook was enabled for this audit. Have the key screens been visually verified at mobile (375px) and desktop (1440px) widths, including tap-target sizing, on a real rendered browser?  (Visual QA & Accessibility)
❓ [UX-08] The vitest-axe CI gate currently mounts only 3 of the app roughly 35 Inertia pages (the consultation-ledger module). Is there a plan to widen automated axe coverage to the rest of the app, starting with the auth/admission pages where UX-04 found real defects axe would likely have caught?  (Visual QA & Accessibility)
❓ [I18N-01] Is there a Unicode round-trip test (emoji + CJK + an RTL string) that POSTs through a real endpoint and reads the value back byte-identical from MySQL? None was found under laravel/tests/; non-blocking given the charset config is already correct.  (i18n, l10n & Text)
❓ [I18N-04] Is the PrivacyNoticeTest EN/AR shape-parity check (or any CI job) a required status check on main? CLAUDE.md section 12 states required status checks on main are not yet configured, so today a broken translation parity test would not by itself block a merge; non-blocking, but worth closing alongside the general required-checks gap.  (i18n, l10n & Text)
❓ [I18N-05] Has non-ASCII (accented Latin, Arabic-script) text ever been entered into a users.username or users.email value in production, and if so, was a case-insensitive duplicate ever missed by the mismatch between PHPs strtolower() and MySQLs utf8mb4_unicode_ci collation? No incident evidence was found either way.  (i18n, l10n & Text)

## Waived (risk-accepted)
None — no .prod-ready/waivers.yml.

## What's Good (curated across categories)

- External header/TLS audit **Grade A**: nonce CSP, 1-year preloaded HSTS, TLS 1.3, Secure/HttpOnly/SameSite session cookie, `frame-ancestors 'none'`, `no-store` on authenticated pages.
- Every guest-reachable endpoint has a named rate limiter; heavy PDF/XLSX generation is admin-gated.
- Mandatory TOTP MFA, mandatory e-mail verification, step-up re-auth on destructive admin actions, proxies pinned to Cloudflare ranges.
- Encrypted, off-box, in-Kingdom nightly backups with a daily verify beacon and a **logged restore drill** (a prior Critical, now closed).
- Clinical narratives encrypted at rest; `APP_KEY` escrowed; MFA secrets and SMTP password encrypted.
- Tamper-evident audit chain (sha256 `prev_hash`/`row_hash`), verified nightly, shipped hourly to a write-once in-Kingdom bucket; PHI reads and exports audited.
- Patient identifiers never in URLs (POST-only search and merge).
- CI is real and green: four SHA-pinned jobs under a read-only token — Vitest+axe, PHPUnit two-pass, gitleaks, Semgrep — plus a blocking composer-audit gate and a build-reproducibility check.
- Historic secret leak **confirmed purged** by an independent full-history gitleaks scan.
- All 383 dependencies licence-clean; ApexCharts replaced by MIT Chart.js.
- `/health` deep probe (DB, storage, scheduler heartbeat) and a scheduler liveness beacon.
- Schema-derived ROPA, DPIA, incident runbook, retention, classification, DPO and transfer drafts exist with sourced, proposed citations and an owner-confirmed facts record.

## Top fixes (prioritised by severity × blast radius ÷ effort)

1. **Gate `main` with the four CI checks and deploy only on green** — add required status checks (Frontend, Backend, Secret scan, SAST) to `main` branch protection and, if Coolify *Auto Deploy* is on, trigger it from the CI success job instead of raw push. Closes both Criticals' substance. [CICD-04, CICD-08, CICD-11]
2. **Switch sessions and cache to the database** — `SESSION_DRIVER=database`, `CACHE_STORE=database` in the Coolify runtime env, then restart (tables already exist). Makes password-reset / MFA-reset / self-change session revocation actually work, survives redeploys, and unblocks a second replica. [SEC-04, PERF-07]
3. **Bound the SMTP call** — set `timeout` (e.g. 10 s) on the `smtp` mailer in `config/mail.php`; with a sync queue an unbounded relay call ties up PHP-FPM workers on every OTP and reset. [RES-01]
4. **Bind "today" from the app clock instead of raw `CURDATE()`** in `DataQualityController`, `DashboardController` and `RegistryController` (the pattern `ConsultationDashboardController::ageing()` already uses), so LOS/delay/occupancy stop resolving to the DB host's UTC day (a ~3-hour daily misdating window). **Do NOT do this by pinning the MySQL session time zone**, which the auditor suggested: the schema's `TIMESTAMP` columns (`created_at`, `assigned_at`, `mfa_enrolled_at`, …) are re-interpreted through the session zone and every stored value would shift by three hours on read. [I18N-02] *(shipped 2026-09-03 with `tests/Feature/AppClockDayBoundaryTest.php`)*
5. **Make CI test the shipped runtime** — pin `php-version: 8.3` and a `mysql:8.4` service in `laravel-ci.yml` (today: PHP 8.4 + MySQL 8.0); wire `vendor/bin/pint --test` into the backend job and add a coverage floor. [CFG drift, TST-02, TST-04]
6. **Name the people and sign the paper** — fill the incident-runbook role holders and an on-call/escalation path, appoint the DPO, sign the controller–processor contract with the operator company and the processor DPAs. [OPS-02, CMP-03, CMP-06]
7. **Give the login and admission forms accessible names** — pair every `<label>` with its control via `for`/`id` (the `fid()` pattern already used in `PatientForm.vue`) in `Auth/Login.vue` and `Admissions/Create.vue`. [UX-04]

Also cheap and worth doing: structured JSON logs to stdout + persist `storage/logs` (or an error-tracking SDK) [OBS-01/03]; fix the three runbooks that still say CI is billing-blocked and `/health` isn't shipped [DOC]; rename the session cookie with the `__Host-` prefix [sec-web SPC-WEB-001]; plan binlog shipping and a second in-Kingdom region (Jeddah) for backups [DATA-02/04].

## Footer

```
Re-run:  /prod-ready:audit            Deep-dive: /prod-ready:<category>
Fix safe items: /prod-ready:fix       CI gate: /prod-ready:audit --ci  (exit <2 unless BLOCKED)
Cache:   laravel/.prod-ready/  (git-ignored; a dated copy of this report is committed under docs/compliance/evidence/)
Engine:  prod-ready@prod-ready-marketplace (18-category engine at Documents/prod-ready-plugin) · auditors ran on claude-sonnet · orchestrated 2026-09-03
```
