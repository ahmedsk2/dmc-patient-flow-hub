# Comprehensive Review — 2026-06-09 (Laravel go-forward system)

> Read-only audit at `000c03b` (clean tree, 23/23 tests green). Four parallel audit tracks
> (functionality, security, performance, quality/testing) + a live UI/accessibility pass.
> Wave A + #1 implemented at `00644a5`. This file is the tracked record; severity/effort per item.

## Baseline
`laravel-replatform` @ `000c03b` · PHPUnit 23/137 green · CI `laravel-ci.yml` (MySQL + artisan test) · npm audit prod = 0 · composer audit not runnable locally (added to CI).

## Verified strong (not findings)
No SQLi (all raw SQL static or `?`-bound) · mass-assignment closed (explicit validated arrays) · CSRF/XSS clean · audit log append-only, session-sourced · MFA secret encrypted, recovery codes hashed+single-use · password reset via broker · object-level ownership (primary-consultant-or-can_manage) enforced server-side · 72h-readmission identical in all 3 controllers · discharge re-entry guards · transfer atomic · stats bucketing correct · **no N+1 queries** (measured) · code-splitting correct · secrets out of source.

## Findings (status as of `00644a5`)

| # | Area | Sev | Finding | Status |
|--|--|--|--|--|
| 1 | Security/PHI | HIGH | Registry/Reports/Statistics + CSV/XLSX exports reachable by any authenticated user (incl. Observer) | ✅ FIXED — admin-gated, nav moved, tests |
| 2 | Perf | HIGH | `outcome` + `transfer_type` unindexed → full scans (death metrics; 1.1s readmit self-join) | ✅ FIXED — migration 16 composite indexes |
| 3 | Security | HIGH | No rate-limit on login / MFA / forgot-password | ✅ FIXED — throttle:5,1 + test |
| 4 | Correctness | HIGH | `completeDischarge` doesn't require prior medical discharge; closes file with outcome=NULL | Wave B |
| 5 | Testing | HIGH | Authz matrix, statistics VALUES, shuffle, 2FA, password reset untested | Wave C |
| 6 | Perf | MED | Nothing cached; `Setting::current()` re-queried per request | ✅ FIXED — once() memoization + test |
| 7 | Security | MED | TOTP replayable within ±90s window | Wave B |
| 8 | Correctness | MED | Import hard-codes `transfer_type='discharge from ward'` for ICU rows | Wave B |
| 9 | Maintainability | MED | Readmit predicate ×3, nonIcu ×3, consultant list ×6 duplicated | Wave D (gated on C) |
| 10 | Maintainability | MED | No FormRequests; rule blocks duplicated store↔modify | Wave D |
| 11 | Tooling | MED | CI ran only PHPUnit | ✅ FIXED — +composer/npm audit, npm build |
| 12 | Observability | MED | No error monitoring (Sentry/Flare) | Open (ops) |
| 13 | Supply chain | MED | `laravel/pao` unrecognized | ✅ VERIFIED legit (github.com/laravel/pao) |
| 14 | Perf | MED | Diagnosis `%LIKE%` scans 72k rows | OWNER: keep as-is (admin-only now) |
| 15 | A11y | MED | Icon-only buttons no aria-label (~600); `<th>` no scope; no chart alt; no reduced-motion | Wave E |
| 16 | Correctness | LOW | LOS float→int deprecation per board row | ✅ FIXED + test |
| 17 | Correctness | LOW | Stale med-discharge fields on transfer; reverse w/o same-day guard; `is_new` never resets | `is_new`: Wave B (24h); rest open |
| 18 | UX | LOW | Diagnosis mode shows inert filter chips | Open |
| 19 | Hardening | LOW | Blanket `$guarded`; prod env flags; stale doc counts | Open (deploy checklist) |

## Owner decisions (recorded)
- Analytics/exports **admin-only**; dashboard = non-admin analytics. ✅ implemented
- Shuffle: load = active **non-ICU** (long-term counts); rounds min_hosp → min_subs → max_hosp → max_subs → overflow. → Wave B
- `is_new` = within **24h of assignment** (rolling). → Wave B
- Mortality % and transfer-out-as-discharge **kept as legacy** (reconciliation preserved).
- Diagnosis search stays substring `LIKE` (no FULLTEXT).

## Sequenced plan
A ✅ → B (4,7,8 + shuffle + is_new) → C (coverage protecting authz/metrics) → D (dedupe/FormRequests, gated on C) → E (a11y) · NEEDS-OWNER items stay out until decided.
