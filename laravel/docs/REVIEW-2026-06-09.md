# Comprehensive Review — 2026-06-09 (Laravel go-forward system)

> Read-only audit at `000c03b` (clean tree, 23/23 tests green). Four parallel audit tracks
> (functionality, security, performance, quality/testing) + a live UI/accessibility pass.
> Wave A + #1 implemented at `00644a5`. This file is the tracked record; severity/effort per item.

## Baseline
`laravel-replatform` @ `000c03b` · PHPUnit 23/137 green · CI `laravel-ci.yml` (MySQL + artisan test) · npm audit prod = 0 · composer audit not runnable locally (added to CI).

## Verified strong (not findings)
No SQLi (all raw SQL static or `?`-bound) · mass-assignment closed (explicit validated arrays) · CSRF/XSS clean · audit log append-only, session-sourced · MFA secret encrypted, recovery codes hashed+single-use · password reset via broker · object-level ownership (primary-consultant-or-can_manage) enforced server-side · 72h-readmission identical in all 3 controllers · discharge re-entry guards · transfer atomic · stats bucketing correct · **no N+1 queries** (measured) · code-splitting correct · secrets out of source.

## Findings (status as of `00644a5`)

| # | Area | Sev | Finding | Status |
|--|--|--|--|--|
| 1 | Security/PHI | HIGH | Registry/Reports/Statistics + CSV/XLSX exports reachable by any authenticated user (incl. Observer) | ✅ FIXED — admin-gated, nav moved, tests |
| 2 | Perf | HIGH | `outcome` + `transfer_type` unindexed → full scans (death metrics; 1.1s readmit self-join) | ✅ FIXED — migration 16 composite indexes |
| 3 | Security | HIGH | No rate-limit on login / MFA / forgot-password | ✅ FIXED — throttle:5,1 + test |
| 4 | Correctness | HIGH | `completeDischarge` doesn't require prior medical discharge; closes file with outcome=NULL | ✅ FIXED — guard + test (Wave B) |
| 5 | Testing | HIGH | Authz matrix, statistics VALUES, shuffle, 2FA, password reset untested | ✅ CLOSED — shuffle (4), 2FA replay+recovery (2), discharge guard, import, analytics-authz, **statistics/dashboard VALUE tests** (hand-computed fixture; CI proxy for the reconciliation), password-reset (2), registry modes incl. dx AND/OR (3). Optional residue: deeper per-role matrix sweep, export-content tests |
| 6 | Perf | MED | Nothing cached; `Setting::current()` re-queried per request | ✅ FIXED — once() memoization + test |
| 7 | Security | MED | TOTP replayable within ±90s window | ✅ FIXED — mfa_last_counter replay guard + test (Wave B) |
| 8 | Correctness | MED | Import hard-codes `transfer_type='discharge from ward'` for ICU rows | ✅ FIXED — derive from location + test (Wave B) |
| 9 | Maintainability | MED | Readmit predicate ×3, nonIcu ×3, consultant list ×6 duplicated | ◑ CORE FIXED — `Admission::NON_ICU_SQL` + `REAL_DISCHARGE_TYPES` constants referenced by all 6 sites (zero query-shape change; recon 317 re-verified). Consultant-list dedupe + fuller MetricsService extraction still optional |
| 10 | Maintainability | MED | No FormRequests; rule blocks duplicated store↔modify | Wave D |
| 11 | Tooling | MED | CI ran only PHPUnit | ✅ FIXED — +composer/npm audit, npm build |
| 12 | Observability | MED | No error monitoring (Sentry/Flare) | ◑ OPS-READY — `.env.example` + DEPLOY doc now specify `LOG_CHANNEL=daily`, `LOG_LEVEL=warning`, and the Sentry install step (`composer require sentry/sentry-laravel` + DSN, inert while empty). Package install happens at deploy (no composer locally) |
| 13 | Supply chain | MED | `laravel/pao` unrecognized | ✅ VERIFIED legit (github.com/laravel/pao) |
| 14 | Perf | MED | Diagnosis `%LIKE%` scans 72k rows | OWNER: keep as-is (admin-only now) |
| 15 | A11y | MED | Icon-only buttons no aria-label (~600); `<th>` no scope; no chart alt; no reduced-motion | ◑ MOSTLY FIXED — board+layout icon buttons labeled (615→0), board `<th scope>`, global `prefers-reduced-motion`; chart text-alternative + secondary-page table scope still open |
| 16 | Correctness | LOW | LOS float→int deprecation per board row | ✅ FIXED + test |
| 17 | Correctness | LOW | Stale med-discharge fields on transfer; reverse w/o same-day guard; `is_new` never resets | ✅ FIXED — transfer clears superseded medical-discharge fields + doesn't carry `bed`; reverse-discharge limited to **SAME-DAY** (legacy undo window; the Recent registry *lists* yesterday+today but undo only works on today's rows); `is_new` rolling 24h. All tested |
| 18 | UX | LOW | Diagnosis mode shows inert filter chips | ✅ N/A — verified: mode switch resets all filters (`preserveState:false`) and each mode renders only its own (applied) inputs; the audit's scenario cannot occur |
| 19 | Hardening | LOW | Blanket `$guarded`; prod env flags; stale doc counts | ✅ CLOSED — env flags + timezone + log rotation in `.env.example`/DEPLOY doc; doc test-count synced (49/325). `$guarded` DELIBERATELY KEPT: every write is an explicit validated array, importer is raw-SQL, and switching to `$fillable` risks silently dropping fields on under-tested admin edit paths — worse than the latent risk it removes |

## Owner decisions (recorded)
- Analytics/exports **admin-only**; dashboard = non-admin analytics. ✅ implemented
- Shuffle: load = active **non-ICU** (long-term counts); rounds min_hosp → min_subs → max_hosp → max_subs → overflow. → Wave B
- `is_new` = within **24h of assignment** (rolling). → Wave B
- Mortality % and transfer-out-as-discharge **kept as legacy** (reconciliation preserved).
- Diagnosis search stays substring `LIKE` (no FULLTEXT).

## Sequenced plan
A ✅ → B ✅ (4,7,8 + shuffle + is_new) → C ◑ (shuffle/2FA/discharge/import/analytics-authz done; statistics-VALUES + password-reset + registry-mode tests pending) → D (dedupe/FormRequests, gated on the statistics-VALUE tests) → E ◑ (board+layout a11y done; chart text-alt + secondary-page scope pending) · NEEDS-OWNER items stay out until decided.

## Remaining (2026-06-10 — review fully worked through)
- **Done this pass:** #17 (transfer hygiene + 48h reverse guard, tested), #18 (verified N/A), #19 (env/docs; `$guarded` deliberately kept — see table), #12 (ops-ready), consultant-dropdown dedupe (`User::consultantOptions()`, 5 sites), FormRequests (`StoreAdmissionRequest`/`ModifyAdmissionRequest`/`ConsultationRequest` — validation AND the matching authz gates in `authorize()`, removed from controllers), `<th scope>` on ALL pages, `role="img"`+`aria-label` on all 12 charts.
- **Deliberately deferred:** Vue composables (`useFilteredList`/`useActionModal`) — cosmetic dedupe with no component-test safety net; do alongside introducing component tests (Vitest) so the refactor is guarded. Fuller `MetricsService` extraction — optional; the constants + value tests already pin the definitions.
- **Ops-only (at deploy):** rotate creds, TLS + `SESSION_SECURE_COOKIE`, backups, `composer require sentry/sentry-laravel` + DSN, set real `ward_beds`/`icu_beds` in Control, official EHC logo asset.
- **Suite: 49 tests / 325 assertions, green.**
