*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build  (committed: deploy host has no Node)
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# prod-ready audit cache + reports (internal, contains infra detail) — except the waiver file,
# which is the change-controlled risk-acceptance record and must live in the repo
/.prod-ready/*
!/.prod-ready/waivers.yml
__pycache__/

# PHPUnit Clover output written by CI pass 1 (scripts/coverage-gate.php reads it)
/coverage
